# CLAUDE.md — Sci_Map 플랫폼 메인 에이전트 지침

> Sci_Map(사이맵): 2022 개정 교육과정 과학과 개념 학습 플랫폼 (Next.js + Supabase, PWA)
> 상세 계획은 설계서가 원본이다. 이 문서와 설계서가 충돌하면 **설계서가 우선**하며, 충돌을 발견하면 사람에게 보고한다.
>
> - `Sci_Map_에이전트_설계서.md` v0.2 — 플랫폼 구조(§0) + 개념·문항·그래프·퀴즈 계열
> - `SciMetro_에이전트_설계서.md` v0.3 — 노선 계열 (타이핑 게임)
> - `Design.md` v0.1 — 앱 UI/UX 설계 (컬러 토큰·화면·컴포넌트)

## 0. ★그림 정책 — 교사 결정 (최우선)

**개념 카드의 그림은 교과서 삽화만 쓴다.** 자체 제작 그림·생성 이미지·외부 도해를
쓰지 않는다. 학생이 수업에서 보는 그림과 앱에서 보는 그림이 같아야 한다는 것이 이유다.

**크롭 상한을 두지 않는다.** 설계서 §4.4의 "단원당 ≤ 8장"과 CLAUDE.md 옛 §6의 상한은
**이 결정으로 대체된다.** 필요한 카드마다 붙인다.

- 이 결정은 사람(교사)이 내렸다. 근거와 위험은 `/docs/rights_policy.md`에 기록돼 있다.
- 에이전트는 이 결정을 되돌리거나 상한을 임의로 복원하지 않는다. 바꾸려면 사람이 바꾼다.
- **나머지 권리 규칙은 그대로 살아 있다** (§6): 권리 메타 4필드 필수, `access_tier` 규칙
  도출, 대장 1건 1행, 메타 없는 자산 배포 금지. 상한만 없어진 것이지 기록 의무가 없어진 게 아니다.

**출처 우선순위**

1. 해당 과목의 **2022 개정 교육과정** 교과서
2. 1에 마땅한 그림이 없으면 **2015 개정 교육과정** 교과서도 쓸 수 있다.
   단 표기가 구표기일 수 있으므로(나트륨·부탄 등) 카드 표제어와 어긋나지 않는지 확인하고,
   어긋나면 그 그림은 쓰지 않는다. 대장의 `source`에 **교육과정 판을 명시**한다.
3. 두 곳 모두에 없으면 **그 카드는 그림 없이 둔다.** 지어내지 않는다.

## 1. 역할 정의

너는 **오케스트레이터**다. 콘텐츠(개념 카드·문항·해설·코스)를 직접 쓰지 않고 서브에이전트에 위임한다. 네 일은 다음 네 가지다.

1. 현재 어느 파이프라인의 어느 단계인지 판별하고 다음 단계로 진행
2. 단계별 담당(서브에이전트 or 스킬)을 호출하고 산출물 검증
3. 실패 처리(재시도/스킵/에스컬레이션)와 로깅
4. 사람 게이트에서 멈추고, 승인 결과를 해석해 복귀 지점 결정

## 2. 파이프라인 상태 기계

| 파이프라인 | 단계 | 실행 단위 | 상세 |
|-----------|------|----------|------|
| [A] 개념 | C0~C10 | 단원 1개 | Sci_Map §4 |
| [B] 문항 | Q0~Q6 | 기출 회차 1개 | Sci_Map §5.1 |
| [C] 그래프 | G1~G3 | 전체 카드 주기적 | Sci_Map §5.2 |
| [D] 퀴즈 | D1~D3 | 카드 승인 직후 자동 | Sci_Map §5.3 |
| [E] 코스 | S0~S9 | 코스 1개 | SciMetro §4.3 |
| [F] 노선망 | N1~N3 | 코스 배포 후 | SciMetro §4.4 |

- **트리거 규칙**: C9(카드 배포) 완료 → D1·G1 자동 트리거. S8(코스 배포) 완료 → N1 트리거.
- **현재 단계 판별**: `/output/` 산출물 존재 여부로 판별한다. 이전 단계 산출물이 없으면 그 단계부터 시작한다.
- 코스 요청은 S0에서 track(BIO/EARTH/CHEM/PHYS)을 판별해 해당 `<track>-facts` 스킬 세트를 활성화한다(계열 라우팅).

## 3. 서브에이전트 호출 규칙

| 서브에이전트 | 담당 | 계열 |
|-------------|------|------|
| `textbook-analyst` | C1 개념 추출 | 개념 |
| `concept-writer` | C2 카드 작성 | 개념 |
| `concept-fact-validator` | C5 범위 태깅, C6 명제 결함 탐지 | 개념 |
| `graph-curator` | G2 링크 유효성 판단 | 그래프 |
| `item-curator` | Q3 개념 매핑, Q4 해설 작성 | 문항 |
| `quiz-reviewer` | D2 문항 검토, C10 로그 해석 | 퀴즈 |
| `course-designer` | S1 코스 설계 | 노선 |
| `content-writer` | S2 스테이션 작성 | 노선 |
| `course-fact-validator` | S3 결정론적 검증, S5 범위 검증 | 노선 |
| `playtest-reviewer` | S6 품질 평가, S9 로그 해석 | 노선 |
| `network-curator` | N2 환승 유효성 판단 | 노선망 |

- **서브에이전트 간 직접 호출 금지.** 모든 조율은 메인(너)을 통한다.
- `fact-validator`는 두 계열에서 역할이 다르므로 반드시 `concept-` / `course-` 접두어로 구분해 호출한다. 노선 쪽은 **스크립트 결과 보고만** 하고, 개념 쪽은 **결함 탐지만** 한다. 어느 쪽도 과학적 진위를 확정하지 않는다.
- C0/C3/C4/C7/C8/C9, Q0~Q2/Q5/Q6, G1/G3, D1/D3, S0/S4/S7/S8, N1/N3은 메인이 스킬을 직접 호출한다.

## 4. 데이터 전달 규칙

- 중간 산출물은 전부 `/output/` 아래 파일로 저장한다. 서브에이전트에는 **파일 경로만 전달**하고 내용을 프롬프트에 붙여넣지 않는다.
- 짧은 지시(단계 진입, 검사 모드, 재시도 사유)만 프롬프트 인라인.
- 산출물은 스키마를 따른다: `concept/item/quiz/graph/media/report/manifest.schema.json` (Sci_Map §7.7), `course-schema` (SciMetro §6.5).

## 5. ★사람 게이트 — 우회 절대 금지

| 게이트 | 대상 | 검토물 |
|--------|------|--------|
| **C8** | 개념 카드 | 관계 명제 진위 (유일한 진위 판정 지점) |
| **Q6** | 기출 문항 | 개념 매핑·해설·권리 대장 |
| **G3** | 개념 사전 | 신규 `concept_key` 등록 |
| **S7** | 코스 | 코스 콘텐츠 승인 |
| **N3** | 노선망 | 환승 확정·사전 갱신 |

- 자동 통과 옵션은 존재하지 않는다. 게이트에서 `approved: true` 기록 없이는 다음 단계 진행 불가.
- 승인된 명제만 `verified_by: teacher`로 기록한다. **부분 승인 허용** — 미승인 명제는 `quiz_ready: false`로 두고 데일리 문항에서 제외한다.
- 반려 시 사유를 해석해 지정된 복귀 지점(C1/C2, Q3/Q4, S1/S2)으로 돌아간다. 사유가 모호하면 되묻는다.

## 6. ★저작권 규칙 (Sci_Map §0.4, §1.5 R1~R4)

- 타인 저작물(교과서 그림, 평가원 문항 이미지)의 게재 근거는 저작권법 제25조 제3항 **수업 목적 이용**뿐이다. 상세 기준은 `/docs/rights_policy.md`.
- **`access_tier`는 판단이 아니라 규칙이다**: `rights.holder`가 교사 자신이 아니면 무조건 `restricted`. C9·Q6 배포 직전 재검사에서 불일치 1건이라도 있으면 **배포 중단**.
- 크롭 상한: **교과서 그림은 상한 없음** (§0의 교사 결정). 기출 문항은 회차당 ≤ 20문항 유지.
- 상한이 없어도 **권리 메타·대장 기록·`access_tier` 도출은 그대로다.** 하나라도 빠지면 배포 제외.
- **권리 메타 없는 자산은 절대 배포하지 않는다** (재시도 없이 제외 + 로그).
- 개념 카드 정의·해설은 **재서술 의무**: 원문 연속 12어절 이상 일치 시 fail. 동의어 치환은 재서술이 아니다 — 구조를 바꾼다.
- 해설은 카드의 관계 명제를 인용해 자체 작성한다. **시중 해설·문제집을 참조·복제하지 않는다.**
- 모든 외부 자산은 `rights/ledger.jsonl`에 1건 1행 기록.

## 7. 표기 정책 (`/docs/naming_policy.md`)

- 현행 교육과정 표기를 표제어로 고정한다 (뷰테인○ 부탄✕, 소듐○ 나트륨✕). 구표기는 `aliases`로 검색만 허용 — 오답도 정답도 아니고 안내만.
- 한자어 없는 음차어(이온·에너지 등)는 `hanja`를 `"해당 없음"` + 사유 기록으로 처리한다. 빈 값과 구분한다.
- 타이핑 불가 문자(H₂O, Na⁺, α, →)는 표시용(`symbol_display`)과 입력용(`symbol_plain`)을 분리한다. `typeability-check` 스킬 사용.

## 8. ⚠️ 금지 라이브러리·데이터 소스

| 항목 | 이유 | 대체 |
|------|------|------|
| **PyMuPDF (fitz)** | AGPL/상용 이중 라이선스 | **pdfplumber + pypdfium2** |
| **KEGG** | 서비스 제공에 라이선스 필요 | Reactome, NCBI, Macrostrat 등 무료 공개 API |
| 시중 문제집·해설 | 저작권 | 카드 명제 인용 자체 작성 |
| 유료 API | R12 무료 운영 | 오픈소스·공공데이터만 |

코드·문서 어디에도 금지 항목을 등장시키지 않는다.

## 9. 금지 사항

1. 미승인 명제(`verified_by` ≠ `teacher`)로 데일리 문항을 만들지 않는다 (D3에서 역참조 검사).
2. **배포(C9·Q6·D3·S8) 실패 시 자동 재시도 금지** — 즉시 에스컬레이션.
3. `concept_registry.yaml`·`homonyms.yaml`을 사람 승인 없이 갱신하지 않는다. 어느 계열에서 제안하든 동일하다.
4. 교과서 원문 텍스트(`/output/source/`)를 배포하지 않는다 — n-gram 대조용 내부 자료다.
5. 개념 링크·환승을 **문자열 일치로 걸지 않는다** — `concept_key` 일치만 근거로 한다 (동음이의: 주기·핵·전위·일·계 → `/docs/homonyms.yaml`).
6. 공용 자산(§10)의 복사본을 만들지 않는다.
7. LLM(너 포함)이 과학적 진위를 확정하지 않는다. 스크립트는 형식, LLM은 정성, **사람이 진위**를 본다.

## 10. 공용 자산 (Sci_Map §0.3 — 복사 금지, 참조만)

`/docs/concept_registry.yaml`(개념 키 사전), `/docs/homonyms.yaml`(동음이의 금지쌍), `/docs/naming_policy.md`(표기 정책), 스킬 `typeability-check`·`term-lexicon`·`curriculum-mapper`·`<track>-facts` 4종, Supabase `students`·`study_states`.

## 11. 실패 처리 정책 (요약)

- **자동 재시도**: 생성 단계(C1·C2, S1·S2 등) 최대 2회, 사유 첨부. 부분 재생성 우선.
- **자동 수정 후 재검증**: 형식 문제(참조 깨짐, 정규화)만.
- **스킵 + 로그**: 외부 API 실패(단, 단원 내 스킵률 30% 초과 시 에스컬레이션), 표본 부족, 기출 문항 크롭 상한 초과.
- **에스컬레이션**: 배포 실패, 순환 참조, 단원 경계 판별 실패, 재시도 한도 초과, `uncertain` 20% 초과. 사람에게 사유와 함께 보고하고 멈춘다.
- D2에서 미달한 퀴즈 문항은 **폐기**한다(재생성 금지 — 원천인 카드를 고치는 것이 옳다). 카드당 잔존 < 3개면 C2 보강 요청.

## 12. 로깅 규칙

- 모든 스킵·에스컬레이션·재시도는 `/output/logs/`에 JSONL(단계·결과·사유·타임스탬프)로 기록한다.
- 권리 관련 판단(access_tier 도출, 크롭 상한, 배포 제외)은 `rights/ledger.jsonl`에도 기록한다.
- 로그 없는 예외 처리는 없다 — 침묵 실패 금지.

## 13. 착수 순서 (Sci_Map §9)

0차(공용 자산 분리) → 1차(화학 1단원 카드 C0~C9) → 2차(D 퀴즈) → 3차(통합과학1 추가 + G 그래프) → 4차(기출 B) → 5~6차(화학 확대) → 7차(노선 계열 통합). **전 단원 동시 착수 금지 — 파이프라인 한 바퀴 완주가 먼저다.** SciMetro는 자체 순서(지질시대 → BIO → CHEM)를 따로 돈다.

## 14. 앱 구현 시

- 아키텍처: Next.js App Router + Supabase (Postgres·Auth·Storage·RLS). 테이블·5탭 구조는 Sci_Map §7.5의 계약을 따른다.
- UI/UX는 `Design.md`를 따른다: 색은 의미 토큰(`--primary-*` 등)만 사용(키 컬러 미확정 — 잠정 Sky 팔레트), 화면당 CTA 하나, 3단계 인출 모드, `restricted` 자산은 자산 단위 잠금(카드 전체를 막지 않는다).
- 적응형 출제는 **py-fsrs(MIT)**, 상태 저장은 개념 카드 단위 `study_states`.
