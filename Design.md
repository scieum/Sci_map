# Sci_Map UI/UX 설계서 (Design.md)

> **Sci_Map(사이맵)** — 2022 개정 교육과정 과학과 개념어 학습 플랫폼의 UI/UX 설계 문서
>
> 버전: **v0.1** | 작성일: 2026-09-05 | 대상: Next.js + Supabase 웹앱 (PWA 모바일 대응)
> 상위 문서: `Sci_Map_에이전트_설계서.md` v0.2 (§7.5 앱 인터페이스 계약과 정합해야 한다)
>
> **키 컬러는 추후 확정한다.** 이 문서는 하늘색(Sky)·초록색(Green) 두 후보 팔레트를 토큰으로 정의하고,
> 모든 화면 설계는 토큰 이름으로만 색을 참조한다. 팔레트 교체 = CSS 변수 한 벌 교체다.

---

## 0. 레퍼런스 조사 요약

이 설계의 근거가 된 레퍼런스다. (유아이볼 UI DB + 선생우비 앱 조사, 2026-09)
2차 조사(온보딩·복습·리포트·게이미피케이션·랭킹·홈 구조)는 `UX_리서치_학습앱_구조.md` 참조.

| 레퍼런스 | 가져올 것 | Sci_Map 적용 지점 |
|----------|----------|------------------|
| **큐알교육학 한판노트** (선생우비/효시컴퍼니) | 대분류→중분류→소분류→토픽 **분류 트리** + "지금 어디를 공부하는지" 상시 노출, **인출→힌트→정답 3단계 모드**, 오답노트, 출석 잔디 | 개념 탭 트리, 카드 인출 모드(§5.2), 오늘 탭, 마이 리포트 |
| **플랭** (메인/학습 전) | 홈에서 선택지를 없애고 **CTA 하나**("오늘의 학습 시작하기")로 밀어주는 구조, 상단 스트릭 칩 | 홈(오늘 탭 진입) 화면(§4.4) |
| **말해보카** (학습 모드 해금) | 잠금→해금 진행감, COMPLETE 배지 | 단원 진행도·노선 완주 표시(§4.1, §4.5) |
| **틈틈잇** (콘텐츠 요약→OX퀴즈) | 읽기 화면 미니멀리즘(제목+본문+CTA 하나), 문항 카드 + 즉시 해설 + "글 보기(원문 복귀)" 버튼, 주제 브레드크럼 칩 | 카드 읽기 화면(§4.1), 퀴즈 결과 화면(§4.4) |
| **하이링구얼** (AI 피드백) | 본문 내 핵심어 **인라인 하이라이트** + 하단 설명 카드 | 카드 본문·해설 속 개념어 링크(§5.4) |

**요약된 6원칙** — 모든 화면 판단의 기준이다.

1. **위치 감각**: 어느 화면에서든 "이 개념이 전체 어디에 있는지"가 보인다 (브레드크럼 + 지도).
2. **인출 우선**: 보여주기 전에 가리고 떠올리게 한다 (인출→힌트→정답).
3. **화면당 CTA 하나**: 읽기 화면과 홈은 다음 행동 하나만 남긴다.
4. **즉시 해설·즉시 복귀**: 채점 화면에서 해설과 원 카드로 한 번에 간다.
5. **가벼운 지속 장치**: 스트릭·잔디·해금. 과한 게이미피케이션(포인트 상점 등)은 하지 않는다.
6. **개념어는 어디서든 링크**: 본문·해설·오답 속 개념어를 탭하면 그 카드로 간다.

---

## 1. 디자인 원칙

| # | 원칙 | 의미 | 반례(금지) |
|---|------|------|-----------|
| D1 | **학습 밀도 > 장식** | 화면의 잉크는 개념·명제·문항에 쓴다 | 배너, 일러스트로 채운 홈 |
| D2 | **한 화면 = 한 과업** | 읽기/풀기/확인을 화면 단위로 분리 | 카드 화면에 퀴즈·통계 동시 노출 |
| D3 | **이동이 곧 학습** | prereq/same/next 링크 이동을 1탭으로 | 링크가 검색을 거쳐야 닿는 구조 |
| D4 | **상태를 색이 아닌 색+형태로** | 정답/오답/잠금을 색상만으로 구분하지 않는다 (색각 접근성) | 초록/빨강 배경만으로 O/X 구분 |
| D5 | **잠금은 정중하게** | `restricted` 그림 잠금은 카드 열람을 막지 않고 그림 자리에만 안내 (§0.4 승계) | 로그인 강제 전면 모달 |
| D6 | **모바일 우선, 태블릿 확장** | 기본 뷰포트 360~430px. 768px 이상에서 2단 확장 | PC 전용 레이아웃 |

---

## 2. 컬러 시스템

### 2.1 토큰 구조 — 키 컬러 교체 가능 설계

키 컬러는 추후 결정한다. 따라서 컴포넌트는 **의미 토큰만** 참조하고, 의미 토큰이 후보 팔레트(램프)를 가리킨다.

```css
/* globals.css — 의미 토큰. 컴포넌트는 이것만 쓴다 */
:root {
  --primary-50:  var(--ramp-50);
  --primary-100: var(--ramp-100);
  --primary-300: var(--ramp-300);
  --primary-500: var(--ramp-500);   /* 브랜드 기본. CTA·활성 탭·링크 */
  --primary-600: var(--ramp-600);   /* CTA hover·pressed */
  --primary-700: var(--ramp-700);   /* 텍스트로 쓸 때 (흰 배경 대비 확보) */

  --bg:          #FFFFFF;
  --bg-subtle:   #F6F8FA;           /* 카드 리스트 배경 */
  --surface:     #FFFFFF;           /* 카드 표면 */
  --border:      #E4E8EC;
  --text:        #1A1F27;
  --text-sub:    #5B6472;
  --text-faint:  #98A1AD;

  --success:     #16A34A;           /* 정답·완료 */
  --danger:      #DC2626;           /* 오답·오개념 경고 */
  --warning:     #D97706;           /* 함정·주의 */
  --info:        #0E8F99;           /* same 링크 배지 등 정보성 (§2.2에서 민트로 이동) */
  --lock:        #6B7280;           /* 잠금 상태 */
}
```

### 2.2 키 컬러 — Indigo (2026-09-06 확정)

참고 이미지(보관 서비스 UI)에서 뽑았다. 지배색인 청보라 한 색으로 램프를 세우고,
같은 이미지의 민트를 `--info` 로 옮겼다.

| 토큰 | HEX | 용도 예 |
|------|-----|--------|
| `--ramp-50`  | `#EEF0FE` | 선택된 리스트 행 배경 |
| `--ramp-100` | `#DDE1FD` | 칩·배지 배경 |
| `--ramp-300` | `#A3ACF7` | 진행 바 트랙 강조 |
| `--ramp-500` | `#4B5BE8` | **CTA·활성 탭·주요 아이콘** (흰 텍스트 대비 5.3:1) |
| `--ramp-600` | `#3A48CC` | hover·pressed |
| `--ramp-700` | `#2C37A0` | 흰 배경 위 링크 텍스트 (대비 9.5:1) |

바뀐 것이 램프 6줄만은 아니다. 함께 옮긴 둘을 적어 둔다.

- `--info`: `#2563EB` → **`#0E8F99`** (민트). 파랑을 그대로 두면 `same` 링크 배지가
  인디고 primary 와 구분되지 않는다. `--info-bg` 도 `#E4F5F6` 으로 함께.
  이 토큰은 `related` 링크 배지도 함께 맡는다 — 링크 3종(먼저·다음·관련)이
  회색·인디고·민트로 갈려야 배지만 보고 종류를 안다.
- 페이지 바탕 `--bg` `#F3F5F8` → **`#F4F5FB`**, `--bg-subtle` → `#EEF0F7`.
  무채색에 옅은 라벤더 기를 넣어 인디고와 같은 계열로 묶었다.
- 브랜드색 그림자는 컴포넌트에 하드코딩돼 있었다(`rgba(24,159,230,…)` 10곳).
  `--glow-cta` / `--glow-hero` / `--glow-chip` 토큰으로 걷어 냈다 —
  **다음 번 팔레트 교체는 다시 램프 몇 줄로 끝난다.**

<details><summary>이전 후보 팔레트 두 벌 (기록용 — 채택되지 않음)</summary>

**후보 A — Sky (하늘색 계열)**

| 토큰 | HEX | 용도 예 |
|------|-----|--------|
| `--ramp-50`  | `#EFF9FF` | 선택된 리스트 행 배경 |
| `--ramp-100` | `#DEF2FF` | 칩·배지 배경 |
| `--ramp-300` | `#7CCFFF` | 진행 바 트랙 강조 |
| `--ramp-500` | `#189FE6` | **CTA·활성 탭·주요 아이콘** |
| `--ramp-600` | `#0E82C4` | hover·pressed |
| `--ramp-700` | `#0A669B` | 흰 배경 위 링크 텍스트 |

**후보 B — Green (초록색 계열)**

| 토큰 | HEX | 용도 예 |
|------|-----|--------|
| `--ramp-50`  | `#F0FAF4` | 선택된 리스트 행 배경 |
| `--ramp-100` | `#DCF4E4` | 칩·배지 배경 |
| `--ramp-300` | `#7DD8A0` | 진행 바 트랙 강조 |
| `--ramp-500` | `#1FA65A` | **CTA·활성 탭·주요 아이콘** |
| `--ramp-600` | `#178A4B` | hover·pressed |
| `--ramp-700` | `#116E3C` | 흰 배경 위 링크 텍스트 |

</details>

적용 규칙:

- 500은 흰 텍스트 대비 4.5:1 이상, 700은 흰 배경 텍스트 대비 4.5:1 이상을 유지한다(WCAG AA). 키 컬러 확정 시에도 이 두 조건을 통과하는 값으로 램프를 재생성한다.
- 채택한 Indigo 는 `--success`(초록)·`--warning`(주황)과 겹치지 않는다. 다만 `--info` 는 겹쳐서 민트로 옮겼다(위 표 아래 항목).
- 브랜드색은 화면 면적의 10% 이내로 쓴다. 나머지는 무채색 — 학습 콘텐츠가 주인공이다(D1).

### 2.4 보조 팔레트 — 같은 참고 이미지에서 (2026-09-06)

키 컬러를 뽑은 그 이미지에서 나머지 색도 함께 뽑았다. **이미지에 없는 색은 만들지
않았다** — 초록·노랑이 그렇다. 상태 색(`--success`·`--warning`)이 이미 그 자리를
쓰고 있으므로 억지로 채울 이유도 없었다.

각 계열은 세 단이면 족하다. 틴트 배경 / 채움 / 흰 배경 위 글자.

| 계열 | 50 (틴트) | 500 (채움) | 700 (글자) | 대비 |
|---|---|---|---|---|
| **azure** 밝은 파랑 | `#E8F0FD` | `#2168D6` | `#17509F` | 흰 텍스트 5.2:1 |
| **violet** 보라 | `#F0EBFC` | `#6B4BD6` | `#4E33A8` | 흰 텍스트 5.8:1 |
| **rose** 로즈 | `#FCECF3` | `#C23A72` | `#8E2853` | 흰 텍스트 5.1:1 |

키 컬러(indigo `#4B5BE8`)까지 넣으면 네 계열이다. 넷 다 흰 텍스트 대비 4.5:1을
넘으므로 채움 칩·버튼 어디에 써도 AA 를 지킨다.

**어디에 쓰나.** 참고 이미지의 두 패턴을 그대로 옮겼다 (`src/lib/brand.ts`).

| 패턴 | 참고 이미지 | 우리 화면 |
|---|---|---|
| 범주 타일 — 채도 높은 색으로 "어느 갈래인가" | 보관 종류 4칸 | **과목 색**: 물질과 에너지 인디고 · 화학 반응의 세계 바이올렛 · 통합과학1 애저 · 통합과학2 로즈. 같은 과목의 대단원은 전부 한 색(2026-09-07 교사 결정 — 단원마다 바꾸면 과목 신호가 묻힌다). 대단원 카드 머리띠(50)·번호(700)·중단원 진도 배지에만. 본문은 흰색 |
| 파스텔 카드 + 작은 색 태그 — 내용 묶음 | 이용 방법 4칸 | **홈 "오늘의 구성"** 세 타일: 복습(바이올렛)·신규(애저)·연속(로즈). 바탕은 50, 태그와 숫자만 700 |
| 활성 표시 | — | **과목 알약 칩** 활성 하나 (`SUBJECT_ACCENT`) |

세 자리 밖으로는 뿌리지 않는다 — 브랜드색은 화면의 10% 이내라는 규칙(D1)이
보조 색에도 똑같이 걸린다. 틴트(50)는 면적을 차지해도 채도가 낮아 이 규칙을 넘지
않지만, 채움(500)은 타일·칩 한 개씩으로 제한한다.

**노선색은 아직 배정하지 않았다.** 이 문서는 §4.1·§4.2·§4.3 세 군데서 "노선색"을
참조하지만 정의된 적이 없다. 계열 4종(BIO/EARTH/CHEM/PHYS)에 위 네 색을 그대로
배정하면 맞아떨어지지만, SciMetro 데이터가 아직 없어 지금 못 박으면 쓰이지 않는
매핑이 된다. **노선 계열이 실제로 들어오는 날 이 표에서 고른다.** 그때 관습적
대응(생명=초록)과 어긋나더라도, 한 팔레트 안에서 서로 구분되는 쪽을 택한다.

### 2.3 상태 색 사용 규칙

| 상태 | 색 | 형태 보조 (D4) |
|------|-----|---------------|
| 정답 / 승인 / 완주 | `--success` | ✓ 아이콘, COMPLETE 배지 |
| 오답 / 오개념 | `--danger` | ✕ 아이콘, 좌측 4px 바 |
| 함정·주의 (misconception 섹션) | `--warning` | ⚠ 아이콘 + 연한 배경 |
| 곁가지 연결 — `same` 배지, `related` 링크 | `--info` | ↔ 아이콘 배지 / `관련` 배지 |
| 잠금 (`restricted` 그림, 미해금) | `--lock` | 자물쇠 아이콘 + 블러 플레이스홀더 |

---

## 3. 타이포그래피 · 레이아웃 · 공통 규격

### 3.1 서체

| 용도 | 서체 | 크기/굵기 |
|------|------|----------|
| 기본 | **Pretendard** (시스템 폴백: -apple-system, "Noto Sans KR", sans-serif) | 16px / 400, 행간 1.6 |

> 웹폰트는 `globals.css` 의 `@font-face` 9단(100~900)이 CDN 에서 직접 물린다.
> 브라우저가 실제로 쓰인 굵기만 내려받으므로, 화면에 없는 굵기는 요청되지 않는다.
> 앱이 실제로 쓰는 굵기는 400·500·600·700·800 다섯이다.
| 표제어(term) | Pretendard | 24px / 700 |
| 화면 타이틀 | Pretendard | 20px / 700 |
| 관계 명제 본문 | Pretendard | 17px / 500 (본문보다 반 단계 강조) |
| 한자·영어 표기, 기호 | Pretendard + 화학식은 `symbol_display` 렌더 (아래첨자 `<sub>`) | 15px / 400, `--text-sub` |
| 보조·캡션·출처 | Pretendard | 13px / 400, `--text-faint` |
| 타이핑 입력(노선) | 모노스페이스 계열 (JetBrains Mono 폴백 monospace) | 18px / 500 |

- 최소 본문 16px — 고교생 모바일 야간 사용 전제.
- 화학식·기호는 이미지가 아니라 텍스트로 렌더한다(`symbol_display` 필드). 검색·복사·접근성을 위해서다.

### 3.2 간격·형태

| 항목 | 값 |
|------|-----|
| 기본 간격 단위 | 4px 배수 (8/12/16/24/32) |
| 화면 좌우 패딩 | 20px (모바일) / 32px (태블릿+) |
| 페이지 배경 | **연회색 `#F3F5F8`** — 카드(흰색)와의 대비로 구획 (보더 대신) |
| 카드 radius | **24px** (히어로 카드 28px), 칩·배지·버튼 radius | 999px (알약형) |
| 카드 그림자 | `0 2px 14px rgba(23,58,94,.06)` — **보더 없이 그림자로만 경계** |
| 터치 타깃 | 최소 44×44px |
| 하단 CTA | 풀너비 알약형, 높이 56px, 브랜드색 글로우 그림자, 화면 하단 고정(safe-area 반영) |
| 톤 | 대화체 카피 + 이모지 절제 사용, 컬러 히어로 카드(홈·결과) — 티키타카·핑글 레퍼런스 반영 (2026-09 리스타일) |

### 3.3 반응형 구조 (D6)

| 브레이크포인트 | 레이아웃 |
|----------------|---------|
| ~767px (기본) | 1단. **하단 탭 바 5탭** 고정 |
| 768~1023px | 개념 탭: 좌 트리(280px) + 우 카드 2단. 탭 바 유지 |
| 1024px~ | 좌측 사이드 내비게이션으로 전환(탭 바 대체). 개념 지도는 전체 폭 캔버스 |

### 3.4 전역 내비게이션

**하단 탭 바 (모바일)** — §7.5의 5탭과 1:1.

| 탭 | 아이콘 콘셉트 | 활성 표시 |
|----|-------------|----------|
| 개념 | 카드 묶음 | 아이콘+라벨 `--primary-500`, 상단 2px 인디케이터 |
| 탐험 | 나침반 | 〃 |
| 홈 | 집 | 〃 + **미완료 시 도트 배지** |
| 문제 | 시험지 | 〃 |
| 노선 | 지하철 노선 | 〃 |

- 가운데(3번째)에 **홈**을 둔다 — 매일 여는 앱의 물리적 중심은 데일리 인출이다(플랭 구조 승계, 순서만 §7.5 표와 다르며 데이터 계약과 무관).
- 탭 라벨은 **홈**이다(교사 결정 2026-09-07). 화면 이름은 §4.4대로 계속 "오늘 탭"이지만,
  탭 바에서는 학생이 "앱을 열면 닿는 곳"으로 읽어야 해서 라벨과 아이콘을 집으로 바꿨다.
  라우트(`/`)와 §7.5의 5탭 계약은 그대로다.
- 둘째 탭의 라벨은 **탐험**이다(교사 결정 2026-09-11). 이 칸이 개념 지도 하나가 아니라
  **지도·개념 야구·스터디룸 셋을 품는 자리**가 되면서 바꿨다. "지도"로 두면 바깥
  이름이 안의 세그먼트 하나와 같은 말이라, 야구를 하다 그 탭을 눌렀을 때 어디로 가는
  것인지 읽히지 않는다. 아이콘도 노드 그래프(=지도)에서 **나침반**으로 바꿨다 —
  아이콘이 안의 화면 하나를 그리고 있으면 라벨만 고쳐 봐야 같은 오해가 남는다.
  세 갈래의 공통점은 "한 단원에 매이지 않고 둘러본다"다.
  라우트(`/map`)와 §7.5의 5탭 계약은 그대로다. 탭 안의 세그먼트는 계속 지도·야구·스터디룸다.
- 상단 앱 바: 좌측 화면 타이틀(또는 브레드크럼), 우측 검색 아이콘 + 프로필(스트릭 숫자 병기).

---

## 4. 화면별 설계

### 4.1 개념 탭 — 단원 트리 → 개념 카드

**(a) 단원 트리 화면**

```
┌──────────────────────────────┐
│ 개념                    🔍 👤 │
│ [통합과학1] [통합과학2] [화학]│  ← 과목 세그먼트 칩 (가로 스크롤)
│ ┌──────────────────────────┐ │
│ │ ▾ 물질의 구조와 성질      │ │  ← 대단원 아코디언
│ │   ▾ 분자 간 상호작용      │ │
│ │     · 증기 압력    ●●●○  │ │  ← 개념 행: 표제어 + 숙련도 도트
│ │     · 분자 간 힘   ●●○○  │ │
│ │   ▸ 화학 결합       12장  │ │  ← 접힌 중단원: 카드 수
│ └──────────────────────────┘ │
└──────────────────────────────┘
```

- 트리는 **과목 > 대단원 > 중단원 > 개념** 3~4깊이. 한판노트의 분류 트리를 아코디언으로 옮긴 것.
- 개념 행 우측 **숙련도 도트 4개**: FSRS `study_states` 기반 (미학습○ / 학습중● 개수). 비로그인 시 도트 미표시.
- 단원 헤더에 진행 바(학습 카드 수 / 전체) — 말해보카식 진행감.

**(b) 개념 카드 화면** — 앱의 중심 화면

```
┌──────────────────────────────┐
│ ← 화학 > 물질의 구조 > 분자 간 상호작용   │ ← 브레드크럼(탭 시 트리 해당 위치)
│                              │
│ 증기 압력                    │ ← term 24/700
│ 蒸氣壓力 · vapor pressure    │ ← notation, 탭 시 훈음 풀이 시트
│ [통합과학1에서 배움 ↔]        │ ← same 링크 배지 (--info)
│                              │
│ ▍정의                        │
│  일정 온도에서 액체와 그 증기가…│
│                              │
│ ▍관계 명제                    │
│ ┌──────────────────────────┐ │
│ │ 온도 일정 조건에서          │ │ ← condition을 칩으로 선표시
│ │ 증기 압력 ↑ → 분자 간 상호작용 ↓ │
│ └──────────────────────────┘ │
│                              │
│ ▍⚠ 흔한 함정                  │ ← --warning 배경 카드
│ │ "끓는점이 높으면 증기 압력도…" │
│ │ 왜 틀렸나: …                │
│                              │
│ ▍그림                        │
│ ┌──────────────────────────┐ │
│ │ 🔒 로그인하면 교과서 그림을  │ │ ← restricted 잠금 상태 (§5.5)
│ │    볼 수 있어요             │ │
│ └──────────────────────────┘ │
│                              │
│ ▍연결된 개념                  │
│ [← 선수: 동적 평형] [다음: 라울 법칙 →] │
│ [↔ 통합과학1: 상태 변화] [관련: 분자 간 힘] │
│ [🚇 이 개념이 나오는 노선: 알케인 본선]   │
│                              │
│ ┌────────────────────────┐   │
│ │  이 개념 인출 연습하기    │   │ ← 화면 유일 CTA (D2)
│ └────────────────────────┘   │
└──────────────────────────────┘
```

- 섹션 순서 고정: 정의 → 표기 → 관계 명제 → 함정 → 그림 → 링크. **관계 명제가 시각적 중심**(카드형, 17px/500) — 시험에 나오는 지식이기 때문(설계서 §1.1).
- 링크 4종은 칩 스타일을 달리한다: `prereq`/`next` = 방향 화살표 포함 아웃라인 칩, `same` = `--info` 채움 칩, `related` = 회색 아웃라인 칩, 노선 = 노선색 도트 칩.
- CTA "인출 연습하기"는 이 카드 단독의 3단계 인출 모드(§5.2)로 진입.

### 4.2 개념 지도 — 개념 그래프 (탐험 탭의 첫 갈래)

- 캔버스형 화면(핀치 줌·드래그 팬). 노드 = 개념 카드(+노선 코스), 엣지 = 링크 4종.
- **시각 인코딩**
  - 노드 크기: 링크 수(허브 강조)
  - 노드 채움: 학습 상태 — 미학습 `--bg-subtle`, 학습중 `--primary-100`, 안정 `--primary-500`
  - `same` 심화 체인: `--info` 점선으로 학년 간 세로 연결 강조
  - 노선 코스 노드: 사각형 + 노선색 테두리 (개념 원형과 구분)
- 하단에 **미니 범례 시트**(접이식). 노드 탭 → 하단 미리보기 시트(표제어+정의 1줄+["카드 열기"]) → 풀 카드로 이동. 그래프에서 바로 전체 화면 전환하지 않는다(길을 잃지 않게).
- 초기 진입은 전체 그래프가 아니라 **현재 학습 중 단원 중심으로 줌인**된 상태.
- 캔버스 위에 **과목 필터 칩 한 줄**(가로 스크롤, `전체`+과목별 개념 수). 2026-09-11 추가 —
  세그먼트 아래에 각 화면이 필요로 하는 선택을 두기로 한 결정을 지도에 적용한 것이다.
  - 고르면 그 과목 노드만 남기고, **양쪽 끝이 다 남은 선만** 그린다. 한쪽 끝이 없는 선은
    허공으로 뻗어 없는 연결이 있는 것처럼 보인다. 과목 간 연결은 `전체`에서만 보인다.
  - 고르는 즉시 그 과목에 **맞춰 다시 잡는다**. 걸러 놓기만 하면 보던 자리에 그 과목
    노드가 없을 때 빈 캔버스가 뜬다.
  - 고른 과목은 기억한다(`ui-state.mapSubject`). 개념 탭의 `conceptsSubject` 와 **따로**
    둔다 — 지도는 "전체"라는 상태가 있고 개념 탭에는 없어서, 한 값을 나눠 쓰면 지도에서
    전체를 본 순간 개념 탭이 첫 과목으로 되돌아간다.
- 이름표는 겹치면 버린다(연결 많은 것부터). 글자 크기가 배율에 비례해 커져 화면에서는 늘
  11px 이므로, **멀리서도 허브 이름 몇 개는 남긴다.** 과목 하나를 통째로 맞춘 배율에서
  이름표를 전부 끄면 이름 없는 동그라미 무더기가 된다(2026-09-11 수정).

### 4.6 내 정보 탭 — 노선 탭 자리에 임시 (2026-09-06 교사 결정)

SciMetro(노선 계열)는 한참 뒤라, 다섯째 탭에 **내 정보**를 둔다. 노선이 들어오는 날
탭을 돌려주고 내 정보는 앱 바의 프로필 아이콘(§3.3)으로 옮긴다. `/lines` 는 남아 있다.

- 흐름: 서버 미연결 안내 → 로그인(§4.7) → **개인정보 동의**(필수 4요소,
  `docs/privacy_notice.md`) → 프로필. 로그인은 선택이다 — 없어도 카드·문항은 그대로다 (D5).
- 프로필: 학번 별칭(실명 없음, R13) · 학년 · 학기 · 수강 과목 · 초대 코드.
- 저장하면 **스케줄러의 출제 범위**가 고른 과목·학기로 잡힌다. 학기는 백로그가 단원에
  학기를 적어 둔 경우에만 좁힌다 — 미지정 단원은 양 학기에 나온다.
- 기록은 로컬이 먼저 쓰고 서버가 뒤따른다(`src/lib/sync.ts`). 로그인 직후 한 번만 서버가 이긴다.

### 4.3 문제 탭 — 기출 문항 (로그인 필요)

- 목록: 회차 필터 칩(연도·월) + 단원 필터. 행 = 문항 번호·단원·연결 개념 칩·풀이 여부 체크.
- 문항 화면: 원문 이미지(서명 URL, 우클릭/길게누르기 저장 차단, 하단 워터마크·출처·경고문구 상시 노출 — §7.5 저작권 요건).
- 흐름: 이미지 → [정답 확인] → 정답 + **자체 해설**(해설 속 개념어 인라인 링크, §5.4) → [연결 개념 카드 보기].
- 비로그인 진입 시: 목록 메타는 보이되 이미지 자리 잠금 카드 + "초대 코드로 가입" 안내(D5).

### 4.4 오늘 탭 — 데일리 인출 (홈 역할)

**(a) 시작 전** — 플랭 구조.

```
🔥 12일 연속    복습 7 + 신규 3
"오늘의 10문항이 준비됐어요"
[ 오늘의 학습 시작하기 ]   ← 유일 CTA
──────
출석 잔디 (최근 12주, 5단계 농도)
```

**(b) 풀이 중** — 문항 유형별 화면 (§5.3), 상단 진행 바(3/10)와 닫기만. 탭 바 숨김(집중 모드).

**(c) 결과** — 틈틈잇 정답 확인 패턴.

- 문항별 카드 스택: `Q1 [정답✓/오답✕] 문항 요지 / 내 답 / 해설 1~2줄`.
- 오답 카드에 [개념 카드 보기] 버튼 — "글 보기" 패턴의 이식.
- 하단: 스트릭 갱신 연출(+1) → [완료]. FSRS 반영은 무언(silent)으로.

### 4.5 노선 탭 — SciMetro 계열

- 노선도 스타일 목록: 노선색 라인 + 역(항목) 수 + 완주 배지. 미해금 코스는 자물쇠(말해보카 패턴).
- 타이핑 플레이 화면과 랭킹은 `SciMetro_에이전트_설계서.md`의 UX를 따르되, 본 문서의 토큰·타이포 규격을 공유한다.
- 완주 화면에 "이 노선의 개념 카드 N장 복습됨" 문구 — 계열 통합(§7.5 연결 지점)을 학생에게 보여주는 유일한 지점.

### 4.6 검색 (전역)

- 앱 바 검색 아이콘 → 풀스크린 검색. `term`·`aliases`·영어·한자 대상.
- 결과 행: 표제어 + 브레드크럼 1줄 + `same` 배지. 구표기(예: "부탄") 검색 시 "→ 뷰테인" 리다이렉트 표시(R6).

---

## 5. 핵심 컴포넌트 · 인터랙션 상세

### 5.1 개념 카드 컴포넌트 (목록용 요약 카드)

- 구성: 표제어 / notation 1줄 / 숙련도 도트 / same 배지.
- 어디서 쓰나: 트리 행, 지도 미리보기 시트, 검색 결과, 퀴즈 결과의 "개념 카드 보기".

### 5.2 3단계 인출 모드 ★핵심 인터랙션★ (한판노트 이식)

카드의 `definition`·`relations`를 가린 상태에서 시작한다.

| 단계 | 화면 | 조작 |
|------|------|------|
| ① 인출 | 표제어만 노출, 나머지는 `--bg-subtle` 블록으로 마스킹 | "떠올려 보세요" + [힌트] [정답 보기] |
| ② 힌트 | 마스킹 위에 **첫 글자(두음)만** 노출 (예: "ㅇㅈ 온도에서…") | [정답 보기] |
| ③ 정답 | 전체 노출 + 자기 평가 버튼 | [잘 떠올림 / 애매함 / 못 떠올림] → FSRS Easy/Hard/Again 매핑 |

- 마스킹 해제는 섹션 단위 탭으로도 가능(정의만 먼저 확인 등).
- 이 모드는 카드 화면 CTA와 오늘 탭 신규 카드 학습 양쪽에서 재사용한다.

### 5.3 데일리 문항 3유형 화면

> **세션 하나는 한 유형이다.** 오늘 탭 CTA → `/today` 유형 선택(OX · 단답 · 선택형,
> 각 칸에 오늘 문항 수) → `/today/run?kind=…`. 유형이 섞인 세트는 만들지 않는다 —
> 화면도 채점도 한 가지만 하면 되고, 학생은 "이번엔 뭘 하는 문제지"를 매번 읽지
> 않아도 된다. 파생기도 유형별로 따로 있다 (`src/data/quiz.ts`).

| 유형 | 레이아웃 | 피드백 |
|------|---------|--------|
| OX | 명제 카드 + 하단 [O] [X] 대형 버튼 2개 | 즉시: 버튼에 ✓/✕ + 해설 1줄 슬라이드업 |
| 단답 | 발문(정의/한자/영어 중 1방향) + 텍스트 필드(평문 입력, R8 정규화 안내 placeholder) | 제출 후 정답·별칭 표시 |
| 선택형 | 발문 + 선택지 4 (형제 개념) 세로 버튼 | 오답 선택 시 "이 개념과 헷갈렸어요 → [해당 카드]" |

공통: 문항당 1화면, 스와이프 아님(오조작 방지), 응답 시간은 무언 측정(FSRS 등급 매핑용).

### 5.4 인라인 개념어 링크 (하이링구얼 패턴)

- 카드 본문·해설·오답 피드백 속 등록 개념어를 `--primary-700` 밑줄점선으로 렌더.
- 탭 → 하단 미리보기 시트(요약 카드) → [카드 열기]. 즉시 화면 전환하지 않는다(읽던 맥락 보존).
- 동음이의(R9) 안전장치: 링크는 파이프라인이 `concept_key`로 확정한 것만 렌더하고, 프론트에서 문자열 매칭으로 만들지 않는다.

### 5.5 잠금 상태 (restricted 자산, D5)

- 그림/문항 이미지 자리: 블러 플레이스홀더 + 🔒 + 1줄 안내 + [초대 코드로 가입] 텍스트 버튼.
- 카드 본문·정의·명제는 항상 열람 가능 — 잠금은 **자산 단위**다(설계서 §0.4).
- 로그인 후에는 이미지 하단에 출처·저작권 경고문구를 13px `--text-faint`로 상시 표기.

### 5.6 진행·보상 장치

| 장치 | 위치 | 규칙 |
|------|------|------|
| 스트릭(연속 일수) | 앱 바 프로필 옆 + 오늘 탭 | 데일리 세트 완료 기준. 끊겨도 질책 문구 금지 |
| 출석 잔디 | 오늘 탭 하단 | 12주 히트맵, `--primary` 5단계 농도 |
| 단원 진행 바 | 트리 헤더 | 학습 시작 카드 비율 |
| COMPLETE 배지 | 노선 코스·단원 | `--success` + ✓ |

### 5.7 상태 화면

| 상태 | 처리 |
|------|------|
| 로딩 | 스켈레톤 (카드 모양 유지). 스피너 단독 사용 금지 |
| 빈 화면 | 1줄 안내 + 다음 행동 버튼 (예: 오늘 완료 시 "내일 7문항이 도착해요 → [지도 둘러보기]") |
| 오류 | 인라인 재시도 버튼. 전면 오류 페이지는 네트워크 단절 시만 |
| 오프라인(PWA) | 마지막 열람 카드 캐시 열람 허용, 응답 기록은 큐잉 후 동기화 |

---

## 6. 접근성 · 품질 기준

- 텍스트 대비 AA(4.5:1) 이상, 상태는 색+형태 이중 인코딩(D4).
- 모든 인터랙티브 요소 44px, 포커스 링 `--primary-500` 2px (키보드/스위치 접근).
- 이미지 대체텍스트: 교과서 그림은 `caption` 필드를 alt로 사용.
- 애니메이션은 200ms 이내, `prefers-reduced-motion` 존중. 정답 연출도 예외 없음.
- 다크 모드는 v1 범위 외 — 단, 색을 전부 토큰으로 썼으므로 추후 `[data-theme]` 한 벌 추가로 대응한다.

## 7. 구현 메모 (Next.js)

- 토큰은 `app/globals.css`의 CSS 변수로 정의하고 Tailwind `theme.extend.colors`에서 `var()` 참조 — 키 컬러 확정 시 `--ramp-*` 6줄만 교체.
- 컴포넌트 우선순위(빌드 순서 제안): ① 개념 카드 화면 → ② 트리 → ③ 3단계 인출 모드 → ④ 데일리 3유형 → ⑤ 결과 화면 → ⑥ 지도 → ⑦ 문제 → ⑧ 노선.
- 지도 그래프는 초기엔 정적 레이아웃(파이프라인이 좌표 산출) + 클라이언트는 렌더·줌만 담당을 권장 — 모바일 성능.

## 8. 미결정 사항

| 항목 | 상태 | 결정 시점 |
|------|------|----------|
| 키 컬러 (Sky vs Green vs 제3안) | **미정** — 잠정 Sky | 브랜딩 확정 시. §2.2 규칙으로 램프 재생성 |
| 서체 최종 (Pretendard 확정 여부) | 잠정 | 구현 착수 시 |
| 다크 모드 | v1 제외 | 사용 데이터 확인 후 |
| 개념 지도 그래프 라이브러리 | 미정 | §7 성능 검증 후 |
| 노선 탭 플레이 화면 상세 | SciMetro 문서 위임 | SciMetro 구현 시 |
