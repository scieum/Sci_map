# SciMetro 노선 계열 콘텐츠 파이프라인 에이전트 설계서

> 과학 전 과목(생명과학·지구과학·화학·물리학)을 **하나의 노선망**으로 묶은 타이핑 학습 게임 **SciMetro**의
> 코스 콘텐츠를 자동 생성·검증·배포하는 에이전트 시스템 설계서
>
> 버전: **v0.3** | 작성일: 2026-09-05 | 이전 버전: v0.2 (2026-08-15), v0.1 (ChemTyping)
>
> 이 문서는 **Sci_Map 플랫폼의 다섯 계열 중 "노선 계열"**을 다룬다. 플랫폼 전체 구조·공용 자산·접근 통제 경계선은 `Sci_Map_에이전트_설계서.md` §0에 정의되어 있으며, 이 문서는 그것을 따른다.
> 이 문서는 Claude Code에서 구현할 때 참조하는 **계획서**다. CLAUDE.md·AGENT.md·스킬의 상세 본문은 구현 단계에서 작성한다.

## v0.1 → v0.2 변경 요약

| 항목 | v0.1 (ChemTyping) | v0.2 (SciMetro) |
|------|-------------------|-----------------|
| 대상 과목 | 화학 단독 | 생명과학·지구과학·화학·물리학 4과목 |
| 콘텐츠 단위 | 독립적인 코스 목록 | **노선망(network)** — 코스 + 과목 간 환승역 |
| 파이프라인 | 코스 단위 S0~S9 | 코스 파이프라인 S0~S9 + **노선망 파이프라인 N1~N3** |
| 검증 서브에이전트 | `chem-validator` (화학 전용) | `fact-validator` (**도메인 플러그인** 방식) |
| 검증 도구 | OPSIN/RDKit/PubChem | 도메인별 4세트 (§5 매트릭스) |
| 신규 제약 | — | **데이터 라이선스**(KEGG 사용 불가), 과목 간 용어 통일, 이수 과목별 접근 |

## v0.2 → v0.3 변경 요약

| 항목 | v0.2 | v0.3 |
|------|------|------|
| 위상 | 독립 게임 | **Sci_Map 플랫폼의 노선 계열** |
| 개념 사전 | 자체 관리 `concept_registry.yaml` | **Sci_Map과 공용** — 복사본 금지 |
| 환승역 개념 | SciMetro 고유 장치 | 개념 계열의 **`same` 링크와 동일한 장치**임이 확인됨. 같은 `concept_key` 위에서 동작 |
| 공통 스킬 | 자체 보유 | `typeability-check`·`term-lexicon`·`curriculum-mapper`·`<track>-facts` 4종을 **공용 자산으로 제공** |
| 학습 기록 | 게임 자체 기록 | 코스 완주가 **개념별 `study_states`(FSRS)에 반영** |
| 리포 루트 | `/scimetro-agent` | **`/scimap`** (개념 계열과 동일 리포) |
| 공개 범위 | 무료·무광고 공개 | **공개 유지 확정** — 코스는 자체 제작물이라 접근제한 대상 아님 |

---

## 1. 작업 컨텍스트

### 1.1 배경 — 왜 노선망인가

[메트로 타이핑](https://metrotyping.kr/)은 지하철 노선을 따라 역 이름을 순서대로 타이핑하며 완주하는 게임이다. 핵심 구조는 다음과 같다.

| 요소 | 메트로 타이핑 |
|------|--------------|
| 진행 단위 | 역(station) 1개 = 타이핑 1단위 |
| 진행 규칙 | 정확히 입력해야 다음 역으로 이동, 오타 시 정지 |
| 묶음 단위 | 노선(line) = 역의 순서 있는 배열 |
| 상위 구조 | **노선망** = 노선 + 환승역 |
| 평가 지표 | 완주 시간, 평균 타수, 정확도 |
| 모드 | 싱글 / 실시간 멀티(최대 4인) / 노선 퀴즈 / 랭킹 |

이 형식이 과학 학습에 성립하려면 대상 지식이 세 조건을 만족해야 한다.

1. **순서가 본질일 것** — 배열에 정답 순서가 존재
2. **항목이 짧은 고유명사일 것** — 역당 2~8자, 1~3초 타이핑
3. **그 순서 자체가 평가 대상일 것** — 암기 가치

이 기준으로 보면 **화학의 주기율표는 노선이 아니라 격자(grid)**다. "1족 라인"은 격자를 억지로 자른 것이다. 반면 생명과학의 대사 경로, 지구과학의 지질시대는 은유가 필요 없는 **진짜 노선**이며, TCA 회로·혈액 순환·암석 순환은 **문자 그대로 순환선**이다.

더 중요한 점은, 과학 4과목이 실제로 **서로 연결되어 있다**는 것이다. 탄소·물·산소·철은 여러 과목에 동시에 등장한다. 이를 **환승역**으로 구현하면 게임 구조가 곧 "과목 간 개념 연결"이라는 교육 목표와 일치한다. 이것이 SciMetro가 단순 암기 게임을 넘어서는 지점이다.

### 1.2 목적

- **1차 목적**: 고등학생이 과학 4과목의 순서·계열 지식을 **반복 플레이로 암기**하고, 환승역을 통해 **과목 간 연결**을 체득한다.
- **2차 목적**: 코스와 환승 관계를 사람이 일일이 설계하지 않고, **에이전트가 생성→검증→검토 요청까지 자동 수행**한다.
- **이 설계서의 대상**: 위 2차 목적을 담당하는 **콘텐츠 파이프라인 에이전트 시스템**. 게임 엔진은 초기 1회 스캐폴딩만 다루고(§7.7), 반복 운영 대상은 콘텐츠다.

노선 계열이 Sci_Map 안에서 맡는 몫은 나머지 계열과 겹치지 않는다. 개념 카드가 "이해"를 맡고 데일리 테스트가 "인출"을 맡는다면, 노선은 **순서·계열 지식의 운동 기억**을 맡는다. 지질시대의 순서나 해당과정의 중간체 배열은 이해로 도달되지 않고 인출 문항 한두 개로도 굳지 않는다. 손이 먼저 다음 역을 치는 상태까지 가야 비로소 쓸 수 있는 지식이 된다. 세 방식이 모두 같은 `concept_key`를 가리키므로 학생은 한 개념을 이해·인출·타이핑의 세 각도에서 만나게 되고, 어느 각도에서 약한지가 기록으로 남는다. 반대 방향도 성립한다. 개념 계열이 없으면 노선 게임은 "왜 이 순서를 외워야 하는지"를 설명할 자리가 없다. 역 이름을 빠르게 치는 능력만 남고 그 역이 무엇인지는 게임 밖에 방치된다. 두 계열은 서로의 결손을 메우는 관계이지 단순 병렬이 아니다.

### 1.3 범위

**포함**

- 코스 콘텐츠의 설계·생성·검증·검토패키징·배포
- **노선망 관리** — 환승역 탐지·유효성 판단·노선망 맵 생성
- 과학적 정확성 검증 (결정론적 + LLM + 사람 3중, 도메인별 도구)
- 교육과정 성취기준 매핑 및 범위 이탈 검사
- 플레이 로그 기반 난이도 재조정 루프
- 게임 엔진 초기 스캐폴딩 스펙(구조 수준)

**제외**

- 게임 UI/UX 비주얼 디자인, 노선도 그래픽 제작
- 백엔드 인프라 운영, 회원/결제
- 콘텐츠의 최종 교육적 승인 (→ 사람 교사의 책임)
- 플랫폼 공통 요소(계정·권한·접근 통제·개념 키 사전 운영) → `Sci_Map_에이전트_설계서.md` §0.3
- 개념 카드·기출 문항·데일리 테스트 파이프라인 → 같은 문서 §4~5

### 1.4 입출력 정의

**입력**

| 입력 | 형식 | 출처 |
|------|------|------|
| 코스 요청 | 자연어 1줄 또는 YAML | 사람 / 백로그 파일 |
| 코스 백로그 | `/docs/course_backlog.yaml` | 사람이 미리 작성 |
| 교육과정 문서 | md (references) | 국가교육과정정보센터 원문 |
| 표기 규정 | md (references) | 과목별 학회·교과서 표기 기준 |
| 도메인 사실 DB | JSON / API | §5 검증기 매트릭스 참조 |
| 플레이 로그 | JSON/CSV | 게임 백엔드 |

**출력**

| 출력 | 경로 | 소비자 |
|------|------|--------|
| 코스 데이터 | `/output/courses/<course-id>.json` | 게임 클라이언트/DB |
| **노선망 맵** | `/output/network/network.json` | 게임 노선도 화면 |
| **환승 후보 목록** | `/output/network/transfer_candidates.json` | 파이프라인 |
| 검증 리포트 | `/output/validation/<course-id>.report.json` | 파이프라인 |
| 교사 검토 패키지 | `/output/review/<course-id>.review.md` | 사람 검토자 |
| 배포 매니페스트 | `/output/deploy/manifest.json` | 배포 스크립트 |
| 개선 제안 | `/output/insights/<course-id>.tuning.md` | 사람 + 다음 사이클 |

### 1.5 제약조건

| # | 제약 | 영향 |
|---|------|------|
| C1 | **교육용이므로 과학 오류 0건이 하드 요구사항** | 결정론적 검증 없이 배포 불가. 사람 게이트 필수 |
| C2 | **한글 표기가 과목마다 두 갈래** — 화학(나트륨/소듐, 부탄/뷰테인), 생명(젖산/락트산), 지구(주향/…) | 정답은 교육과정 현행 표기로 고정, 구표기는 **별칭 처리**(오답 아님, 정답도 아님, 안내만) |
| C3 | **타이핑 불가 문자** — 하첨자(H₂O), 위첨자(Na⁺), 그리스문자(α-케토글루타르산), 화살표(→) | **표시용 문자열과 입력용 문자열을 분리 필드로 관리** |
| C4 | **교육과정 개정 상태** | 2022 개정에서 과목 체계 재편. 일반선택(물리학·화학·생명과학·지구과학) + 진로선택(역학과 에너지, 전자기와 양자, 물질과 에너지, 화학 반응의 세계, 세포와 물질대사, 생물의 유전, 지구시스템과학, 행성우주과학). **양쪽 성취기준 코드 병행 태깅** |
| C5 | **개념이 여러 과목에 걸침** | 주기율표는 `통합과학1`, 대사 경로는 `세포와 물질대사`(진로선택) 소관. 범위 검증 시 **연계 과목까지 허용 목록에 포함** |
| C6 | **⚠️ 데이터 라이선스** | **KEGG는 학술 목적이라도 "서비스 제공"에는 라이선스가 필요**하므로 공개 게임에 사용 불가. 대체 소스 사용 필수 (§5.2) |
| C7 | **과목별 이수 편차** | 고1은 통합과학만 이수. 코스마다 **선수 과목 태그**를 달고 게임에서 난이도 필터 제공 |
| C8 | **환승역의 개념 동일성 문제** | 이름이 같아도 개념이 다를 수 있음("일", "주기", "핵"). 환승 유효성은 **LLM 판단 + 사람 확인** 필요 |
| C9 | 무료·무광고 지향, 초기 1인 개발 | 오픈소스·퍼블릭도메인 데이터만 사용. 노선 코스는 자체 제작물·공공데이터 기반이므로 Sci_Map의 **공개 계층**에 속한다(같은 문서 §0.4). 접근제한 대상은 교과서 그림·기출 이미지뿐이며, 따라서 **서버 검증 랭킹과 실시간 멀티가 온전히 성립한다.** |
| C10 | 서버 검증 랭킹 요구 | 코스 데이터는 **불변 버전** 관리. 배포 후 수정 시 새 버전 발급 |
| C11 | **개념 키 사전이 공용 자산이다** | 신규 키 등록은 어느 계열에서 제안하든 **사람 승인**을 거친다. N2·N3에서 SciMetro가 단독으로 사전을 갱신하지 않는다. 개념 계열의 `same` 링크 판정과 환승 판정이 **같은 사전을 놓고 경합**하므로, 판정 근거를 사전에 함께 기록한다 |

### 1.6 용어 정의

| 용어 | 정의 |
|------|------|
| **코스(Course)** | 메트로 타이핑의 "노선". 순서 있는 스테이션 배열 + 메타데이터. 배포 단위 |
| **스테이션(Station)** | 타이핑 1단위. 역 하나에 대응 |
| **노선망(Network)** | 전체 코스 + 환승 관계의 그래프. 게임의 노선도 화면에 대응 |
| **환승역(Transfer)** | 둘 이상의 코스에 등장하며 **개념적으로 동일**하다고 확인된 스테이션 |
| **대환승역(Hub)** | 3개 이상 코스가 만나는 환승역 (예: 철, 탄소) |
| **계열(Track)** | 코스의 과목 대분류. BIO / EARTH / CHEM / PHYS |
| **순환선(Loop)** | 마지막 역이 첫 역으로 돌아오는 코스 (TCA 회로, 혈액 순환, 암석 순환) |
| **분기선(Branch)** | 특정 역에서 조건에 따라 갈라지는 코스 (별의 진화) |
| **정답 세트** | 하나의 스테이션에서 정답으로 인정되는 입력 문자열 집합(정규화 후) |
| **왕복 검증** | 이름 → 구조/ID → 정규 표현으로 되돌려 원본과 일치하는지 확인하는 결정론적 검증 |
| **검토 게이트** | 사람 승인 없이는 진행 불가한 지점 |

---

## 2. 노선 카탈로그 (도메인 매핑)

에이전트가 무엇을 생성하는지 명확해야 워크플로우가 정의되므로 먼저 고정한다.
**★ = 우선 착수 권장 (조건 3가지 충족도가 높음)**

### 2.1 BIO — 생명과학

| 코스 | 유형 | 역 구성 | 역 수 |
|------|------|---------|-------|
| ★ 해당과정 본선 | 직선 | 포도당 → 포도당-6-인산 → 과당-6-인산 → 과당-1,6-이인산 → 글리세르알데하이드-3-인산 → 1,3-이인산글리세르산 → 3-인산글리세르산 → 2-인산글리세르산 → 포스포엔올피루브산 → 피루브산 | 10 |
| ★ TCA **순환선** | 순환 | 시트르산 → 아이소시트르산 → 알파케토글루타르산 → 석신일코에이 → 석신산 → 푸마르산 → 말산 → 옥살아세트산 → (시트르산) | 8 |
| ★ 혈액 **순환선** | 순환 | 대정맥 → 우심방 → 우심실 → 폐동맥 → 폐 → 폐정맥 → 좌심방 → 좌심실 → 대동맥 → (대정맥) | 9 |
| 캘빈 **순환선** | 순환 | 리불로스이인산 → 3-인산글리세르산 → 1,3-이인산글리세르산 → 글리세르알데하이드-3-인산 → (리불로스이인산) | 4~6 |
| ★ 분류 계급선 | 직선 | 종 → 속 → 과 → 목 → 강 → 문 → 계 → 역 | 8 |
| 소화 경로선 | 직선 | 입 → 인두 → 식도 → 위 → 십이지장 → 소장 → 대장 → 직장 → 항문 | 9 |
| 세포 주기선 | 순환 | G1기 → S기 → G2기 → 전기 → 전중기 → 중기 → 후기 → 말기 → 세포질분열 → (G1기) | 9 |
| 단백질 합성선 | 직선 | 디엔에이 → 전사 → 메신저아르엔에이 → 핵공 → 리보솜 → 티아르엔에이 → 펩타이드결합 → 폴리펩타이드 | 8 |
| 뉴런 전달선 | 직선 | 수상돌기 → 신경세포체 → 축삭둔덕 → 축삭 → 랑비에결절 → 축삭말단 → 시냅스 | 7 |
| 아미노산 라인 | 격자 | 20종 (글리신, 알라닌, 발린 …) | 20 |
| 전자전달계선 | 직선 | 복합체1 → 유비퀴논 → 복합체3 → 사이토크롬c → 복합체4 → 에이티피합성효소 | 6 |

**BIO는 순환선이 3개**(TCA·혈액·캘빈)로 가장 많다. 메트로 게임에서 순환선은 "무한 완주 모드"가 가능해 게임적 가치가 높다.

### 2.2 EARTH — 지구과학

| 코스 | 유형 | 역 구성 | 역 수 |
|------|------|---------|-------|
| ★★ 지질시대 본선 | 직선 | 캄브리아기 → 오르도비스기 → 실루리아기 → 데본기 → 석탄기 → 페름기 → 트라이아스기 → 쥐라기 → 백악기 → 팔레오기 → 네오기 → 제4기 | 12 |
| ★ 별의 진화 **분기선** | 분기 | 성운 → 원시별 → 주계열성 → **[저질량]** 적색거성 → 행성상성운 → 백색왜성 / **[고질량]** 초거성 → 초신성 → 중성자별 or 블랙홀 | 6+3 |
| ★ 암석 **순환선** | 순환 | 마그마 → 화성암 → 퇴적물 → 퇴적암 → 변성암 → (마그마) | 5~7 |
| 지구 내부선 | 직선 | 지각 → 모호면 → 상부맨틀 → 하부맨틀 → 구텐베르크면 → 외핵 → 레만면 → 내핵 | 8 |
| 대기권선 | 직선 | 대류권 → 대류권계면 → 성층권 → 성층권계면 → 중간권 → 중간권계면 → 열권 | 7 |
| ★ 태양계선 | 직선 | 수성 → 금성 → 지구 → 화성 → 소행성대 → 목성 → 토성 → 천왕성 → 해왕성 | 9 |
| 분광형선 | 직선 | 오형 → 비형 → 에이형 → 에프형 → 지형 → 케이형 → 엠형 | 7 |
| 물의 **순환선** | 순환 | 증발 → 응결 → 강수 → 침투 → 유출 → (증발) | 5 |
| 판 구조선 | 격자 | 태평양판 → 유라시아판 → 북아메리카판 → 남아메리카판 → 아프리카판 → 인도호주판 → 남극판 | 7 |
| 조암광물선 | 직선 | 감람석 → 휘석 → 각섬석 → 흑운모 → 사장석 → 정장석 → 석영 (보웬 반응 계열) | 7 |

**지질시대 본선이 전체 노선 중 가장 강력하다.** 시간축이 곧 노선이고, 역명이 낯설어 암기 난이도가 적당하며, 순서가 그대로 평가 대상이다. 여기서 시작하기를 권장한다.

### 2.3 CHEM — 화학 (v0.1에서 승계)

| 코스 | 유형 | 역 구성 | 역 수 |
|------|------|---------|-------|
| 주기율표 주기선 | 격자 | 1~3주기 원소 순 | 18 |
| 1족 알칼리선 | 격자 | 수소 → 리튬 → 소듐 → 포타슘 → 루비듐 → 세슘 → 프랑슘 | 7 |
| 17족 할로젠선 | 격자 | 플루오린 → 염소 → 브로민 → 아이오딘 → 아스타틴 | 5 |
| 18족 비활성선 | 격자 | 헬륨 → 네온 → 아르곤 → 크립톤 → 제논 → 라돈 | 6 |
| 알케인 본선 | 직선 | 메테인 → 에테인 → 프로페인 → 뷰테인 → 펜테인 → 헥세인 → 헵테인 → 옥테인 → 노네인 → 데케인 | 10 |
| 작용기 산화선 | 직선 | 알케인 → 알코올 → 알데하이드 → 카복실산 → 에스터 | 5 |
| 다원자 이온선 | 격자 | 암모늄 → 수산화 → 질산 → 황산 → 탄산 → 인산 | 6 |

### 2.4 PHYS — 물리학

| 코스 | 유형 | 역 구성 | 역 수 |
|------|------|---------|-------|
| ★ 전자기파선 | 직선 | 감마선 → 엑스선 → 자외선 → 가시광선 → 적외선 → 마이크로파 → 전파 | 7 |
| 가시광선 지선 | 직선 | 보라 → 남색 → 파랑 → 초록 → 노랑 → 주황 → 빨강 | 7 |
| 에스아이 접두어선 | 직선 | 피코 → 나노 → 마이크로 → 밀리 → 센티 → 킬로 → 메가 → 기가 → 테라 | 9 |
| 에스아이 기본단위선 | 격자 | 미터 → 킬로그램 → 초 → 암페어 → 켈빈 → 몰 → 칸델라 | 7 |
| 표준모형 라인 | 격자 | 쿼크 6종 + 렙톤 6종 + 게이지보손 | 17 |

**PHYS는 적합도가 가장 낮다.** 물리의 핵심은 수식 계산이며 타이핑 게임과 맞지 않는다. 위 5개 정도가 상한이므로 **보조 계열로 취급**하고 우선순위를 낮춘다.

### 2.5 환승역 설계 — 노선망의 핵심

먼저 짚어둘 것이 있다. 환승역과 개념 계열의 `same` 링크는 **같은 장치의 두 이름**이다. 둘 다 "표기가 아니라 `concept_key` 일치로 건다"는 하나의 규칙 위에 서 있고, 다른 것은 그 링크를 보여주는 화면뿐이다. 노선 탭에서는 두 코스가 만나는 역으로 보이고 개념 탭에서는 두 카드를 잇는 선으로 보인다. 따라서 C8(환승역의 개념 동일성 문제)과 개념 계열의 R9(동음이의)는 서로 다른 두 문제가 아니라 같은 문제를 두 계열이 각자의 자리에서 만난 것이다. 이 점을 인정하면 대응도 하나여야 한다. 동음이의 금지쌍 목록은 `/docs/homonyms.yaml` 한 벌만 두고 두 계열이 함께 참조한다. 목록을 계열별로 나누면 한쪽에서 걸러낸 쌍이 다른 쪽에서 통과하는 일이 반드시 생기고, 그때 잘못 이어진 링크는 두 탭 모두에서 동시에 틀린다.

과목 간 개념 연결이 곧 환승이다. 초기 후보:

| 환승역 | 연결 코스 | 환승 수 |
|--------|----------|---------|
| **철** | CHEM 전이금속 · BIO 전자전달계(사이토크롬) · EARTH 지구 내부(핵) · EARTH 별의 진화(초신성 경계 원소) | **4 — 대환승역** |
| **탄소** | CHEM 2주기 · CHEM 알케인 · BIO 유기물 · EARTH 탄소 순환 | **4 — 대환승역** |
| **포도당** | BIO 해당과정 · BIO 캘빈 회로 · CHEM 탄수화물 | 3 |
| **산소** | CHEM 2주기 · BIO 전자전달계 · BIO 광합성 · EARTH 대기 진화 | **4 — 대환승역** |
| **물** | CHEM 화합물 · BIO 광합성 · EARTH 물의 순환 | 3 |
| **이산화탄소** | CHEM 화합물 · BIO TCA/광합성 · EARTH 탄소 순환·온실효과 | 3 |
| **에이티피** | BIO 해당과정 · BIO TCA · BIO 전자전달계 · BIO 광합성 | 4 (BIO 내부) |
| **수소** | CHEM 1족 · EARTH 핵융합 · PHYS 원자 모형 | 3 |
| **빛(광자)** | PHYS 전자기파 · BIO 광합성 · EARTH 복사평형 | 3 |
| **캄브리아기** | EARTH 지질시대 · BIO 생물 다양성 | 2 |

**환승역의 게임적 의미**: 한 코스를 완주하면 그 코스의 환승역들이 "해금"되어 인접 코스로 이어진다. 학생은 자연스럽게 "이 개념이 다른 과목에도 나오는구나"를 체험한다. 대환승역(철·탄소·산소)은 노선도의 중심에 배치한다.

**⚠️ 환승 유효성 문제 (C8)**: 이름이 같아도 개념이 다르면 환승이 아니다.
- 유효: 화학의 "산화"와 생명의 "산화" (전자 잃음, 동일 개념)
- **무효**: 물리의 "일(work)"과 일상어 "일", 화학의 "주기"와 물리의 "주기(period)", 지구의 "핵(core)"과 생물의 "핵(nucleus)"
- → N2 단계에서 LLM이 판단하고, 애매하면 사람에게 확인한다.

### 2.6 스테이션 데이터 스키마 (개요)

```jsonc
{
  "id": "bio-glyco-01",
  "track": "BIO",
  "display": "포도당",              // 화면 표시 (하첨자/특수문자 허용)
  "display_formula": "C₆H₁₂O₆",     // 보조 표시 (선택)
  "answers": ["포도당"],            // 정답 세트 (정규화 후 비교)
  "answers_alt": ["글루코스"],      // 별칭 → 오답 아님, 안내 토스트
  "typeable": true,                 // 키보드 입력 가능 여부 (검증 결과)
  "hint": { "초성": "ㅍㄷㄷ", "설명": "..." },
  "meta": { "source_db": "reactome", "source_id": "R-ALL-70171" },
  "curriculum": ["12세물01-03"],
  "prereq_subject": "세포와 물질대사",
  "difficulty": 2,
  "transfer": ["chem-carbohydrate", "bio-calvin"]   // 환승 대상 코스
}
```

---

## 3. 노선망 설계

### 3.1 코스 유형별 게임 규칙

| 유형 | 게임 규칙 | 해당 코스 |
|------|----------|----------|
| **직선(Linear)** | 시작역→종착역 1회 완주 | 지질시대, 해당과정, 전자기파 |
| **순환(Loop)** | 종착역=시작역. **무한 랩 모드** 지원(몇 바퀴 돌았나로 경쟁) | TCA, 혈액 순환, 암석 순환, 물의 순환, 세포 주기 |
| **분기(Branch)** | 분기점에서 조건 선택. 선택에 따라 다른 종착역. **양쪽 다 완주해야 100%** | 별의 진화 |
| **격자(Grid)** | 순서보다 위치가 중요. 노선도가 표 형태 | 주기율표, 아미노산, 표준모형, 판 구조 |

**순환선의 무한 랩 모드**는 메트로 타이핑에 없는 SciMetro 고유 요소이며, TCA 회로처럼 "돌고 도는" 지식의 성격과 정확히 맞는다.

### 3.2 노선망 그래프 모델

```
network.json
├── courses[]            # 코스 노드 (id, track, type, station_count, version)
├── transfers[]          # 환승 엣지 (station_key, course_ids[], validated_by, note)
└── layout               # 노선도 배치 좌표 (수동 조정 가능, 에이전트는 초안만)
```

- **환승 키(station_key)**: 정규화된 개념 식별자. 표시명이 달라도 같은 개념이면 같은 키 (`concept:iron`, `concept:glucose`)
- **개념 사전**: `/docs/concept_registry.yaml`에 키↔표시명 매핑을 사람이 관리. 에이전트는 신규 키 **제안만** 하고 확정은 사람이 한다 (C8 대응)

---

## 4. 워크플로우 정의

### 4.1 전체 흐름 — 두 개의 파이프라인

```
[A] 코스 파이프라인 (코스 1개 단위, 반복 실행)

[사람] 코스 요청 / 백로그
   │
   ▼
S0  요청 파싱 + 계열 라우팅                 [코드]
   │      └─ track(BIO/EARTH/CHEM/PHYS) 판별 → 도메인 검증기 세트 활성화
   ▼
S1  코스 설계 (구성·순서·유형·난이도 곡선)   [LLM]  ← course-designer
   │
   ▼
S2  스테이션 데이터 생성                    [LLM]  ← content-writer
   │
   ▼
S3  결정론적 사실 검증                      [코드]  ← fact-validator + 도메인 스킬
   │      └─ 실패 → 자동 재시도(최대 2회) → 초과 시 에스컬레이션
   ▼
S4  입력 가능성·스키마 검증                 [코드]
   │      └─ 실패 → 자동 수정 후 재검증
   ▼
S5  교육과정 범위 검증                      [LLM]  ← fact-validator
   │      └─ 범위 이탈 → 스테이션 제거 or S1 복귀
   ▼
S6  게임플레이 품질 검증                    [LLM]  ← playtest-reviewer
   │      └─ 미달 → S1/S2 부분 재작업(최대 2회)
   ▼
S7  교사 검토 패키지 생성 → ★사람 게이트★    [코드 + 사람]
   │      └─ 반려 → 사유 해석 후 S1 or S2 복귀
   ▼
S8  배포 (버전 발급·매니페스트 갱신)         [코드]
   │
   ▼
S9  플레이 로그 수집 → 튜닝 제안             [코드 + LLM]  ← playtest-reviewer
   │
   └────→ (다음 사이클 S1 입력으로 환류)


[B] 노선망 파이프라인 (코스 배포 후 또는 주기적으로 실행)

S8 완료 (코스 1개 이상 신규 배포)
   │
   ▼
N1  환승 후보 탐지                          [코드]
   │      └─ 전체 코스 스캔 → 개념 키 충돌·유사 스테이션 추출
   ▼
N2  환승 유효성 판단                        [LLM]  ← network-curator
   │      └─ 동일 개념인가? 애매 → ★사람 확인★
   ▼
N3  노선망 맵 생성 + 개념 사전 갱신 제안      [코드 + 사람]
   │
   └────→ network.json 배포
```

### 4.2 LLM 판단 영역 vs 코드 처리 영역

| 에이전트가 직접 판단 | 스크립트가 처리 |
|---------------------|----------------|
| 코스 구성: 어떤 항목을 몇 개, 어떤 순서로 | 도메인 DB 조회 (Reactome, NCBI, ICS, CODATA …) |
| 코스 유형 결정 (직선/순환/분기/격자) | OPSIN 이름→구조 파싱, RDKit 정규화 |
| 난이도 곡선 설계 | 지질시대 순서·연대 대조 |
| 힌트 문구·설명 자연어 작성 | 분류 계급 상하위 관계 검증 |
| 교육과정 범위 이탈 판단 (경계 사례) | 한글 초성 추출, 문자열 정규화(NFC) |
| **환승 유효성 판단 (개념 동일성)** | **환승 후보 탐지 (문자열/키 충돌 스캔)** |
| 게임플레이 재미·지루함 정성 평가 | 키보드 입력 가능성 기계 판정 |
| 플레이 로그 해석 → 왜 여기서 이탈했는가 | JSON 스키마 검증, 중복 검사, 로그 집계 |
| 검증 실패 리포트 해석 → 수정 방향 결정 | 버전 발급, 매니페스트 갱신, 파일 I/O |

### 4.3 코스 파이프라인 상세

---

#### S0. 요청 파싱 + 계열 라우팅 — [코드]

- **입력**: 자연어 요청 또는 `course_backlog.yaml` 항목
- **처리**: 계열(BIO/EARTH/CHEM/PHYS), 코스 유형, 범위, 난이도, 입력 모드를 구조화. **계열에 따라 활성화할 도메인 검증기 세트를 결정**
- **출력**: `/output/requests/<course-id>.request.json`
- **성공 기준**: 필수 필드(track, type, scope, mode) 모두 채워짐 + 검증기 세트 지정됨
- **검증 방법**: 스키마 검증
- **실패 시**: 계열 판별 실패 → **에스컬레이션** (사람에게 확인)

---

#### S1. 코스 설계 — [LLM · course-designer]

- **입력**: `request.json` + 교육과정 references + 기존 코스 목록 + **개념 사전**(환승 후보 인지용)
- **처리**
  - 스테이션 후보와 **순서** 결정
  - **코스 유형** 확정 (직선/순환/분기/격자) — 순환선이면 종착역=시작역 명시
  - **난이도 곡선**: 앞 20%는 익숙한 항목 → 중반 신규 → 후반 헷갈리는 항목
  - **환승역 후보 지정** — 개념 사전에 있는 키와 겹치는 스테이션 표시
- **출력**: `/output/courses/<course-id>.outline.json`
- **성공 기준**
  - 스테이션 수 8~24 (순환선은 5~12 허용)
  - 인접 스테이션 난이도 차 ≤ 2
  - 기존 코스와 80% 이상 중복되지 않음
  - 환승역 후보 ≥ 1개 (완전 고립 코스 방지)
- **검증 방법**: 규칙 기반
- **실패 시**: **자동 재시도** 최대 2회(실패 사유 첨부) → 초과 시 **에스컬레이션**

---

#### S2. 스테이션 데이터 생성 — [LLM · content-writer]

- **입력**: `outline.json` + 계열별 표기 규정 references + 스키마
- **처리**: 각 스테이션의 `display` / `answers` / `answers_alt` / `hint` / `meta` 채우기. **`meta.source_db`·`source_id`에 검증 대조용 외부 ID를 반드시 기입**
- **출력**: `/output/courses/<course-id>.draft.json`
- **성공 기준**: 필수 필드 완비, 정답 세트 비어있지 않음, 현행 교육과정 표기 사용, 외부 ID 기입률 ≥ 80%
- **검증 방법**: 스키마 검증 + 규칙 기반(표기 사전 대조)
- **실패 시**: **자동 재시도** 최대 2회 (누락 스테이션만 부분 재생성)

---

#### S3. 결정론적 사실 검증 — [코드 · fact-validator 총괄]

**시스템의 핵심 안전장치. LLM 생성물을 LLM으로만 검증하지 않는다.**
계열별 검증 항목은 §5 매트릭스 참조.

- **출력**: `/output/validation/<course-id>.report.json` (스테이션별 pass/fail/warn + 사유)
- **성공 기준**: **fail 0건** (warn은 허용, 리포트에 기록)
- **검증 방법**: 스크립트 (결정론적)
- **실패 시**
  - fail ≤ 3건 → 해당 스테이션만 **자동 재시도**(S2 부분 재생성, 사유 첨부, 최대 2회)
  - fail > 3건 또는 재시도 초과 → **에스컬레이션** (코스 설계 자체 문제 가능성)
  - 외부 API 조회 실패(네트워크/미등재) → **스킵 + 로그**, 단 **동일 코스에서 스킵률 30% 초과 시 에스컬레이션**

---

#### S4. 입력 가능성·스키마 검증 — [코드]

- **처리**
  - `answers`에 입력 불가 문자(하첨자·위첨자·그리스문자·화살표·특수 대시) 검사 → `display`에만 남기고 `answers`는 평문 자동 변환 (`α-케토글루타르산` → `알파케토글루타르산`, `H₂O` → `H2O`)
  - 한/영 자판 혼용, 전각/반각, 유니코드 정규화(NFC)
  - **코스 내 정답 중복 검사** (같은 정답 2개 = 게임 버그)
  - **순환선 무결성 검사** (마지막 역의 다음이 첫 역인지)
  - JSON 스키마 최종 검증
- **성공 기준**: 전 스테이션 `typeable: true`, 정답 중복 0건, 순환/분기 구조 무결, 스키마 통과
- **검증 방법**: 스크립트
- **실패 시**: **자동 수정 후 재검증**(결정론적). 자동 수정 불가 케이스만 **에스컬레이션**

---

#### S5. 교육과정 범위 검증 — [LLM · fact-validator]

- **입력**: 검증 통과 draft + 성취기준 references + 대상 학년/과목
- **처리**: 각 스테이션의 성취기준 코드 태깅 + 범위 이탈 판단 + **선수 과목 태그** 부여(C7)
- **성공 기준**: 전 스테이션 성취기준 코드 ≥ 1개, 범위 이탈 0건, 선수 과목 태그 부여됨
- **검증 방법**: **LLM 자기 검증** + 태깅 필드 존재 검사(규칙 기반)
- **실패 시**
  - 이탈 ≤ 2건 → 스테이션 **제거 후 진행** + 로그 (최소 길이 미만이 되면 S1 복귀)
  - 이탈 > 2건 → **S1 복귀**
  - 판단 유보 → **에스컬레이션** (S7 검토 항목에 병합)

---

#### S6. 게임플레이 품질 검증 — [LLM · playtest-reviewer]

성공 기준이 "교육 효과·재미 균형"이므로 정식 게이트로 둔다.

- **평가 항목** (각 1~5점)
  1. **난이도 곡선** — 초반 진입 장벽이 낮고 후반에 도전이 있는가
  2. **길이 적정성** — 1회 플레이 60~180초인가 (역 수 × 평균 글자수 ÷ 기준 타수)
  3. **인지 부하** — 연속으로 헷갈리는 항목이 3개 이상 붙어있지 않은가
  4. **학습 가치** — 반복 시 실제 암기가 되는 구성인가 (인출 간격, 환승역 재등장)
  5. **재미 요소** — 코스 이름·힌트·완주 문구가 매력적인가
  6. **유형 적합성** — 순환/분기 규칙이 이 내용에 실제로 어울리는가 *(v0.2 신규)*
- **성공 기준**: 전 항목 3점 이상, 평균 3.5점 이상
- **검증 방법**: **LLM 자기 검증** (근거를 반드시 스테이션 인덱스와 함께 기술)
- **실패 시**: 미달 항목 지정 → **S1(구성) 또는 S2(문구) 부분 재작업**, 최대 2회 → 초과 시 **에스컬레이션**

---

#### S7. 교사 검토 패키지 생성 → ★사람 게이트★

- **처리**: 검토자가 5분 내 판단 가능한 md 리포트 생성
  - 코스 요약 (계열, 유형, 범위, 역 수, 예상 플레이 시간, 선수 과목)
  - **전체 스테이션 표** (표시/정답/외부 ID/성취기준/난이도)
  - **검증 결과 요약** (S3~S6 통과 내역, warn 목록, 스킵 내역)
  - **★검토자가 특히 봐야 할 지점** — 자동 검증이 커버 못 한 항목만
    (별칭 처리, 경계 사례 성취기준, 외부 DB 미등재 스테이션, LLM 판단 유보, **신규 환승 후보**)
  - 승인/반려 기록 방법
- **성공 기준**: 사람이 `approved: true` 기록
- **검증 방법**: **사람 검토**
- **실패 시**: 반려 사유를 LLM이 해석해 S1 또는 S2 복귀. 모호하면 되묻는다
- **주의**: **자동 통과 옵션을 두지 않는다** (C1)

---

#### S8. 배포 — [코드]

- **처리**: 버전 발급(`v1`, `v2`…), `/output/courses/<id>@<version>.json` 확정, 매니페스트 갱신, DB 반영용 diff 산출, **N1 트리거 발행**
- **성공 기준**: 매니페스트 스키마 통과, 기존 버전 미덮어씀(불변성)
- **검증 방법**: 스키마 검증 + 파일 해시 비교
- **실패 시**: **에스컬레이션** (배포는 되돌리기 비용이 크므로 자동 재시도 금지)

---

#### S9. 플레이 로그 → 튜닝 제안 — [코드 집계 + LLM 해석]

- **트리거**: 배포 후 누적 플레이 N회(기본 200) 도달, 또는 수동 실행
- **집계(코드)**: 스테이션별 평균 오타율, 이탈 지점 분포, 완주율, 재도전율, 평균 완주 시간, **환승 이용률**
- **해석(LLM)**: "12번 역 오타율 62% — 그리스문자 평문 변환(알파케토글루타르산)이 길어서로 추정. 힌트 보강 또는 별칭 추가 권장"
- **출력**: `/output/insights/<course-id>.tuning.md`
- **성공 기준**: 데이터 근거가 붙은 실행 가능한 제안 3개 이상
- **검증 방법**: 규칙 기반(제안 수) + LLM 자기 검증(근거 유무)
- **실패 시**: 표본 부족 → **스킵 + 로그**, 다음 임계치에서 재시도

### 4.4 노선망 파이프라인 상세

---

#### N1. 환승 후보 탐지 — [코드]

- **입력**: 배포된 전체 코스 + `/docs/concept_registry.yaml`
- **처리**
  - 정답 문자열·별칭·외부 ID의 **완전 일치** → 강한 후보
  - 개념 키 매칭 → 확정 후보
  - 문자열 유사도(편집거리) 임계치 이상 → 약한 후보
- **출력**: `/output/network/transfer_candidates.json` (후보별 강도 등급)
- **성공 기준**: 전체 코스 스캔 완료, 후보에 근거(일치 유형) 기재
- **검증 방법**: 규칙 기반
- **실패 시**: **자동 재시도** 1회 → 초과 시 에스컬레이션

---

#### N2. 환승 유효성 판단 — [LLM · network-curator]

- **입력**: `transfer_candidates.json` + 양쪽 코스의 맥락
- **처리**: 각 후보에 대해 **"두 코스에서 이것이 같은 개념인가"** 판정
  - `valid` — 환승 성립 (예: 화학 산화 ↔ 생명 산화)
  - `invalid` — 동음이의 (예: 지구 핵 ↔ 생물 핵)
  - `uncertain` — 사람 확인 필요
- **출력**: `/output/network/transfer_verdicts.json` (판정 + 근거 문장)
- **성공 기준**: 전 후보에 판정 + 근거 기재, `uncertain` 비율 ≤ 20%
- **검증 방법**: **LLM 자기 검증** (근거 필수) + 규칙 기반(판정 누락 검사)
- **실패 시**: `uncertain` 20% 초과 → **에스컬레이션**(사람 일괄 확인 요청). 개별 `uncertain`은 **보류 + 로그**(다음 사이클 재판정)
- **주의**: **`valid` 판정만으로 자동 반영하지 않는다.** 신규 개념 키 등록은 사람 승인 사항(C8)

---

#### N3. 노선망 맵 생성 + 개념 사전 갱신 제안 — [코드 + 사람]

- **처리**: `valid` 환승으로 그래프 구성, 대환승역(3+ 연결) 식별, 노선도 배치 좌표 초안 산출, **개념 사전에 추가할 신규 키를 제안 목록으로 출력**
- **출력**: `/output/network/network.json` + `/output/network/registry_proposal.yaml`
- **성공 기준**: 그래프 연결성 확보(고립 코스 0개 지향), 대환승역 ≥ 3개
- **검증 방법**: 규칙 기반(그래프 연결성 검사) + **사람 검토**(개념 사전 갱신)
- **실패 시**: 고립 코스 발생 → **스킵 + 로그**(해당 코스는 독립 노선으로 유지, 다음 코스 추가 시 재평가)

### 4.5 코스 길이·난이도 기준 (규칙 상수)

| 항목 | 직선 | 순환 | 분기 | 격자 |
|------|------|------|------|------|
| 스테이션 수 | 8~24 | 5~12 | 6~15 | 5~20 |
| 목표 1회 플레이 시간 | 90초 | 45초/랩 | 100초 | 80초 |
| 인접 난이도 차 | ≤ 2 | ≤ 2 | ≤ 2 | 해당 없음 |
| 초반 쉬움 구간 | 앞 20% | 앞 20% | 앞 20% | 해당 없음 |
| 최대 정답 길이 | 12자 | 12자 | 12자 | 12자 |
| 환승역 최소 | 1개 | 1개 | 1개 | 1개 |

---

## 5. 검증 전략

### 5.1 검증 유형 배치

| 검증 유형 | 적용 단계 | 구체 내용 |
|-----------|----------|----------|
| **스키마 검증** | S0, S2, S4, S8, N1 | request/station/course/manifest/network JSON 스키마 |
| **규칙 기반** | S1, S3, S4, S6, S9, N1, N3 | 개수·길이·난이도 차·중복률·외부 ID 대조·순환 무결성·그래프 연결성 |
| **LLM 자기 검증** | S5, S6, S9, **N2** | 교육과정 범위 판단, 게임플레이 정성 평가, 로그 해석, **환승 개념 동일성** |
| **사람 검토** | S7, **N3** | 배포 전 교사 승인(필수), **개념 사전 갱신 승인** |

**설계 의도**: 과학적 사실은 **절대 LLM 자기 검증에 맡기지 않는다**(S3는 전부 스크립트). LLM 자기 검증은 정성 판단 영역(범위 해석·재미·개념 동일성)에만 쓴다. 사람 게이트는 자동 검증이 원리적으로 커버 못 하는 것만 보게 하여 검토 피로를 낮춘다.

### 5.2 도메인별 사실 검증기 매트릭스 ★핵심★

**라이선스가 도구 선택을 결정한다.** 게임은 공개 서비스이므로 재배포·서비스 제공이 허용되는 데이터만 쓸 수 있다.

| 계열 | 검증 대상 | 도구 / 데이터 소스 | 라이선스 | 가부 |
|------|----------|-------------------|----------|------|
| **CHEM** | IUPAC명 → 구조 파싱 | OPSIN (`py2opsin`/`pyopsin`) | Open source (MIT/Apache) | ✅ |
| CHEM | 정규 SMILES·분자식 | RDKit | BSD | ✅ |
| CHEM | 화합물 외부 대조 | PubChem PUG REST | 미국 NIH, 퍼블릭 도메인 | ✅ |
| CHEM | 원소 사실 | 로컬 주기율표 데이터셋 (IUPAC 공표값) | 자체 구축 | ✅ |
| **BIO** | 대사 경로 순서·반응 | **Reactome ContentService** | **데이터 CC0** — 상업 이용·재배포 자유 | ✅ **권장** |
| BIO | 대사 경로 보조 | WikiPathways | CC0 | ✅ |
| BIO | 반응 정보 | Rhea (EBI) | CC-BY | ✅ (출처 표기) |
| BIO | 분류 체계 | NCBI Taxonomy (E-utilities), GBIF | 퍼블릭 도메인 / Open | ✅ |
| BIO | 단백질·효소 | UniProt | CC-BY | ✅ (출처 표기) |
| BIO | ~~대사 경로~~ | ~~KEGG~~ | **학술 목적이라도 "서비스 제공"에 라이선스 필요** | ❌ **사용 금지** |
| **EARTH** | 지질시대 순서·연대 | ICS 국제연대층서표 (stratigraphy.org), Macrostrat API | 무료 공개(인용 조건) | ✅ |
| EARTH | 항성·천체명 | IAU WGSN 공식 항성명 목록 | 공개 | ✅ |
| EARTH | 태양계 천체 데이터 | NASA JPL / NASA 공개 데이터 | 미국 정부, 퍼블릭 도메인 | ✅ |
| EARTH | 광물 | 정적 데이터셋 자체 구축 (교과서 기준) | 자체 | ✅ |
| **PHYS** | 물리 상수 | NIST CODATA | 미국 정부, 퍼블릭 도메인 | ✅ |
| PHYS | SI 단위·접두어 | BIPM SI Brochure | 공개 | ✅ |
| PHYS | 소립자 | Particle Data Group (PDG) | 공개 | ✅ |
| **공통** | 교육과정 성취기준 | 국가교육과정정보센터(NCIC) 원문 | 공공저작물 | ✅ |

> **C6 대응 결론**: 생명과학 대사 경로의 1차 검증 소스는 **Reactome(CC0)**으로 확정한다. KEGG는 코드·문서·프롬프트 어디에도 등장시키지 않는다. `fact-validator`의 지침에 **금지 소스 목록**을 명시한다.

### 5.3 계열별 검증 항목

| 계열 | 검사 항목 | 판정 기준 |
|------|----------|----------|
| CHEM | 이름→구조 파싱 성공 / 왕복 SMILES 일치 / 분자식 일치 / 원자번호·족·주기 일치 / 순서 단조성 | 완전 일치 |
| BIO | 대사 중간체가 해당 경로에 실제 존재 / **경로 내 순서 일치** / 분류 계급 상하위 관계 / 효소·기질 대응 | 외부 DB와 순서까지 일치 |
| EARTH | 지질시대 순서·연대 범위 / 층서 상하 관계 / 천체 공식 명칭 / 분광형 온도 순서 / 보웬 계열 순서 | ICS·IAU 공식 데이터와 일치 |
| PHYS | 파장·진동수 대소 관계 / 접두어 지수 / 단위 기호 / 상수 유효숫자 | CODATA·BIPM과 일치 |
| 공통 | 정답 문자열 유일성 / 입력 가능성 / 표기 규정 준수 / 순환·분기 구조 무결성 | 규칙 위반 0건 |

---

## 6. 구현 스펙

### 6.1 폴더 구조

```
/scimap
  ├── CLAUDE.md                          # 메인 에이전트(오케스트레이터) 지침
  ├── /.claude
  │   ├── /skills
  │   │   ├── /chem-facts                 # CHEM 계열 사실 검증          ★공용
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # opsin_parse.py, roundtrip.py, pubchem.py, element.py
  │   │   │   └── /references            # elements.json, 대한화학회 표기 사전
  │   │   ├── /bio-facts                  # BIO 계열 사실 검증           ★공용
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # reactome.py, pathway_order.py, taxonomy.py
  │   │   │   └── /references            # 경로 스냅샷(CC0), 생명 용어 표기, ⚠️금지 소스 목록
  │   │   ├── /earth-facts                # EARTH 계열 사실 검증         ★공용
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # ics_timescale.py, iau_names.py, order_check.py
  │   │   │   └── /references            # ICS 연대층서표 데이터, 천체명 목록
  │   │   ├── /phys-facts                 # PHYS 계열 사실 검증          ★공용
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # codata.py, si_prefix.py, spectrum_order.py
  │   │   │   └── /references            # SI 기준 문서 요약
  │   │   ├── /typeability-check          # 공통: 입력 가능성            ★공용
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # check_typeable.py, normalize.py, chosung.py
  │   │   ├── /curriculum-mapper          # 공통: 성취기준 매핑          ★공용
  │   │   │   ├── SKILL.md
  │   │   │   └── /references            # 2022개정 성취기준 전문, 2015개정 대응표
  │   │   ├── /course-schema              # 공통: 스키마·중복·구조 검증
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # validate.py, dedupe.py, loop_check.py
  │   │   │   └── /references            # *.schema.json
  │   │   ├── /network-graph              # 노선망: 환승 탐지·그래프
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # detect_transfers.py, build_graph.py, layout.py
  │   │   │   └── /references            # concept_registry 스키마
  │   │   ├── /review-packager
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # build_review.py
  │   │   ├── /playlog-analyzer
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # aggregate.py
  │   │   └── /course-deploy
  │   │       ├── SKILL.md
  │   │       └── /scripts               # version.py, manifest.py, diff.py
  │   └── /agents
  │       ├── /course-designer
  │       │   └── AGENT.md
  │       ├── /content-writer
  │       │   └── AGENT.md
  │       ├── /fact-validator
  │       │   └── AGENT.md
  │       ├── /playtest-reviewer
  │       │   └── AGENT.md
  │       └── /network-curator            # v0.2 신규
  │           └── AGENT.md
  ├── /output
  │   ├── /requests  /courses  /validation  /review
  │   ├── /network                        # v0.2 신규
  │   ├── /deploy  /insights  /logs
  ├── /docs
  │   ├── course_backlog.yaml
  │   ├── concept_registry.yaml           # ★공용 — 환승 개념 사전(사람 관리)
  │   ├── homonyms.yaml                   # ★공용 — 동음이의 금지쌍
  │   ├── naming_policy.md                # ★공용 — 과목별 표기 정책
  │   ├── data_sources.md                 # v0.2 신규 — 허용/금지 데이터 소스와 라이선스
  │   └── game_spec.md
  └── /app                                # Sci_Map 앱 (Next.js + Supabase). 노선 탭이 이 계열의 소비자
```

위 트리에는 노선 계열이 쓰는 것만 적었다. 개념 계열의 스킬과 서브에이전트는 같은 `/.claude` 아래 나란히 배치되며, 그 상세 목록과 배치는 `Sci_Map_에이전트_설계서.md` §7.1을 따른다. **★ 표시가 붙은 자산은 두 계열이 같은 파일을 읽는다. 어떤 경우에도 복사본을 만들지 않는다.** 복사본을 하나 만드는 순간 표기 정책과 개념 키가 계열마다 갈라지기 시작하고, 그 어긋남은 노선 탭과 개념 탭이 같은 개념을 다르게 부르는 형태로 학생에게 먼저 드러난다.

### 6.2 CLAUDE.md 핵심 섹션 목록

> 상세 본문은 구현 시 작성. 아래는 **섹션 목록과 담아야 할 것**만 정의.

1. **역할 정의** — 오케스트레이터. 직접 콘텐츠를 만들지 않고 서브에이전트에 위임
2. **두 파이프라인의 상태 기계** — 코스(S0~S9) / 노선망(N1~N3), 현재 단계 판별 규칙, N1 트리거 조건
3. **계열 라우팅 규칙** — track 판별 → 활성화할 도메인 스킬 결정 (*v0.2 신규*)
4. **서브에이전트 호출 규칙** — 언제 누구를, 서브에이전트 간 직접 호출 금지 명시
5. **데이터 전달 규칙** — 중간 산출물은 `/output/`에 파일로, 프롬프트에는 경로만
6. **실패 처리 정책** — 단계별 재시도 횟수, 에스컬레이션 조건, 스킵 허용 단계
7. **사람 게이트 규칙** — S7·N3 우회 절대 금지, 승인 확인 방법
8. **표기 정책** — 과목별 현행 표기 고정, 별칭 취급 방식
9. **⚠️ 금지 데이터 소스** — KEGG 등 라이선스 불가 소스 명시 (*v0.2 신규*)
10. **금지 사항** — 과학 사실을 스크립트 검증 없이 통과시키지 않기, 배포 자동 재시도 금지, 개념 사전 무단 갱신 금지
11. **로깅 규칙** — 모든 스킵/에스컬레이션은 `/output/logs/`에 사유 기록

### 6.3 에이전트 구조

**서브에이전트 분리를 채택한다.** 근거:

- 각 역할이 요구하는 **도메인 지식이 완전히 다르다** — 코스 설계는 교육과정+게임디자인, 콘텐츠 작성은 과목별 표기 규칙, 검증은 도구 사용법, 노선망은 개념 온톨로지, 플레이테스트는 학습과학+UX
- **과목이 4개로 늘면서 references 총량이 v0.1의 4배**가 된다. 성취기준 전문·표기 사전·4개 계열 DB 가이드를 항상 로드하는 것은 비효율 → 컨텍스트 최적화 효과가 결정적
- 파이프라인 단계가 명확히 분리되어 **독립 작업 블록** 조건 충족

| # | 서브에이전트 | 담당 단계 | 역할 |
|---|-------------|----------|------|
| 1 | `course-designer` | S1 | 코스 구성·순서·유형·난이도 곡선 설계 |
| 2 | `content-writer` | S2 | 스테이션 데이터 작성 (표기·힌트·외부 ID) |
| 3 | `fact-validator` | S3, S5 | 계열별 결정론적 검증 실행 + 교육과정 범위 판단 |
| 4 | `playtest-reviewer` | S6, S9 | 게임플레이 품질 평가 + 플레이 로그 해석 |
| 5 | `network-curator` | N2 | 환승 유효성 판단, 개념 사전 갱신 제안 (*v0.2 신규*) |

한 리포에 두 계열이 들어가면서 이름 충돌이 하나 생긴다. 개념 계열에도 `fact-validator`가 있으므로, 구현 시에는 `course-fact-validator`(노선) / `concept-fact-validator`(개념)로 분리해 명명한다. 단순한 이름 문제가 아니라 역할이 실제로 다르다. 노선 쪽은 스크립트 검증 결과를 읽어 보고하는 역할이고 스스로 사실을 판정하지 않는다. 개념 쪽은 명제 자체의 구조적 결함을 탐지하는 역할이라 판단 비중이 크다. 같은 이름으로 두면 메인 에이전트가 위임할 때 두 지침이 섞이고, 섞인 결과는 "LLM이 과학 사실을 스스로 판정하는" 가장 피하고 싶은 상태로 수렴한다.

S0/S4/S7/S8/N1/N3은 메인 에이전트가 스킬을 직접 호출해 처리한다(판단 요소가 적음).

**계열 확장 방식**: 새 과목이 추가되어도 서브에이전트는 늘리지 않는다. **`<track>-facts` 스킬 하나만 추가**하면 `fact-validator`가 계열 라우팅으로 집어 쓴다. 이것이 v0.2에서 `chem-validator` → `fact-validator`로 일반화한 이유다.

### 6.4 서브에이전트 상세

| 항목 | course-designer |
|------|----------------|
| 트리거 | 메인이 S1 진입 시 |
| 입력 | `request.json` 경로 + 기존 코스 목록 경로 + `concept_registry.yaml` 경로 |
| 출력 | `<id>.outline.json` |
| 참조 스킬 | `curriculum-mapper`, 해당 계열 `<track>-facts` |
| 핵심 지식 | 교육과정 위계, 코스 유형 판단, 난이도 곡선 설계, 중복 회피 |

| 항목 | content-writer |
|------|----------------|
| 트리거 | 메인이 S2 진입 시 (S1 산출물 존재) |
| 입력 | `outline.json` 경로 |
| 출력 | `<id>.draft.json` |
| 참조 스킬 | 해당 계열 `<track>-facts`, `course-schema`, `typeability-check` |
| 핵심 지식 | 과목별 현행 표기, 힌트 작성 요령, 외부 ID 기입 규칙 |

| 항목 | fact-validator |
|------|----------------|
| 트리거 | 메인이 S3 또는 S5 진입 시 |
| 입력 | `<id>.draft.json` 경로 + 검사 모드(`deterministic` \| `curriculum`) + track |
| 출력 | `<id>.report.json` (스테이션별 pass/fail/warn + 사유) |
| 참조 스킬 | `chem-facts` / `bio-facts` / `earth-facts` / `phys-facts` (track에 따라 택1), `curriculum-mapper` |
| 핵심 지식 | 계열별 검증 절차, 외부 DB 사용법, **금지 소스 목록**, 성취기준 전문 |
| 특기 | 과학 사실 판정을 **스스로 하지 않고 스크립트 결과만 보고**한다 |

| 항목 | playtest-reviewer |
|------|----------------|
| 트리거 | 메인이 S6 진입 시, 또는 S9 튜닝 요청 시 |
| 입력 | 검증 통과 draft 경로 (S6) / 집계 로그 경로 (S9) |
| 출력 | `<id>.playtest.json` / `<id>.tuning.md` |
| 참조 스킬 | `playlog-analyzer` |
| 핵심 지식 | 학습과학(간격 반복·인출 연습), 타이핑 게임 UX, 코스 유형별 평가 기준 |

| 항목 | network-curator *(v0.2 신규)* |
|------|----------------|
| 트리거 | 메인이 N2 진입 시 (N1 산출물 존재) |
| 입력 | `transfer_candidates.json` 경로 + 관련 코스 경로 목록 |
| 출력 | `transfer_verdicts.json` (valid/invalid/uncertain + 근거) |
| 참조 스킬 | `network-graph` |
| 핵심 지식 | 과학 개념 온톨로지, 과목 간 동음이의어 목록, 교육과정 상 연계 지점 |
| 특기 | **자동 확정 권한 없음.** 판정과 근거만 제출하고 개념 사전 갱신은 사람이 승인 |

**서브에이전트 간 직접 호출은 금지**한다. 모든 조율은 메인 에이전트를 통한다.

### 6.5 스킬 목록

| 스킬 | 역할 | 트리거 조건 | 주요 스크립트 |
|------|------|------------|--------------|
| `chem-facts` | 원소·IUPAC명·분자식 검증 | track=CHEM인 S2·S3 | `opsin_parse.py`, `roundtrip.py`, `pubchem.py`, `element.py` |
| `bio-facts` | 대사 경로 순서·분류 체계 검증 (Reactome/NCBI) | track=BIO인 S2·S3 | `reactome.py`, `pathway_order.py`, `taxonomy.py` |
| `earth-facts` | 지질시대·천체명·순서 검증 (ICS/IAU) | track=EARTH인 S2·S3 | `ics_timescale.py`, `iau_names.py`, `order_check.py` |
| `phys-facts` | 물리상수·단위·스펙트럼 순서 검증 (CODATA/BIPM) | track=PHYS인 S2·S3 | `codata.py`, `si_prefix.py`, `spectrum_order.py` |
| `typeability-check` | 입력 가능성 판정·평문 변환·초성 생성 | S2 직후, S4 | `check_typeable.py`, `normalize.py`, `chosung.py` |
| `curriculum-mapper` | 성취기준 태깅·범위 이탈 검사 보조 | S1, S5 | (references 중심) |
| `course-schema` | 스키마 검증·정답 중복·순환/분기 무결성 | S0, S2, S4, S8 | `validate.py`, `dedupe.py`, `loop_check.py` |
| `network-graph` | 환승 후보 탐지·그래프 구성·배치 초안 | N1, N2, N3 | `detect_transfers.py`, `build_graph.py`, `layout.py` |
| `review-packager` | 교사 검토용 md 리포트 생성 | S7 진입 시 | `build_review.py` |
| `playlog-analyzer` | 플레이 로그 집계·통계 | S9 트리거 충족 시 | `aggregate.py` |
| `course-deploy` | 버전 발급·매니페스트·diff | S7 승인 후 | `version.py`, `manifest.py`, `diff.py` |

**외부 의존성**: `rdkit`, `py2opsin`, `jsonschema`, `requests`, `networkx`(그래프). 전부 오픈소스이며, 외부 API는 Reactome·PubChem·NCBI·Macrostrat 등 무료 공개 엔드포인트만 사용한다.

### 6.6 데이터 전달 패턴

| 데이터 | 방식 | 근거 |
|--------|------|------|
| 코스 outline / draft / 최종본 | **파일 기반** (`/output/courses/`) | 스테이션 수십 개, 구조화 |
| 검증 리포트 | **파일 기반** (`/output/validation/`) | 스테이션별 상세 사유 |
| 환승 후보·판정·노선망 | **파일 기반** (`/output/network/`) | 전체 코스 교차 참조, 대용량 |
| 플레이 로그 집계 | **파일 기반** (`/output/logs/`) | 대용량 |
| 단계 진입 지시·track·검사 모드·재시도 사유 | **프롬프트 인라인** | 짧고 단순 |
| 사람 승인 여부 | **파일 기반** (`review.md` / `registry_proposal.yaml` 플래그) | 감사 추적 필요 |

원칙: **서브에이전트에는 파일 경로만 전달**하고 내용을 프롬프트에 붙여넣지 않는다.

### 6.7 Sci_Map 노선 탭 스펙 (초기 1회 스캐폴딩, 구조 개요)

노선 게임은 별도 앱이 아니다. **Sci_Map 앱의 다섯 탭 중 하나**로 들어가며, 계정·기록·개념 사전을 나머지 탭과 공유한다. 아래 레이어 구성은 그 탭이 필요로 하는 것을 적은 것이고, 앱 껍데기·라우팅·인증은 Sci_Map 쪽이 이미 세운 것을 그대로 쓴다.

**아키텍처**: 웹 + 백엔드 (서버 검증 랭킹, 실시간 멀티 요구사항 반영)

| 레이어 | 구성 요소 | 역할 |
|--------|----------|------|
| 프론트 | 타이핑 엔진 | 입력 정규화, 정답 판정, 오타 시 정지, 자동/Space 이동 옵션 |
| 프론트 | **노선망 뷰** | 전체 과학 노선도. 환승역·대환승역 강조, 완주 코스 색칠 |
| 프론트 | 코스 뷰 | 직선/순환/분기/격자 4종 레이아웃 렌더러 |
| 프론트 | 모드 화면 | 싱글 / 멀티 / 퀴즈 / 암기 / 타임어택 / **순환선 무한 랩** |
| 프론트 | **개념 점프** | 스테이션에서 그 개념의 카드로 이동. 카드 쪽에서도 역방향 진입 |
| 백엔드 | 코스 API | `/output/courses/` 산출물을 버전별 서빙 |
| 백엔드 | **노선망 API** | `network.json` 서빙, 환승 해금 상태 관리 |
| 백엔드 | **개념 연동** | 코스의 각 스테이션 `concept_key`로 개념 카드 조회. 완주 시 해당 개념들의 `study_states` 갱신 |
| 백엔드 | 기록 검증 | 타수·정확도·완주시간 서버 재계산 (조작 방지) |
| 백엔드 | 랭킹 | 코스·버전별 리더보드 |
| 백엔드 | 실시간 멀티 | WebSocket 룸, 최대 4인 |
| 백엔드 | 로그 수집 | 스테이션별 오타·이탈 이벤트 → S9 입력 |

**핵심 인터페이스 계약**: 게임은 `course.schema.json`과 `network.schema.json`을 따르는 JSON만 소비한다. 파이프라인과 게임의 유일한 접점이며, 스키마 변경은 버전 발급을 동반한다. 다만 계정·기록 테이블(`students`·`study_states`)은 개념 계열과 공용이므로 스키마 소유권은 `Sci_Map_에이전트_설계서.md` §7.5에 있다.

### 6.8 주요 산출물 파일 형식

| 산출물 | 형식 | 스키마 |
|--------|------|--------|
| 코스 요청 | JSON | `request.schema.json` |
| 코스 아웃라인 | JSON | `outline.schema.json` |
| 코스 데이터 | JSON | `course.schema.json` |
| **노선망 맵** | JSON | `network.schema.json` |
| **환승 후보/판정** | JSON | `transfer.schema.json` |
| 검증 리포트 | JSON | `report.schema.json` |
| 교사 검토 패키지 | Markdown | 템플릿 고정 |
| 개념 사전 | YAML | 사람 관리, 스키마 검증만 |
| 배포 매니페스트 | JSON | `manifest.schema.json` |
| 튜닝 제안 | Markdown | 템플릿 고정 |
| 실행 로그 | JSONL | 단계·결과·사유·타임스탬프 |

---

## 7. 성공 기준 (시스템 레벨)

최우선 기준은 **"교육 효과·재미 균형"**이다. 측정 가능한 형태로 분해한다.

| 층위 | 지표 | 목표 |
|------|------|------|
| **정확성 (하한선)** | 배포 콘텐츠의 과학 오류 건수 | **0건** (타협 불가) |
| 정확성 | 자동 검증이 잡아낸 오류 / 전체 오류 | ≥ 95% |
| 정확성 | 라이선스 위반 데이터 소스 사용 | **0건** |
| **교육 효과** | 코스 완주 후 동일 코스 재도전율 | ≥ 40% |
| 교육 효과 | 3회차 평균 오타율 / 1회차 | ≤ 50% |
| 교육 효과 | **환승역을 통한 타 과목 코스 진입률** | ≥ 25% *(v0.2 신규 — 노선망의 존재 이유)* |
| **재미** | 코스 시작 대비 완주율 | ≥ 60% |
| 재미 | S6 게임플레이 평가 평균 | ≥ 3.5 / 5 |
| 재미 | 순환선 평균 랩 수 | ≥ 2랩 |
| **효율** | 코스 1개당 사람 개입 시간 | ≤ 10분 (교사 검토만) |
| 효율 | 요청→검토 대기까지 자동 처리 성공률 | ≥ 70% |
| **노선망** | 고립 코스(환승 0개) 비율 | ≤ 10% |
| 노선망 | N2 `uncertain` 비율 | ≤ 20% |

---

## 8. 착수 순서 권장안

전 과목을 동시에 시작하지 않는다. **검증 난이도가 낮고 노선 적합도가 높은 순서**로 간다.

| 단계 | 대상 | 이유 |
|------|------|------|
| **0차** | 공용 자산 분리 (`concept_registry.yaml`·`homonyms.yaml`·`naming_policy.md`·공통 스킬 4종·계정 스키마) | 나중에 합치면 이미 갈라진 뒤다. 계열 통합보다 먼저 |
| **1차** | EARTH — 지질시대 본선 | 노선 적합도 최고, 검증 소스(ICS)가 정적 데이터라 가장 단순. 파이프라인 S0~S9 검증에 최적 |
| **2차** | BIO — 해당과정 + TCA 순환선 | 순환선 게임 규칙 검증, Reactome 연동 검증 |
| **3차** | CHEM — 알케인 본선 + 주기율표 | OPSIN 왕복 검증 등 가장 복잡한 검증 로직 |
| **4차** | 노선망 파이프라인 N1~N3 가동 | 코스가 3계열 이상 쌓인 뒤에야 환승이 의미를 가짐 |
| **5차** | PHYS — 전자기파선 | 적합도 낮음, 보조 계열 |
| **통합** | Sci_Map 5탭 연결, 개념 점프, `study_states` 상호 반영 | 노선 계열과 개념 계열이 **각각 완주한 뒤**. 1인 개발에서 우산을 먼저 세우면 양쪽 다 미완성으로 끝난다 |

1차만으로 파이프라인 전체가 한 바퀴 돌아야 하며, 그 전에 2차로 넘어가지 않는다. 0차만 제대로 해두면 통합은 연결 코드를 쓰는 일이 되고, 건너뛰면 데이터 마이그레이션이 된다.

---

## 9. 열린 질문 / 다음 단계

1. **표기 정책 확정** — 과목별 별칭 처리 기준. 정답 인정 범위를 어디까지 (`/docs/naming_policy.md`)
2. **교육과정 기준 확정** — 2022 개정 단독인지, 2015 개정 병행 태깅인지
3. **개념 사전 초기 구축** — `concept_registry.yaml`의 시드 개념을 누가 몇 개나 만들 것인지. §2.5의 10개로 시작할지. **개념 계열과 공용이므로 0차 대상**
4. **동음이의어 목록** — `network-curator`가 참조할 "환승 금지 쌍" 목록을 미리 만들 것인지. **개념 계열과 공용이므로 0차 대상**
5. **격자형 코스의 게임 규칙** — 주기율표·아미노산 같은 격자는 "순서 타이핑"이 어색하다. 별도 모드(위치 맞히기)로 뺄지
6. **BIO 용어의 타이핑 부담** — `글리세르알데하이드-3-인산` 같은 15자 이상 항목 처리. 약어 정답(`G3P`) 허용 여부
7. **게임 엔진 리포 분리 여부** — → **v0.3에서 해결.** Sci_Map과 한 리포(`/scimap`), 앱은 `/app`
8. **노선 완주의 인출 가중치** — 코스 완주가 `study_states`에 몇 회 인출로 반영될지. 타이핑은 화면에 다음 역이 보이는 재인에 가까워, 카드 인출과 같은 무게를 주면 실력이 과대평가된다. 부분 가중(예: 0.3회분)이나 별도 계열 지표 분리를 검토
9. **판정 경합 조정 규칙** — 환승 판정(N2)과 개념 계열의 `same` 링크 판정이 같은 후보를 놓고 엇갈릴 때 무엇을 우선할지. 먼저 확정된 쪽을 따를지, 두 판정이 모두 `valid`일 때만 사전에 올릴지

---

## 부록 A. 참고 자료

**레퍼런스 게임**

- [METRO TYPING](https://metrotyping.kr/)
- [메트로 타이핑 — 나무위키](https://namu.wiki/w/메트로%20타이핑)

**교육과정**

- [2022 개정 교육과정 과학과 고등학교 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교)
- [2022 개정 교육과정 화학 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교/화학)
- [2022 개정 교육과정 생명과학 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교/생명과학)
- [2022 개정 교육과정 세포와 물질대사 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교/세포와%20물질대사)

**검증 도구 · 데이터 소스**

- [OPSIN (dan2097/opsin)](https://github.com/dan2097/opsin) — IUPAC 이름 → 구조 오픈소스 파서
- [pyopsin (PyPI)](https://pypi.org/project/pyopsin)
- [Reactome ContentService API](https://reactome.org/ContentService/) — 대사 경로
- [Reactome License Agreement](http://reactome.org/license/) — **데이터 CC0 확인**
- [KEGG Copyright and Disclaimer](https://www.kegg.jp/kegg/legal.html) — **서비스 제공 시 라이선스 필요, 사용 불가 근거**
- [ICS Interactive International Chronostratigraphic Chart](https://stratigraphy.org/timescale/) — 지질시대
- [strat2file (기계 판독용 ICS 데이터)](https://github.com/TobbeTripitaka/strat2file)

**같은 플랫폼의 다른 계열**

- `Sci_Map_에이전트_설계서.md` v0.2 — Sci_Map 플랫폼 정의(§0), 개념·문항·지도·오늘 계열 파이프라인
- `ChemTyping_에이전트_설계서.md` v0.1 — 이 문서의 전신(화학 단독)
