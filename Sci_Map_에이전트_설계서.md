# Sci_Map 콘텐츠 파이프라인 에이전트 설계서

> 2022 개정 교육과정 과학과의 **단원별 핵심 개념 카드 · 개념 연계 그래프 · 기출 문항 은행 · 적응형 데일리 테스트**를
> 교과서 원본에서 자동 생성·검증·배포하는 에이전트 시스템 설계서
>
> 버전: **v0.2** | 작성일: 2026-09-05 | 대상 앱: **Sci_Map(사이맵)** — Next.js + Supabase 웹앱
> 사이음(sci_eum) 연구회 산출물 계열. 이 문서는 Claude Code에서 구현할 때 참조하는 **계획서**다.
> CLAUDE.md·AGENT.md·스킬의 상세 본문은 구현 단계에서 작성한다.
>
> **이 문서가 다루는 범위**: Sci_Map 플랫폼 전체 구조(§0.2) + **개념·문항·그래프·퀴즈 4개 계열**의 파이프라인.
> **노선 계열**(타이핑 게임)은 `SciMetro_에이전트_설계서.md` v0.3에서 별도로 다룬다. 두 문서는 §0.3의 공용 자산을 공유한다.

## v0.1 → v0.2 변경 요약

| 항목 | v0.1 (SciLex) | v0.2 (Sci_Map) |
|------|--------------|----------------|
| 이름 | SciLex(가칭, 개념이음) | **Sci_Map(사이맵)** — sci_eum 표기 컨벤션 승계 |
| 위상 | 단독 앱 | **플랫폼**. SciMetro가 그 안의 노선 계열로 편입 (§0.2) |
| 접근 통제 | 앱 전체 폐쇄 | **자산 단위 접근제어** — 저작물만 닫고 자체 제작물은 공개 (§0.4) |
| 개념 사전 | 자체 관리 | **SciMetro와 공용** `concept_registry.yaml` (§0.3) |
| 공통 스킬 | SciMetro에서 복사 | **공용 패키지로 분리** — 복사본 금지 (§0.3) |

---

## 0. 전제와 플랫폼 구조

### 0.1 확정된 전제

착수 인터뷰에서 확정한 사항이다. 이후 모든 설계는 이 전제 위에 서 있다.

| # | 항목 | 결정 |
|---|------|------|
| P1 | 설계 범위 | **콘텐츠 파이프라인 중심**. 앱은 스키마 계약과 구조 개요만 (§7) |
| P2 | 기출 소스 | **수능·평가원 모의고사** 문항 원문 이미지 |
| P3 | 앱 형태 | **웹앱 — Next.js + Supabase** (PWA로 모바일 대응) |
| P4 | 초기 범위 | **화학 계열 종단** — 통합과학1·2의 화학 단원 → 화학 → 물질과 에너지 / 화학 반응의 세계 |
| P5 | 개념 카드 필수 필드 | 정의 + 한자어 + 영어 / **관계 명제** / 흔한 오개념·함정 / 선수·후속 개념 링크 (4종 전부) |
| P6 | 시각자료 | **교과서 그림 크롭** |
| P7 | 데일리 테스트 | **망각곡선 기반 적응형** |
| P8 | 저작물 열람 범위 | **내 수업 학생 한정** (초대 기반). 단 §0.4에 따라 **앱 전체가 아니라 해당 자산에만** 적용 |
| P9 | 플랫폼 통합 | **SciMetro를 Sci_Map 안의 노선 계열로 편입.** 데이터·계정·앱은 통합, 파이프라인은 분리 (§0.2) |

**P2·P6·P8이 한 덩어리로 묶인다.** 타인의 저작물(교과서 그림, 평가원 문항)을 그대로 쓰는 근거는 저작권법 제25조 제3항의 **수업 목적 이용**이다. 이 조항은 공짜 통행증이 아니라 **기술적 의무를 동반한다**(§1.5 R1~R3). 그 의무가 곧 앱의 필수 기능이 되므로, 이 설계서에서는 저작권을 법적 주석이 아니라 **아키텍처 제약**으로 다룬다.

### 0.2 플랫폼 구조 — 다섯 개의 콘텐츠 계열

Sci_Map은 앱 하나가 아니라 **개념 키를 공유하는 학습 콘텐츠 플랫폼**이다. 계열마다 학습 방식이 다르고 파이프라인도 다르지만, 가리키는 개념은 같다.

| 계열 | 콘텐츠 단위 | 학습 방식 | 파이프라인 | 문서 |
|------|------------|----------|-----------|------|
| **개념** | 개념 카드 | 읽고 이해 | [A] C0~C10 | 이 문서 |
| **문항** | 기출 문제 | 풀고 확인 | [B] Q0~Q6 | 이 문서 |
| **지도** | 개념 그래프 | 훑고 이동 | [C] G1~G3 | 이 문서 |
| **오늘** | 데일리 문항 | 인출 반복 | [D] D1~D3 | 이 문서 |
| **노선** | 타이핑 코스 | 순서 암기 | [E] S0~S9 + N1~N3 | `SciMetro_에이전트_설계서.md` v0.3 |

**통합의 근거는 `concept_key` 하나다.** SciMetro의 **환승역**과 이 문서의 **`same` 링크**는 서로 다른 이름을 붙였을 뿐 **같은 장치**다 — 둘 다 "이름이 달라도 같은 개념이면 같은 키"라는 규칙 위에 서 있다. 사전이 두 벌이면 `concept:carbon`이 두 시스템에서 갈라지고, 그 순간 학생이 알케인 본선에서 만난 "탄소"와 개념 카드의 "탄소"가 남남이 된다. 통합의 실익은 전부 여기서 나온다.

**반대로 파이프라인은 합치지 않는다.** 검증 철학이 정반대이기 때문이다.

| | 노선 계열 (SciMetro) | 개념 계열 (이 문서) |
|---|---|---|
| 무엇을 검증하나 | 항목의 **순서와 사실** | 명제의 **진위** |
| 누가 판정하나 | **스크립트** (ICS 연대표, OPSIN 왕복, Reactome) | **사람** (교사 게이트, C8) |
| 사람 게이트의 역할 | 자동 검증이 놓친 것 확인 | **유일한 진위 판정 지점** |
| 사람 개입 시간 | 코스당 ≤ 10분 | 단원당 ≤ 20분 |

외부 DB로 대조 가능한 지식과, 원리적으로 대조 불가능한 지식을 한 파이프라인에 밀어 넣으면 둘 중 하나가 망가진다. 리포는 하나로 두되 **계열별 파이프라인은 병렬로 배치**한다.

### 0.3 공용 자산

두 설계서가 **같은 실체를 가리키는** 항목이다. 복사본을 두지 않는다 — 복사한 순간 갈라진다.

| 자산 | 경로 | 성격 |
|------|------|------|
| **개념 키 사전** | `/docs/concept_registry.yaml` | 사람 관리. 신규 키 등록은 어느 계열에서 제안하든 **사람 승인** |
| 동음이의 금지쌍 | `/docs/homonyms.yaml` | 사람 관리. "주기·핵·전위·일·계" |
| 표기 정책 | `/docs/naming_policy.md` | 현행 표기 고정, 구표기 별칭 |
| `typeability-check` 스킬 | `/.claude/skills/typeability-check` | 정답 평문 정규화·초성 |
| `term-lexicon` 스킬 | `/.claude/skills/term-lexicon` | 한자어·영어 대조 |
| `curriculum-mapper` 스킬 | `/.claude/skills/curriculum-mapper` | 성취기준 태깅 |
| `chem-facts` 스킬 | `/.claude/skills/chem-facts` | OPSIN·RDKit·PubChem 화학 사실 검증 |
| 학생 계정·학습 기록 | Supabase `students`, `study_states` | **계열 통합.** 노선 완주 기록이 개념 카드의 FSRS 상태에 반영된다 |

마지막 항목이 학생 체감 통합의 핵심이다. 알케인 본선을 완주한 학생이 `concept:alkane` 카드에서 신규 취급을 받으면, 한 앱에 둘 이유가 없다.

### 0.4 접근 통제 경계선 ★설계 변경★

v0.1은 앱 전체를 폐쇄로 잡았다. 다시 보면 과했다. **닫아야 하는 것은 앱이 아니라 자산 두 종류**다.

| 계층 | 저작권 성격 | 공개 범위 |
|------|-----------|----------|
| 개념 카드 **본문** | 원문 재서술 → **교사 저작물** (R4가 이를 보증) | 공개 가능 |
| 개념 그래프 · 데일리 문항 | 카드에서 파생 → 교사 저작물 | 공개 가능 |
| 노선 코스 데이터 | 자체 제작 + 공공데이터 | 공개 가능 |
| **교과서 그림 크롭** | 천재교과서 저작물 | **로그인 + 초대 코드 + 서명 URL** |
| **기출 문항 이미지** | 한국교육과정평가원 저작물 | **로그인 + 초대 코드 + 서명 URL** |

경계선을 앱 바깥이 아니라 **자산 단위로** 긋는 것이 두 가지를 동시에 얻는다.

1. **노선 계열의 공개 랭킹·실시간 멀티가 살아난다.** SciMetro의 동기 부여 장치는 서버 검증 랭킹인데, 학생 25명짜리 리더보드는 리더보드가 아니다.
2. **저작권 근거가 오히려 단단해진다.** "앱 전체를 닫았다"보다 "해당 저작물에만 접근제한 조치를 걸었다"가 제25조 제12항의 취지에 정확히 대응한다.

구현상으로는 `media_assets.access_tier` 한 필드로 갈린다(`public` \| `restricted`). `restricted` 자산은 비공개 버킷 + 단시간 서명 URL로만 나가고, 그 자산이 붙은 카드를 비로그인 사용자가 열면 **카드 본문은 보이고 그림 자리만 잠금 안내**가 뜬다. 카드를 통째로 막지 않는다.

---

## 1. 작업 컨텍스트

### 1.1 배경

**선생우비**의 두 축은 다음과 같다.

| 앱 | 구조 | 학습 원리 |
|----|------|----------|
| 2차 면접 앱 | 예상 문제 + 역대 기출을 반복 연습 | 실전 형식 노출 |
| 큐알교육학 한판노트 | 개념을 가리고 **인출**하게 함 | 인출 연습(retrieval practice) |

이 조합이 효과적인 이유는 **개념 저장소와 인출 장치가 같은 데이터를 공유**하기 때문이다. 개념 노트와 문제집이 따로 놀면 학생은 두 번 왔다 갔다 하지만, 한 앱 안에서 개념 카드가 곧 퀴즈의 원천이면 "보고 → 가리고 → 틀리고 → 다시 보고"가 한 화면에서 돈다.

Sci_Map은 이 구조를 과학과로 옮기되, 과학 교과의 두 가지 특성을 반영한다.

**첫째, 과학 개념은 낱말이 아니라 명제다.** "증기 압력"의 뜻을 아는 것과, "같은 온도에서 증기 압력이 크면 분자 간 상호작용이 작다"를 아는 것은 다른 지식이다. 시험에서 평가되는 것은 후자다. 그래서 개념 카드의 중심 필드는 정의가 아니라 **관계 명제**이며, OX 문항과 오답 선택지는 이 명제를 뒤집어서 만든다(§2.4).

**둘째, 과학 개념은 학년을 가로질러 반복된다.** 통합과학1에서 배운 "원소의 주기성"은 화학의 "물질의 구조와 성질"에서 다시, 더 깊게 나온다. 학생 입장에서는 같은 단어인데 교재가 갈라져 있어 연결이 끊긴다. 개념 카드에 **동일 개념의 학년 간 심화 체인**을 걸어두면, 화학을 공부하다 막혔을 때 통합과학의 그 개념으로 바로 되돌아갈 수 있다. 이것이 SciMetro의 "환승역"을 학습 사전 문맥으로 옮긴 것이다.

### 1.2 목적

- **1차 목적**: 학생이 단원별 핵심 개념을 **정의·표기·관계 명제·함정**까지 한 카드에서 파악하고, 관련 개념·선수 개념으로 끊김 없이 이동하며, 매일 짧은 인출 테스트로 유지한다.
- **2차 목적**: 교사가 개념 정리와 문항 정리를 손으로 하지 않고, **에이전트가 교과서에서 생성 → 검증 → 교사 검토 요청까지 자동 수행**한다.
- **이 설계서의 대상**: 위 2차 목적을 담당하는 **콘텐츠 파이프라인 에이전트 시스템**. 앱 자체는 스키마 계약과 초기 스캐폴딩 구조만 다룬다(§7).

### 1.3 범위

**포함**

- 교과서 PDF → 단원 분할 → 개념 카드 생성·검증·배포
- 개념 연계 그래프(선수/후속/동일 개념 심화) 구축 및 유효성 판단
- 교과서 그림 크롭·권리 메타 부착
- 평가원 기출 문항 분할·개념 매핑·자체 해설 작성
- 데일리 테스트 문항 풀 생성(OX·단답·선택형) 및 품질 검토
- 학습 로그 기반 카드·문항 튜닝 루프
- 앱의 스키마 계약과 저작권 대응 기능 요구사항(구조 수준)

**제외**

- 앱 UI/UX 비주얼 디자인
- 인프라 운영, 결제, 학부모 리포트
- 실시간 출제 로직의 구현 상세 (알고리즘 선택과 인터페이스만 §7.4)
- 콘텐츠의 최종 교육적 승인 (→ 교사 책임)

### 1.4 입출력 정의

**입력**

| 입력 | 형식 | 출처 | 비고 |
|------|------|------|------|
| 교과서 원본 | PDF | `/inbox/textbook/` (천재교과서) | 텍스트 레이어 유무 편차 있음 |
| 기출 원본 | PDF | `/inbox/exam/` (평가원 공개본) | 문항지 + 정답표 |
| 단원 백로그 | `/docs/unit_backlog.yaml` | 교사 작성 | 착수 순서·우선순위 |
| 교육과정 성취기준 | md (references) | NCIC 원문 | 공공저작물 |
| 용어 표기 기준 | md + API | 대한화학회 화학술어, 표준국어대사전, 편수용어 | 한자어·영어 대조용 |
| 교사 보유 오개념 목록 | `/docs/misconceptions.yaml` | 교사 작성 | 현장 데이터 — LLM이 못 만드는 부분 |
| 학습 로그 | JSON/CSV | Supabase | 카드·문항 튜닝 입력 |

**출력**

| 출력 | 경로 | 소비자 |
|------|------|--------|
| 개념 카드 | `/output/concepts/<concept-id>.json` | 앱 / Supabase |
| 개념 그래프 | `/output/graph/graph.json` | 앱 개념 지도 화면 |
| 링크 후보·판정 | `/output/graph/link_candidates.json`, `link_verdicts.json` | 파이프라인 |
| 시각자료 | `/output/media/<asset-id>.{png,webp}` + `.meta.json` | 앱 (서명 URL로만 노출) |
| 문항 | `/output/items/<item-id>.json` (+ 크롭 이미지) | 앱 문제 탭 |
| 데일리 문항 풀 | `/output/quiz/<concept-id>.quiz.json` | 앱 출제 엔진 |
| 검증 리포트 | `/output/validation/<unit-id>.report.json` | 파이프라인 |
| 교사 검토 패키지 | `/output/review/<unit-id>.review.md` | 교사 |
| 권리 대장 | `/output/rights/ledger.jsonl` | 교사 / 감사 대응 |
| 배포 매니페스트 | `/output/deploy/manifest.json` | 배포 스크립트 |
| 튜닝 제안 | `/output/insights/<unit-id>.tuning.md` | 교사 + 다음 사이클 |

### 1.5 제약조건

| # | 제약 | 근거 / 영향 |
|---|------|------------|
| **R1** | **타인 저작물 게재는 수업 목적 이용으로만 성립한다** | 저작권법 제25조 제3항. 고등학교 이하 학교는 제6항에 따라 **보상금 지급 의무 면제**. 단, 제12항에 따라 공중송신 시 **복제방지 등 대통령령이 정하는 조치** 필요 → §7.5의 4대 기능이 선택이 아닌 **필수**. 단 그 의무는 §0.4의 `restricted` 자산에만 발생한다 |
| **R2** | **"공표된 저작물의 일부분"만 이용 가능** | 교과서 그림을 단원 전량 크롭하면 일부분을 넘어선다. **단원당 크롭 상한**을 규칙 상수로 두고 초과 시 차단 (§4.3 C7) |
| **R3** | **저작물의 접근 대상은 "수업을 받는 자"** | §0.4에 따라 **`restricted` 자산에만** 적용. 초대 코드 가입, 학기 종료 시 만료, 서명 URL, 검색 색인 차단. `media_assets.access_tier`와 RLS로 강제. 자체 저작물(카드 본문·노선 코스)에는 걸지 않는다 |
| **R4** | **개념 카드는 재서술이어야 한다** | 교과서 문장을 그대로 옮기면 개념 카드 자체가 복제물이 된다. **원문 n-gram 중복 검사**를 자동 게이트로 (§4.3 C4) |
| **R5** | **교육용이므로 과학 오류 0건이 하드 요구사항** | 관계 명제는 스크립트로 참·거짓을 판정할 수 없다 → **사람 게이트 우회 불가** |
| **R6** | **한글 화학 표기가 두 갈래** (나트륨/소듐, 부탄/뷰테인) | 현행 표기를 표제어로 고정, 구표기는 `aliases`로 검색만 허용 — SciMetro 표기 정책 승계 |
| **R7** | **한자어가 없는 용어가 많다** | 이온·에너지·엔트로피 등 음차어. `hanja` 필드는 **nullable**이며 빈 값과 "해당 없음"을 구분해 기록 |
| **R8** | **타이핑 불가 문자** (H₂O, Na⁺, α, →) | 단답형 정답은 평문 정규화. SciMetro `typeability-check` 스킬 재사용 |
| **R9** | **개념 동음이의** — "주기"(주기율표 / 진동), "핵"(원자핵 / 세포핵), "전위"(전기 / 지형) | 링크는 표기 일치가 아니라 **개념 키 일치**로만 건다. 애매하면 사람 확인 (§5 G2) |
| **R10** | **교과서·기출 PDF 품질 편차** | 텍스트 레이어 유무를 먼저 판별하고 없으면 OCR 경로. 판별 실패는 스킵이 아니라 에스컬레이션 |
| **R11** | **PDF 라이브러리 라이선스** | PyMuPDF는 **AGPL/상용 이중 라이선스**. 산출물이 서비스로 나가므로 기본 도구는 **pypdfium2(Apache-2.0) + pdfplumber(MIT)** 로 고정 (§6.2) |
| **R12** | **1인 개발·무료 운영** | 오픈소스·공공데이터만 사용. 유료 API 도입은 별도 결정 |
| **R13** | **학생 개인정보 최소 수집** | 실명 대신 학번 별칭. 학습 기록은 개인 식별 최소 단위로만 저장 |

### 1.6 용어 정의

| 용어 | 정의 |
|------|------|
| **개념 카드(Concept Card)** | 배포 단위. 하나의 개념에 대한 정의·표기·관계 명제·오개념·링크·시각자료 묶음 |
| **개념 키(concept_key)** | 표기와 무관한 개념 식별자 (`concept:vapor-pressure`). 학년·과목이 달라도 같은 개념이면 같은 키 |
| **관계 명제(Relation)** | 조건·선행량·방향·후행량으로 형식화된 문장. 카드의 핵심 필드이자 문항 생성의 원천 |
| **성립 범위(scope)** | 관계 명제가 참인 조건 경계. 이것이 빠지면 명제가 오개념이 된다 |
| **반전 가능(invertible)** | 관계 명제의 후행 방향을 뒤집으면 반드시 거짓이 되는가. OX 오답 지문 자동 생성의 가부를 결정 |
| **개념 링크(Link)** | 두 개념 카드 사이의 방향성 있는 관계. `prereq` / `next` / `same` / `related` 4종 |
| **심화 체인(same-chain)** | 같은 개념 키가 학년·과목을 넘어 반복될 때 생기는 사슬 (통합과학1 주기성 → 화학 주기적 성질) |
| **문항(Item)** | 기출 1문제. 원문 이미지 + 메타 + 자체 해설 |
| **데일리 문항(Quiz)** | 카드에서 파생된 짧은 인출 문항. OX / 단답 / 선택형 3종 |
| **권리 대장(Rights Ledger)** | 모든 외부 저작물 자산의 출처·이용 근거·게재 조건 기록. 감사 추적용 |
| **검토 게이트** | 교사 승인 없이는 진행 불가한 지점 |

---

## 2. 콘텐츠 설계 (도메인 매핑)

에이전트가 무엇을 만드는지 확정해야 워크플로우가 정의되므로 먼저 고정한다.

### 2.1 개념 카드 스키마 (개요)

```jsonc
{
  "id": "chem-vapor-pressure",
  "concept_key": "concept:vapor-pressure",
  "term": "증기 압력",
  "aliases": ["증기압"],                    // 검색 허용, 표제어 아님
  "notation": {
    "hanja": "蒸氣壓力",                    // R7: null 허용, "해당 없음"과 구분
    "hanja_gloss": "蒸 찔 증 · 氣 기운 기 · 壓 누를 압 · 力 힘 력",
    "english": "vapor pressure",
    "symbol_display": "P_vap",             // 화면 표시용
    "symbol_plain": "Pvap"                 // 입력·검색용 (R8)
  },
  "definition": "일정 온도에서 액체와 그 증기가 동적 평형을 이룰 때 증기가 나타내는 압력",
  "definition_source": "restated",         // 원문 복제 아님을 명시 (R4)

  "relations": [
    {
      "id": "rel-vp-01",
      "text": "같은 온도에서 증기 압력이 크면 분자 간 상호작용이 작다",
      "form": {
        "condition": "온도 일정",
        "antecedent": { "quantity": "증기 압력", "direction": "증가" },
        "consequent": { "quantity": "분자 간 상호작용", "direction": "감소" },
        "type": "inverse",                 // direct | inverse | conditional | causal | classification
        "scope": "순수 액체"
      },
      "invertible": true,                  // 방향 반전 시 반드시 거짓 → OX 자동 생성 가능
      "curriculum": ["12화학02-03"],
      "verified_by": "teacher"             // ★사람 게이트 통과 표식
    }
  ],

  "misconceptions": [
    {
      "text": "끓는점이 높은 물질일수록 같은 온도에서 증기 압력도 높다",
      "why_wrong": "끓는점이 높다는 것은 분자 간 상호작용이 크다는 뜻이므로 증기 압력은 오히려 낮다",
      "linked_relation": "rel-vp-01",
      "source": "teacher"                  // teacher | log (오답 로그에서 발견)
    }
  ],

  "links": [
    { "type": "prereq", "target": "chem-dynamic-equilibrium", "note": "동적 평형" },
    { "type": "same",   "target": "isci1-state-change", "grade_from": "통합과학1" },
    { "type": "next",   "target": "chem-raoult-law" },
    { "type": "related","target": "chem-intermolecular-force" }
  ],

  "media": [
    { "asset_id": "a3f19c…", "kind": "graph", "caption": "온도–증기 압력 곡선",
      "rights": "textbook-crop", "source": "천재 화학 p.148 그림 3-12" }
  ],

  "curriculum": ["12화학02-03"],
  "subject": "화학",
  "unit": "물질의 구조와 성질 > 분자 간 상호작용",
  "difficulty": 3,
  "version": 1
}
```

**필드 4종이 각각 무엇을 위해 존재하는지**가 설계의 핵심이다.

| 필드 | 학습 기능 | 파이프라인 기능 |
|------|----------|----------------|
| `notation` (한자·영어) | 낱말 자체의 이해. "蒸氣壓力"을 풀면 정의가 절반 나온다 | 단답형 문항의 **문제 방향 다변화** (한자→한글, 영어→한글) |
| `relations` | 시험에서 실제로 평가되는 지식 | **OX·선택형 문항의 원천** (§2.4) |
| `misconceptions` | 함정 회피 | **오답 지문의 원천** — LLM이 지어낸 오답보다 정확도가 높다 |
| `links` | 과목·학년 간 이동 | **선택형 오답 선택지 후보**(형제 개념) + 개념 지도 |

### 2.2 개념 링크 4종

| 유형 | 의미 | 예시 | 방향 |
|------|------|------|------|
| `prereq` | 이것을 알아야 이 개념을 이해한다 | 동적 평형 → 증기 압력 | 단방향 |
| `next` | 이 개념을 발판으로 나아간다 | 증기 압력 → 라울 법칙 | 단방향 (`prereq`의 역) |
| `same` | **같은 개념의 학년/과목 간 재등장** | 통합과학1 상태 변화 ↔ 화학 증기 압력 | 양방향, `grade_from` 표기 |
| `related` | 함께 보면 이해가 깊어진다 | 증기 압력 ↔ 분자 간 힘 | 양방향 |

**`same`이 이 앱의 존재 이유다.** 사용자가 요청한 "화학에서 나오는 단어인데 통합과학에서 배웠으면 연동"이 바로 이 링크다. 구현상 중요한 점은, `same` 링크는 **표기 일치로 걸면 안 된다**는 것이다(R9). 반드시 `concept_key` 일치를 근거로 하고, 신규 키 확정은 사람이 한다(§5 G2·G3).

### 2.3 문항(기출) 스키마 (개요)

```jsonc
{
  "id": "item-2025-09-mock-ch-14",
  "exam": { "year": 2025, "month": 9, "type": "평가원 모의평가", "subject": "화학I", "no": 14 },
  "media": { "asset_id": "…", "kind": "item-scan" },   // 문항 원문 이미지 (크롭)
  "answer": 3,
  "explanation": "…",                    // ★자체 작성 (원 해설 복제 아님)
  "concepts": ["chem-vapor-pressure", "chem-intermolecular-force"],
  "unit": "물질의 구조와 성질",
  "curriculum": ["12화학02-03"],
  "difficulty_official": null,           // 공개된 정답률이 있으면 기입
  "rights": {
    "holder": "한국교육과정평가원",
    "basis": "저작권법 제25조 제3항 수업목적 이용",
    "display": ["출처 표기", "워터마크", "서명 URL", "다운로드 차단"]
  },
  "version": 1
}
```

**해설은 반드시 자체 작성한다.** 시중 해설을 옮기면 R1의 근거가 닿지 않는 별개의 저작물 복제가 된다. 해설 작성은 `item-curator`가 개념 카드의 관계 명제를 인용해 쓰며, 이 방식은 부수 효과로 **문항과 개념 카드가 서로를 참조하는 구조**를 만든다.

### 2.4 데일리 문항 생성 규칙 ★핵심 설계★

문항을 매번 LLM으로 생성하지 않는다. **카드 필드에서 결정론적으로 파생**시키고 LLM은 검토만 한다. 비용·중복·오류가 동시에 줄어든다.

| 문항 유형 | 생성 규칙 | 정답 | 오답 |
|-----------|----------|------|------|
| **OX (참)** | `relation.text` 그대로 | O | — |
| **OX (거짓·방향 반전)** | `invertible: true`인 명제의 `consequent.direction` 반전 | X | 스크립트 생성 |
| **OX (거짓·조건 탈락)** | `condition` 또는 `scope`를 제거해 과일반화 | X | 스크립트 생성 |
| **OX (거짓·오개념)** | `misconceptions[].text` 그대로 | X | 교사 입력 |
| **단답형** | `definition` → `term` / `hanja` → `term` / `english` → `term` 3방향 | 정규화된 `term` + `aliases` | — |
| **선택형** | 발문은 관계 명제, 선택지는 정답 개념 1 + **같은 단원 형제 개념 3** | 그래프에서 결정론적 추출 | 링크 거리 1~2인 개념 |

**`invertible` 플래그가 안전장치다.** `type`이 `classification`이거나(예: "산은 수용액에서 H⁺를 내놓는다") `conditional`인 명제는 방향을 뒤집어도 거짓이 안 되거나 무의미해진다. 스크립트는 `invertible: true`인 것만 자동 반전하고, 나머지 유형의 거짓 지문은 `quiz-reviewer`가 작성한 뒤 사람 검토로 넘긴다(§5 D2).

**선택형의 오답 선택지를 그래프에서 뽑는 이유**는, 무작위 개념을 넣으면 학생이 소거법으로 맞히기 때문이다. 링크 거리 1~2의 형제 개념은 실제로 헷갈리는 후보이므로 변별력이 생기고, 동시에 오답을 고른 기록이 "이 두 개념을 혼동한다"는 진단 신호가 된다.

### 2.5 적응형 출제 (런타임)

| 항목 | 결정 |
|------|------|
| 알고리즘 | **FSRS** (`py-fsrs`, **MIT 라이선스**) — SM-2 후속. 보유율 목표 기본 90% |
| 상태 저장 단위 | **개념 카드 단위** (문항 단위 아님) — 같은 개념을 다른 문항으로 물어도 기억 상태는 이어져야 한다 |
| 등급 매핑 | 정답+빠름 → Easy / 정답 → Good / 정답+느림 → Hard / 오답 → Again |
| 하루 세트 | 복습 도래분 우선, 부족분을 신규 카드로 채움. 기본 10문항 |
| 저장 위치 | Supabase `study_states` — 기기를 바꿔도 이어지려면 서버여야 한다 |

이 부분은 **에이전트가 아니라 앱 런타임**이 수행한다. 파이프라인의 책임은 문항 풀을 채워두는 것까지다(§5 D1~D3).

---

## 3. 워크플로우 정의 — 네 개의 파이프라인

```
[A] 개념 파이프라인 (단원 1개 단위, 반복 실행)          C0 ─ C10
[B] 문항 파이프라인 (기출 회차 1개 단위)                Q0 ─ Q6
[C] 그래프 파이프라인 (카드가 쌓인 뒤 주기적)            G1 ─ G3
[D] 퀴즈 파이프라인 (카드 승인 직후 자동 트리거)         D1 ─ D3
```

```
        /inbox/textbook/*.pdf                    /inbox/exam/*.pdf
                 │                                       │
                 ▼                                       ▼
        ┌────────────────┐                      ┌────────────────┐
        │ [A] 개념        │                      │ [B] 문항        │
        │ C0 전처리       │                      │ Q0 전처리       │
        │ C1 개념 추출    │                      │ Q1 문항 분할    │
        │ C2 카드 작성    │                      │ Q2 텍스트·정답  │
        │ C3 용어 대조    │                      │ Q3 개념 매핑 ───┼──┐
        │ C4 스키마·원문  │                      │ Q4 해설 작성    │  │
        │ C5 범위 태깅    │                      │ Q5 권리 검사    │  │
        │ C6 명제 자기검증│                      │ Q6 ★게이트→배포│  │
        │ C7 시각자료     │                      └────────────────┘  │
        │ C8 ★사람 게이트│                                           │
        │ C9 배포 ────────┼──────┬────────────────────────────────────┘
        │ C10 로그 튜닝   │      │        (문항은 개념 카드를 참조)
        └────────────────┘      │
                                ├──▶ [D] 퀴즈  D1 파생 → D2 검토 → D3 배포
                                └──▶ [C] 그래프 G1 탐지 → G2 판정 → G3 맵+★사전 승인
```

### 3.1 LLM 판단 영역 vs 코드 처리 영역

| 에이전트가 직접 판단 | 스크립트가 처리 |
|---------------------|----------------|
| 단원에서 무엇이 "핵심 개념"인가, 몇 개로 끊을 것인가 | PDF 텍스트 레이어 판별, OCR 실행, 페이지·단원 분할 |
| 관계 명제를 어떤 조건·방향으로 형식화할 것인가 | 그림 영역 검출, 크롭, 리사이즈, 워터마크 |
| `invertible` 판정 (뒤집으면 정말 거짓인가) | 한자어·영어 표기 사전 대조 조회 |
| 정의를 교과서와 다른 문장으로 재서술 | **원문 n-gram 중복률 계산** |
| 오개념이 실제로 학생이 빠지는 함정인가 | JSON 스키마 검증, 링크 참조 무결성, 순환 참조 검사 |
| 교육과정 범위 이탈 판단 (경계 사례) | 정답 문자열 정규화·중복 검사 (typeability) |
| **개념 링크 유효성** (같은 개념인가, 동음이의인가) | **링크 후보 탐지** (키 충돌·문자열 유사도 스캔) |
| 기출 문항이 어느 개념에 걸리는가 | 문항 번호 경계 검출, 정답표 파싱 |
| 해설 서술, 오답 선택지 타당성 | **OX·단답·선택형 문항 템플릿 파생** |
| 학습 로그 해석 — 왜 이 카드에서 막히는가 | 로그 집계, 권리 대장 기록, 버전 발급, 파일 I/O |

**설계 의도**: 과학적 참·거짓 판정은 LLM에게도, 스크립트에게도 최종 권한을 주지 않는다. 스크립트는 **형식**(표기·스키마·중복·범위)만 잡고, LLM은 **초안과 정성 판단**만 하며, **명제의 진위는 반드시 교사가 본다**(R5). 대신 교사가 보는 양을 줄이기 위해, 자동 검증이 잡을 수 있는 것은 게이트 이전에 전부 털어낸다.

---

## 4. [A] 개념 파이프라인 상세

### 4.1 실행 단위

**단원 1개**(예: `화학 > 물질의 구조와 성질 > 분자 간 상호작용`)를 한 사이클로 돈다. 교과서 전체를 한 번에 돌리지 않는 이유는, 교사 게이트가 한 번에 감당할 수 있는 분량이 20~30장 카드이기 때문이다.

### 4.2 단계 요약

| 단계 | 담당 | 산출물 |
|------|------|--------|
| C0 교과서 전처리 | 코드 | `/output/source/<unit-id>/` |
| C1 개념 추출 | LLM · `textbook-analyst` | `<unit-id>.candidates.json` |
| C2 카드 작성 | LLM · `concept-writer` | `<concept-id>.draft.json` |
| C3 용어 대조 검증 | 코드 | `report.json` (notation 절) |
| C4 스키마·원문 유사도 검증 | 코드 | `report.json` (form 절) |
| C5 교육과정 범위 태깅 | LLM · `fact-validator` | `report.json` (curriculum 절) |
| C6 관계 명제 자기 검증 | LLM · `fact-validator` | `report.json` (relation 절) |
| C7 시각자료 부착·권리 태깅 | 코드 (+LLM 선택) | `/output/media/`, `rights/ledger.jsonl` |
| C8 검토 패키지 → **★사람 게이트★** | 코드 + 사람 | `<unit-id>.review.md` |
| C9 배포 | 코드 | `manifest.json`, Supabase diff |
| C10 로그 → 튜닝 | 코드 + LLM | `<unit-id>.tuning.md` |

### 4.3 단계별 정의

---

#### C0. 교과서 전처리 — [코드]

- **입력**: `/inbox/textbook/*.pdf` + `unit_backlog.yaml`의 대상 단원
- **처리**
  - 텍스트 레이어 유무 판별 → 없으면 OCR 경로(`ocrmypdf` 또는 Tesseract)로 분기 (R10)
  - 목차·헤더 기준 **단원 경계 검출** → 페이지 범위 확정
  - 본문 텍스트 + 페이지별 레이아웃 박스 + 그림 후보 영역 좌표 추출
  - 원문 텍스트는 `/output/source/<unit-id>/text.jsonl`에 **대조용으로만** 보관 (C4의 n-gram 비교 기준. 배포 대상 아님)
- **출력**: `/output/source/<unit-id>/{text.jsonl, layout.json, figures.json}`
- **성공 기준**: 단원 페이지 범위 확정 + 본문 텍스트 추출률 ≥ 95% (문자 수 기준)
- **검증 방법**: 규칙 기반 (추출률 임계치, 페이지 연속성)
- **실패 시**: 추출률 미달 → OCR 재시도 1회 → 여전히 미달이면 **에스컬레이션**(스캔 품질 문제는 자동 복구 불가). 단원 경계 판별 실패 → **에스컬레이션**

---

#### C1. 단원 개념 추출 — [LLM · textbook-analyst]

- **입력**: `text.jsonl` 경로 + 성취기준 references + 기존 개념 목록(중복 회피) + `concept_registry.yaml`
- **처리**
  - 개념 후보 목록과 **단원 내 위계**(선수 → 후속) 초안 작성
  - 각 후보에 잠정 `concept_key` 부여 — 기존 사전에 있으면 **재사용**(이것이 `same` 링크의 씨앗)
  - 카드로 만들 것과 만들지 않을 것 구분 (예: "실험 유의사항"은 개념 아님)
- **출력**: `/output/concepts/<unit-id>.candidates.json`
- **성공 기준**
  - 단원당 개념 수 10~30
  - 성취기준 1개당 개념 ≥ 1개 (누락 단원 없음)
  - 기존 카드와 표제어 중복 0건 (중복이면 `same` 후보로 표시)
  - 위계 그래프에 순환 없음
- **검증 방법**: 규칙 기반 (개수·중복·순환 검사)
- **실패 시**: **자동 재시도** 최대 2회(사유 첨부) → 초과 시 **에스컬레이션**

---

#### C2. 개념 카드 작성 — [LLM · concept-writer]

- **입력**: `candidates.json` 경로 + 해당 단원 원문 경로 + 표기 정책 + `misconceptions.yaml`
- **처리**: 개념별로 §2.1의 전 필드를 채운다. 작성 규칙:
  - `definition`은 **원문을 보고 다시 쓴다**. 표현을 바꾸는 것이 아니라 **구조를 바꾼다**(정의항의 순서 변경, 조건 명시화) — R4
  - `relations`는 최소 1개, 권장 2~4개. 각각 `condition`·`scope`를 **반드시** 채운다. 비어 있으면 C6에서 반려된다
  - `invertible`은 작성자가 판정하되 **근거 한 줄**을 남긴다
  - `misconceptions`는 `misconceptions.yaml`에 해당 개념이 있으면 우선 사용하고, 없으면 생성하되 `source: "llm"`으로 표시(게이트에서 우선 검토 대상)
  - `notation.hanja`가 없는 경우 `null`이 아니라 `"해당 없음"` + `hanja_note`에 사유 (R7)
- **출력**: `/output/concepts/<concept-id>.draft.json`
- **성공 기준**: 필수 필드 완비, 관계 명제 ≥ 1개, 모든 명제에 `condition`·`scope` 존재, 오개념 ≥ 1개
- **검증 방법**: 스키마 검증 + 규칙 기반
- **실패 시**: **자동 재시도** 최대 2회 (누락 카드·누락 필드만 부분 재생성)

---

#### C3. 용어 대조 검증 — [코드]

| 검사 | 대상 | 소스 | 판정 |
|------|------|------|------|
| 한자어 대조 | `notation.hanja` | 표준국어대사전 오픈API | 표제어 한자 표기와 일치 |
| 한자 훈음 | `hanja_gloss` | 한자 사전 로컬 데이터 | 글자 수 = 훈음 항목 수 |
| 영어 대조 | `notation.english` | **대한화학회 화학술어**, 편수용어 references | 등재 용어와 일치 |
| 현행 표기 | `term` | 표기 사전 (SciMetro 승계) | 구표기면 fail, `aliases`로 이동 |
| 기호 입력 가능성 | `symbol_plain` | `typeability-check` | 평문 변환 가능 |

- **출력**: `/output/validation/<unit-id>.report.json`의 `notation` 절 (카드별 pass/fail/warn + 사유)
- **성공 기준**: **fail 0건**. 사전 미등재는 warn
- **검증 방법**: 스크립트 (결정론적)
- **실패 시**
  - fail ≤ 3건 → 해당 카드만 **자동 재시도**(C2 부분 재생성, 최대 2회)
  - fail > 3건 → **에스컬레이션**
  - 외부 API 조회 실패 → **스킵 + 로그**. 단 **단원 내 스킵률 30% 초과 시 에스컬레이션**

---

#### C4. 스키마·원문 유사도·입력 가능성 검증 — [코드]

- **처리**
  - JSON 스키마 검증, 필수 필드 완비
  - **원문 n-gram 중복 검사** (R4) — `definition`·`relations[].text`를 `text.jsonl`과 대조. **연속 12어절 이상 일치 시 fail**, 8~11어절 일치는 warn
  - `links[].target`이 실재하는 카드인지 참조 무결성 검사
  - 링크 그래프 **순환 참조 검사** (`prereq` 사이클 금지)
  - 단답형 정답이 될 `term`의 평문 정규화·중복 검사 (같은 단원 내 정답 충돌 = 출제 버그)
- **성공 기준**: 스키마 통과, 12어절 이상 일치 0건, 참조 깨짐 0건, 순환 0건, 정답 중복 0건
- **검증 방법**: 스크립트
- **실패 시**
  - 원문 중복 → 해당 필드만 **C2 재작성 요청**(최대 2회). 3회째는 **에스컬레이션**(그 개념은 정의가 하나뿐일 수 있음 → 교사가 인용 처리 판단)
  - 참조 깨짐·정규화 문제 → **자동 수정 후 재검증**
  - 순환 참조 → **에스컬레이션** (위계 설계 오류)

---

#### C5. 교육과정 범위 태깅 — [LLM · fact-validator]

- **처리**: 카드별 성취기준 코드 태깅, 범위 이탈 판단, 선수 과목 표시
  - 화학 계열 코드 체계: `10통과1-02-03`(원소의 주기성), `10통과1-02-04`(화학 결합), `10통과2-01-03`(산화·환원), `10통과2-01-04`(산·염기 중화), `12화학01~04`(화학의 언어 / 물질의 구조와 성질 / 화학 평형 / 역동적인 화학 반응)
  - 진로선택(`물질과 에너지`, `화학 반응의 세계`) 코드는 references에서 확정 후 병행 태깅
- **성공 기준**: 전 카드 성취기준 ≥ 1개, 범위 이탈 0건
- **검증 방법**: **LLM 자기 검증** + 태깅 필드 존재 검사(규칙 기반)
- **실패 시**: 이탈 ≤ 2건 → 카드 **보류 후 진행** + 로그 / 이탈 > 2건 → **C1 복귀** / 판단 유보 → **C8 검토 항목에 병합**

---

#### C6. 관계 명제 자기 검증 — [LLM · fact-validator]

C4가 형식을, C5가 범위를 봤다면 C6은 **명제의 구조적 결함**을 본다. 진위 판정이 아니라 **결함 탐지**임에 유의한다.

- **검사 항목**
  1. `scope` 누락·과도한 일반화 ("항상", "모든"이 붙었는데 예외가 있는가)
  2. `invertible` 판정의 타당성 — `classification`·`conditional`형이 `true`로 표시돼 있지 않은가
  3. 명제와 `definition`의 **동어반복** 여부 (정의를 문장으로 바꾼 것에 불과한 명제는 학습 가치 없음)
  4. `misconceptions`가 실제로 해당 명제의 반대편에 있는가 (`linked_relation` 정합성)
  5. 같은 카드 내 명제 간 **모순**
- **성공 기준**: 결함 지적 0건, 또는 지적된 항목이 전부 수정 완료
- **검증 방법**: **LLM 자기 검증** (지적 시 명제 ID와 근거 필수)
- **실패 시**: 지적 항목 → **C2 부분 재작업**, 최대 2회 → 초과 시 **에스컬레이션**
- **주의**: **이 단계 통과가 과학적 참을 의미하지 않는다.** `verified_by` 필드는 C8 통과 시에만 `teacher`로 기록된다

---

#### C7. 시각자료 부착·권리 태깅 — [코드 + LLM 선택]

- **처리**
  - `figures.json`의 그림 후보 중 카드에 붙일 것을 선택 (LLM 판단: 어느 그림이 어느 개념의 것인가)
  - 선택된 영역을 **크롭 → 리사이즈 → 워터마크(학교명·수업목적 이용 문구)** (코드)
  - **권리 메타 필수 기입** — 출처(교과서명·페이지·그림번호), 권리자, 이용 근거, 게재 조건
  - **`access_tier` 결정론적 도출** — `rights.holder`가 교사 자신이 아니면 무조건 `restricted` (§0.4). 판단이 아니라 규칙이다
  - `rights/ledger.jsonl`에 1행 기록
  - **R2 상한 검사**: 단원당 크롭 수 ≤ 상한(§4.4). 초과분은 자동 제외 후 로그
- **성공 기준**: 전 자산에 권리 메타 완비, **`access_tier` 도출 규칙 위반 0건**, 단원 크롭 상한 준수, 원본 해상도 대비 축소 적용
- **검증 방법**: 규칙 기반 (메타 필드 존재, `holder`↔`tier` 정합성, 개수 상한, 이미지 크기)
- **실패 시**: 메타 누락 → **자산 배포 제외 + 로그** (권리 미상 자산은 절대 배포하지 않는다). 상한 초과 → **스킵 + 로그**

---

#### C8. 검토 패키지 생성 → ★사람 게이트★

교사가 **단원 1개를 20분 안에** 판단할 수 있는 md를 만든다.

- **패키지 구성**
  - 단원 요약 (과목, 성취기준 커버리지, 카드 수, 신규 `same` 링크 수)
  - **카드별 표** — 표제어 / 한자 / 영어 / 관계 명제 수 / 오개념 수 / 링크 수 / 난이도
  - **★관계 명제 전문 목록** — 이것이 검토의 중심. 명제마다 승인 체크박스
  - **자동 검증 요약** — C3~C7 통과 내역, warn 목록, 스킵 내역
  - **★특히 봐야 할 지점** (자동 검증이 원리적으로 못 잡는 것만)
    - `source: "llm"`인 오개념 전부
    - `invertible: true`로 판정된 명제 전부 (오답 지문으로 자동 변환되므로 틀리면 학생에게 오개념을 심는다)
    - 원문 중복 8~11어절 warn 항목
    - 사전 미등재 한자어·영어
    - 신규 `concept_key` 제안
    - C5 판단 유보 항목
  - 승인/반려 기록 방법
- **성공 기준**: 교사가 `approved: true` 기록 + 승인된 명제에 `verified_by: teacher` 반영
- **검증 방법**: **사람 검토**
- **실패 시**: 반려 사유를 LLM이 해석해 C1(구성) 또는 C2(문구) 복귀. 모호하면 되묻는다
- **주의**: **자동 통과 옵션을 두지 않는다** (R5). 명제 단위 부분 승인은 허용 — 미승인 명제는 카드에 남되 `quiz_ready: false`로 데일리 문항에서 제외

---

#### C9. 배포 — [코드]

- **처리**: 카드 버전 발급, `/output/concepts/<id>@<v>.json` 확정, 매니페스트 갱신, Supabase upsert diff 산출, 미디어를 **비공개 버킷**에 업로드, **D1·G1 트리거 발행**
- **성공 기준**: 매니페스트 스키마 통과, 기존 버전 미덮어씀, **`access_tier` 오분류 0건**, `restricted` 자산의 공개 URL 노출 0건
- **검증 방법**: 스키마 검증 + 해시 비교 + **`rights.holder` ↔ `access_tier` 정합성 재검사** + 버킷 공개 설정 점검
- **실패 시**: **에스컬레이션** (배포 자동 재시도 금지)

---

#### C10. 학습 로그 → 튜닝 제안 — [코드 집계 + LLM 해석]

- **트리거**: 카드별 누적 응답 N회(기본 50) 도달, 또는 수동
- **집계(코드)**: 카드별 정답률, 첫 시도 오답률, `Again` 비율, 평균 응답 시간, **오답 시 고른 선택지 분포**, `same` 링크 이동률
- **해석(LLM)**: "증기 압력 카드의 선택형에서 78%가 '끓는점'을 골랐다 → `misconceptions`에 이미 있는 항목이 실제로 지배적. 이 오개념을 OX 필수 항목으로 승격 권장"
- **출력**: `/output/insights/<unit-id>.tuning.md` + `misconceptions.yaml` 갱신 제안(`source: "log"`)
- **성공 기준**: 데이터 근거가 붙은 실행 가능한 제안 3개 이상
- **검증 방법**: 규칙 기반(제안 수) + LLM 자기 검증(근거 유무)
- **실패 시**: 표본 부족 → **스킵 + 로그**

### 4.4 규칙 상수

| 항목 | 값 | 근거 |
|------|-----|------|
| 단원당 개념 카드 수 | 10~30 | 교사 게이트 1회 20분 |
| 카드당 관계 명제 | 1~4 | 4개 초과 시 카드가 요약본이 됨 |
| 카드당 오개념 | 1~3 | |
| 카드당 링크 | 2~6 | 2 미만은 고립 카드 |
| 정의 길이 | 40~120자 | |
| 원문 연속 일치 상한 | **11어절** (12어절부터 fail) | R4 |
| 단원당 교과서 그림 크롭 | **≤ 8장** | R2 "일부분" 요건 |
| 회차당 기출 문항 크롭 | ≤ 20문항 | R2 |
| 데일리 세트 | 10문항 (복습 우선, 부족분 신규) | |
| 단답형 정답 최대 길이 | 12자 | SciMetro 승계 |

---

## 5. [B]·[C]·[D] 파이프라인 상세

### 5.1 [B] 문항 파이프라인

---

#### Q0. 기출 전처리 — [코드]

- **입력**: `/inbox/exam/*.pdf` (문항지 + 정답표)
- **처리**: 텍스트 레이어 판별, 회차 메타(연도·월·시행기관·과목) 추출, 페이지 정규화
- **성공 기준**: 회차 메타 4필드 확정, 페이지 수 = 예상 문항 수 범위 내
- **검증 방법**: 스키마 검증
- **실패 시**: 메타 판별 실패 → **에스컬레이션**

---

#### Q1. 문항 단위 분할·크롭 — [코드]

- **처리**: 문항 번호 패턴(`1.`~`20.`)과 여백 기준으로 문항 경계 검출 → 영역 크롭 → 워터마크 → 비공개 저장
- **성공 기준**: 검출 문항 수 = 정답표 문항 수, 크롭 영역이 인접 문항을 침범하지 않음(겹침 검사)
- **검증 방법**: 규칙 기반 (개수 일치, 영역 겹침률)
- **실패 시**: 개수 불일치 ≤ 2건 → 해당 문항만 **에스컬레이션**(수동 좌표 지정). > 2건 → 회차 전체 **에스컬레이션**

---

#### Q2. 문항 텍스트·정답 추출 — [코드]

- **처리**: 크롭 영역의 텍스트 추출(개념 매핑용 내부 데이터), 정답표 파싱, 문항–정답 결합
- **성공 기준**: 전 문항 정답 결합, 텍스트 추출률 ≥ 90%
- **검증 방법**: 규칙 기반
- **실패 시**: 정답 결합 실패 → **에스컬레이션**. 텍스트 추출 미달 → **warn 후 진행**(Q3에서 이미지 판단으로 보완)
- **주의**: 추출된 문항 텍스트는 **매핑용 내부 데이터**이며 배포하지 않는다. 학생에게 보이는 것은 크롭 이미지뿐이다

---

#### Q3. 개념 매핑·단원 분류 — [LLM · item-curator]

- **처리**: 문항이 묻는 개념을 **기존 카드 ID로** 연결. 해당 카드가 없으면 `unmapped`로 표시하고 개념 백로그에 제안
- **성공 기준**: 전 문항에 개념 ≥ 1개 또는 `unmapped` 사유 기재, `unmapped` 비율 ≤ 25%
- **검증 방법**: **LLM 자기 검증**(근거 필수) + 규칙 기반(참조 무결성)
- **실패 시**: `unmapped` 25% 초과 → **스킵 + 로그**(해당 단원 카드가 아직 없다는 뜻 → 개념 파이프라인 선행 필요). 개별 `unmapped`는 보류 후 다음 사이클 재매핑

---

#### Q4. 해설 작성 — [LLM · item-curator]

- **처리**: 연결된 카드의 관계 명제를 근거로 **자체 해설** 작성. 각 해설은 인용한 명제 ID를 명시
- **성공 기준**: 전 문항 해설 존재, 인용 명제 ID ≥ 1개, **원 해설·시중 해설과의 n-gram 대조는 불가하므로 "명제 인용 구조" 자체를 형식 요건으로 강제**
- **검증 방법**: 규칙 기반(명제 ID 존재) + **사람 검토**(Q6)
- **실패 시**: **자동 재시도** 2회 → 초과 시 해당 문항 `explanation: null`로 두고 배포(해설 없는 문항으로 노출) + 로그

---

#### Q5. 권리 메타·게재 조건 검사 — [코드 · rights-tagger]

- **처리**: 문항 자산마다 출처·권리자·이용 근거·게재 조건 4필드 검사, `rights/ledger.jsonl` 기록, 워터마크 존재 확인, 회차당 크롭 상한 검사
- **성공 기준**: 4필드 완비 100%, 워터마크 100%, 상한 준수
- **검증 방법**: 규칙 기반
- **실패 시**: 미비 자산 → **배포 제외 + 로그** (재시도 없음)

---

#### Q6. 검토 → ★사람 게이트★ → 배포

- **검토 대상**: 개념 매핑 정확성, 해설 내용, `unmapped` 목록, 권리 대장 요약
- **성공 기준**: 교사가 `approved: true` 기록
- **검증 방법**: **사람 검토** (자동 통과 옵션 없음 — R1·R5)
- **실패 시**: 반려 사유 해석 후 Q3 또는 Q4 복귀
- **배포**: 카드와 동일한 버전·매니페스트 규칙

---

### 5.2 [C] 그래프 파이프라인

#### G1. 링크 후보 탐지 — [코드]

- **처리**: 전체 카드 스캔 → ① `concept_key` 완전 일치(강한 후보, `same`) ② `term`/`aliases` 일치(중간) ③ 문자열 유사도 임계 이상(약한 후보) ④ 카드 내 명시 링크의 역방향 누락 검사
- **출력**: `/output/graph/link_candidates.json` (후보별 강도·근거)
- **성공 기준**: 전 카드 스캔 완료, 후보마다 근거 유형 기재
- **검증 방법**: 규칙 기반 (스캔 커버리지, 근거 필드 존재)
- **실패 시**: **자동 재시도** 1회 → 초과 시 에스컬레이션

#### G2. 링크 유효성 판단 — [LLM · graph-curator]

- **처리**: 후보별로 `valid`(유형 지정) / `invalid`(동음이의) / `uncertain` 판정 + 근거 문장. R9의 동음이의 목록을 우선 대조
- **성공 기준**: 전 후보 판정 + 근거, `uncertain` ≤ 20%
- **검증 방법**: **LLM 자기 검증** + 규칙 기반(판정 누락)
- **실패 시**: `uncertain` 20% 초과 → **에스컬레이션**(일괄 확인). 개별 `uncertain`은 **보류 + 로그**
- **주의**: **`valid` 판정만으로 자동 반영하지 않는다.** 신규 `concept_key` 등록은 사람 승인 사항

#### G3. 그래프 생성 + 개념 사전 갱신 제안 — [코드 + 사람]

- **처리**: `valid` 링크로 그래프 구성, `prereq` 순환 검사, **허브 개념**(연결 5+) 식별, 고립 카드 탐지, 앱 개념 지도용 배치 좌표 초안, `concept_registry.yaml` 갱신 제안
- **출력**: `/output/graph/graph.json` + `registry_proposal.yaml`
- **성공 기준**: `prereq` 순환 0건, 고립 카드 ≤ 10%, `same` 체인 ≥ 5개(학년 간 연결이 실제로 생겼는가)
- **검증 방법**: 규칙 기반(그래프 검사) + **사람 검토**(사전 갱신)
- **실패 시**: 순환 발생 → **에스컬레이션**. 고립 카드 → **스킵 + 로그**(다음 단원 추가 시 재평가)

---

### 5.3 [D] 퀴즈 파이프라인

#### D1. 문항 파생 — [코드 · quiz-builder]

- **트리거**: C9 배포 직후 자동
- **처리**: §2.4 규칙으로 카드당 OX·단답·선택형 파생. 선택형 오답 선택지는 `graph.json`에서 링크 거리 1~2 개념을 추출
- **성공 기준**: 카드당 문항 ≥ 4개(OX 2 + 단답 1 + 선택형 1), 선택지 중복 0건, 정답 위치 균등 분포
- **검증 방법**: 규칙 기반
- **실패 시**: 선택지 후보 부족(그래프 미성숙) → **선택형만 스킵 + 로그**, OX·단답으로 진행

#### D2. 문항 품질 검토 — [LLM · quiz-reviewer]

- **검사 항목**
  1. 반전 지문이 **실제로 거짓인가** (`invertible` 판정 오류 최종 확인)
  2. 조건 탈락 지문이 **애매하지 않은가** (참으로도 읽히면 폐기)
  3. 단답형 정답이 **유일한가** (동의어가 정답 세트에 다 있는가)
  4. 선택형 오답이 **너무 뻔하거나 너무 헷갈리지 않은가**
  5. 문항 표현이 학생 눈높이인가
- **성공 기준**: 항목별 3점 이상(1~5점), 폐기 문항 표시 완료
- **검증 방법**: **LLM 자기 검증**
- **실패 시**: 미달 문항 **폐기 + 로그**(재생성하지 않는다 — 원천은 카드이므로 카드를 고치는 것이 옳다). 카드당 잔존 문항 < 3개면 **C2 카드 보강 요청**

#### D3. 문항 풀 배포 — [코드]

- **성공 기준**: 스키마 통과, `quiz_ready: false` 명제 파생 문항 0건(미승인 명제 유출 차단)
- **검증 방법**: 스키마 검증 + 규칙 기반(`verified_by: teacher` 역참조 검사)
- **실패 시**: **에스컬레이션** (배포 자동 재시도 금지)

---

## 6. 검증 전략

### 6.1 검증 유형 배치

| 검증 유형 | 적용 단계 | 구체 내용 |
|-----------|----------|----------|
| **스키마 검증** | C2, C4, C9, Q0, D3 | concept / item / quiz / graph / manifest JSON 스키마 |
| **규칙 기반** | C0~C4, C7, C10, Q1·Q2·Q5, G1·G3, D1 | 개수·길이·중복률·**원문 n-gram**·참조 무결성·순환·크롭 상한·권리 메타 완비 |
| **LLM 자기 검증** | C5, C6, C10, Q3, Q4, **G2**, **D2** | 범위 판단, 명제 결함 탐지, 개념 매핑, 링크 동일성, 문항 품질 |
| **사람 검토** | **C8**, **Q6**, **G3** | 관계 명제 진위(필수), 해설·매핑, 개념 사전 갱신 |

**설계 의도**: 3단 구조다. 스크립트가 **형식**을, LLM이 **정성**을, 사람이 **진위**를 본다. 어느 한 층도 다른 층의 역할을 대신하지 않는다. 특히 관계 명제는 스크립트가 원리적으로 검증할 수 없고(외부 사실 DB로 대조 가능한 SciMetro의 지질시대 순서와는 성격이 다르다), LLM 자기 검증은 그럴듯한 오류를 통과시킨다. 그래서 **C8이 유일한 진위 판정 지점**이며, 그 대신 교사가 볼 양을 자동 검증으로 최대한 줄인다.

### 6.2 도구·데이터 소스

| 용도 | 도구 / 소스 | 라이선스 | 가부 |
|------|------------|----------|------|
| PDF 텍스트·레이아웃 추출 | **pdfplumber** | MIT | ✅ |
| PDF 렌더링·영역 크롭 | **pypdfium2** | Apache-2.0 / BSD-3 | ✅ |
| ~~PDF 종합~~ | ~~PyMuPDF~~ | **AGPL / 상용 이중** — 서비스 배포 시 조건 발생 | ⚠️ **기본 도구에서 제외** (R11) |
| OCR | Tesseract (`pytesseract`), `ocrmypdf` | Apache-2.0 / MPL-2.0 | ✅ |
| 이미지 처리·워터마크 | Pillow | HPND | ✅ |
| 간격 반복 스케줄링 | **py-fsrs** | **MIT** | ✅ |
| 한자어 대조 | 표준국어대사전 오픈API | 공공 | ✅ (키 발급 필요) |
| 화학 용어 영문 대조 | 대한화학회 화학술어 | 학회 공개 | ✅ (출처 표기) |
| 화합물 명명 검증 | OPSIN, RDKit, PubChem | MIT/Apache, BSD, 퍼블릭도메인 | ✅ (SciMetro 승계) |
| 성취기준 | NCIC 원문 | 공공저작물 | ✅ |
| 그래프 연산 | networkx | BSD | ✅ |
| 스키마 검증 | jsonschema | MIT | ✅ |

> **R11 대응 결론**: PDF 처리 기본 스택은 **pdfplumber + pypdfium2**로 고정한다. PyMuPDF는 코드·문서 어디에도 등장시키지 않으며, `textbook-parser` 스킬 지침에 **금지 라이브러리 목록**으로 명시한다.

---

## 7. 구현 스펙

### 7.1 폴더 구조

```
/scimap
  ├── CLAUDE.md                          # 메인 에이전트(오케스트레이터) 지침
  ├── /.claude
  │   ├── /skills
  │   │   ├── /textbook-parser            # PDF → 단원·텍스트·레이아웃
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # detect_layer.py, split_unit.py, extract_text.py, ocr.py
  │   │   │   └── /references            # 교과서 판형별 레이아웃 노트, ⚠️금지 라이브러리 목록
  │   │   ├── /figure-cropper             # 그림 영역 크롭·워터마크
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # detect_figure.py, crop.py, watermark.py
  │   │   ├── /term-lexicon               # ★공용 — 한자어·영어 표기 대조
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # stdict.py, kcs_term.py, hanja_gloss.py
  │   │   │   └── /references            # 편수용어 발췌, 표기 정책
  │   │   ├── /originality-check          # 원문 n-gram 중복 검사
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # ngram_overlap.py
  │   │   ├── /concept-schema             # 스키마·참조 무결성·순환·정답 중복
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # validate.py, refcheck.py, cycle.py, dedupe.py
  │   │   │   └── /references            # concept/item/quiz/graph *.schema.json
  │   │   ├── /typeability-check          # ★공용 — 정답 평문 정규화·초성
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # normalize.py, check_typeable.py, chosung.py
  │   │   ├── /curriculum-mapper          # ★공용 — 성취기준 태깅 보조
  │   │   │   ├── SKILL.md
  │   │   │   └── /references            # 2022개정 과학과 성취기준 전문
  │   │   ├── /graph-linker               # 링크 후보 탐지·그래프 구성·배치
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # detect_links.py, build_graph.py, layout.py
  │   │   │   └── /references            # concept_registry 스키마 (금지쌍은 /docs/homonyms.yaml 참조)
  │   │   ├── /item-ingest                # 기출 문항 분할·정답표 파싱
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # split_items.py, parse_answers.py
  │   │   ├── /quiz-builder               # 카드 → OX·단답·선택형 파생
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # derive_ox.py, derive_short.py, derive_mcq.py
  │   │   ├── /rights-tagger              # 권리 메타·대장·상한 검사
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # tag.py, ledger.py, quota.py
  │   │   │   └── /references            # 저작권법 제25조 요건 체크리스트
  │   │   ├── /review-packager
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # build_review.py
  │   │   ├── /loglyzer                   # 학습 로그 집계
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # aggregate.py
  │   │   └── /content-deploy             # 버전·매니페스트·Supabase diff
  │   │       ├── SKILL.md
  │   │       └── /scripts               # version.py, manifest.py, supabase_diff.py
  │   └── /agents
  │       ├── /textbook-analyst   /AGENT.md
  │       ├── /concept-writer     /AGENT.md
  │       ├── /fact-validator     /AGENT.md
  │       ├── /graph-curator      /AGENT.md
  │       ├── /item-curator       /AGENT.md
  │       └── /quiz-reviewer      /AGENT.md
  ├── /inbox
  │   ├── /textbook                       # 천재교과서 PDF (교사가 투입)
  │   └── /exam                           # 평가원 기출 PDF
  ├── /output
  │   ├── /source  /concepts  /graph  /items  /quiz          # 개념·문항·지도·오늘 계열
  │   ├── /courses  /network                                  # ★노선 계열 (SciMetro)
  │   ├── /media  /validation  /review  /rights  /deploy  /insights  /logs
  ├── /docs
  │   ├── unit_backlog.yaml
  │   ├── course_backlog.yaml              # ★노선 계열 백로그
  │   ├── concept_registry.yaml            # ★공용 — 개념 키 사전 (사람 관리)
  │   ├── homonyms.yaml                    # ★공용 — 동음이의 금지쌍
  │   ├── naming_policy.md                 # ★공용 — 표기 정책
  │   ├── misconceptions.yaml              # 교사 보유 오개념 (사람 관리)
  │   ├── rights_policy.md                 # 저작권 이용 근거·게재 조건·상한·access_tier 판정 기준
  │   ├── data_sources.md                  # 허용/금지 데이터 소스와 라이선스
  │   └── app_contract.md                  # 앱 인터페이스 계약
  └── /app                                 # Next.js + Supabase 앱 (별도 리포 분리 가능)
```

**노선 계열의 스킬·서브에이전트**(`chem-facts`·`earth-facts`·`bio-facts`·`phys-facts`, `network-graph`, `course-designer`·`content-writer`·`fact-validator`·`playtest-reviewer`·`network-curator`)는 같은 `/.claude` 아래에 나란히 배치된다. 상세는 `SciMetro_에이전트_설계서.md` §6.1을 따른다. **★ 표시 자산은 두 계열이 같은 파일을 참조하며, 복사본을 만들지 않는다**(§0.3).

### 7.2 CLAUDE.md 핵심 섹션 목록

> 상세 본문은 구현 시 작성. 아래는 **섹션 목록과 담아야 할 것**만 정의.

1. **역할 정의** — 오케스트레이터. 직접 카드를 쓰지 않고 서브에이전트에 위임
2. **네 파이프라인의 상태 기계** — A(C0~C10) / B(Q0~Q6) / C(G1~G3) / D(D1~D3), 현재 단계 판별 규칙, C9 완료 시 D1·G1 트리거 조건
3. **서브에이전트 호출 규칙** — 언제 누구를, **서브에이전트 간 직접 호출 금지**
4. **데이터 전달 규칙** — 중간 산출물은 `/output/`에 파일로, 프롬프트에는 경로만
5. **실패 처리 정책** — 단계별 재시도 횟수, 에스컬레이션 조건, 스킵 허용 단계
6. **★사람 게이트 규칙** — C8·Q6·G3 우회 절대 금지, `verified_by` 기록 방법, 부분 승인 처리
7. **★저작권 규칙** — 이용 근거, 크롭 상한, 권리 메타 없는 자산 배포 금지, 원문 재서술 의무, 배포 전 버킷 공개 설정 점검
8. **표기 정책** — 현행 표기 고정, 구표기 별칭 처리
9. **⚠️ 금지 라이브러리·소스** — PyMuPDF(AGPL), 시중 문제집·해설 복제
10. **금지 사항** — 미승인 명제로 문항 만들지 않기, 배포 자동 재시도 금지, 개념 사전 무단 갱신 금지, 원문 텍스트 배포 금지
11. **로깅 규칙** — 모든 스킵·에스컬레이션·권리 판단은 `/output/logs/`와 `rights/ledger.jsonl`에 사유 기록

### 7.3 에이전트 구조

**서브에이전트 분리를 채택한다.** 근거:

- **도메인 지식이 층위별로 완전히 다르다** — 교과서 구조 파악(교재 편집), 카드 작성(표기 규칙·명제 형식화), 검증(성취기준 전문·결함 패턴), 그래프(개념 온톨로지·동음이의), 문항(평가 문항 분석), 퀴즈(출제 기술)
- **references 총량이 크다** — 성취기준 전문 + 표기 사전 + 편수용어 + 동음이의 목록 + 저작권 체크리스트를 항상 로드하는 것은 명백히 비효율. **컨텍스트 최적화 효과가 결정적**
- 파이프라인 단계가 명확히 분리되어 **독립 작업 블록** 조건 충족

| # | 서브에이전트 | 담당 | 역할 |
|---|-------------|------|------|
| 1 | `textbook-analyst` | C1 | 단원 구조 파악, 개념 후보·위계 추출 |
| 2 | `concept-writer` | C2 | 카드 전 필드 작성 (정의 재서술·명제 형식화) |
| 3 | `fact-validator` | C5, C6 | 교육과정 범위 판단 + 명제 결함 탐지 |
| 4 | `graph-curator` | G2 | 링크 유효성 판단, 개념 사전 갱신 제안 |
| 5 | `item-curator` | Q3, Q4 | 기출 개념 매핑 + 자체 해설 작성 |
| 6 | `quiz-reviewer` | D2, C10 | 데일리 문항 품질 검토 + 학습 로그 해석 |

C0/C3/C4/C7/C8/C9, Q0~Q2/Q5/Q6, G1/G3, D1/D3은 메인 에이전트가 스킬을 직접 호출한다(판단 요소가 적음).

**과목 확장 방식**: 생명·지구·물리로 넓힐 때 서브에이전트는 늘리지 않는다. `term-lexicon`의 references에 **과목별 용어집을 추가**하고 `curriculum-mapper`에 성취기준을 더하면 된다. 과목 특유의 사실 검증은 노선 계열이 이미 갖춘 `<track>-facts` 스킬 4종(`chem`/`bio`/`earth`/`phys`)을 **그대로 호출**한다 — §0.3의 공용 자산이므로 복사하지 않는다.

**노선 계열과의 관계**: SciMetro의 서브에이전트 5종(`course-designer`·`content-writer`·`fact-validator`·`playtest-reviewer`·`network-curator`)은 별개로 유지한다. 이름이 겹치는 `fact-validator`는 **역할이 다르므로**(노선 = 스크립트 결과 보고, 개념 = 명제 결함 탐지) 구현 시 `concept-fact-validator` / `course-fact-validator`로 분리 명명한다.

### 7.4 서브에이전트 상세

| 항목 | textbook-analyst |
|------|-----------------|
| 트리거 | 메인이 C1 진입 시 (C0 산출물 존재) |
| 입력 | `text.jsonl` 경로 + 성취기준 references 경로 + 기존 개념 목록 경로 + `concept_registry.yaml` 경로 |
| 출력 | `<unit-id>.candidates.json` |
| 참조 스킬 | `curriculum-mapper`, `concept-schema` |
| 핵심 지식 | 교과서 편집 구조, 개념/사례/활동 구분, 단원 내 위계 판단 |

| 항목 | concept-writer |
|------|----------------|
| 트리거 | 메인이 C2 진입 시 |
| 입력 | `candidates.json` 경로 + 원문 경로 + `misconceptions.yaml` 경로 |
| 출력 | `<concept-id>.draft.json` |
| 참조 스킬 | `term-lexicon`, `concept-schema`, `typeability-check`, `originality-check` |
| 핵심 지식 | 현행 표기, **관계 명제 형식화 규칙**, 재서술 원칙, 힌트·설명 문체 |
| 특기 | 원문을 **구조적으로** 다시 쓴다. 동의어 치환은 재서술이 아니다 |

| 항목 | fact-validator |
|------|----------------|
| 트리거 | 메인이 C5 또는 C6 진입 시 |
| 입력 | `draft.json` 경로 + 검사 모드(`curriculum` \| `relation`) |
| 출력 | `report.json`의 해당 절 |
| 참조 스킬 | `curriculum-mapper`, `chem-facts`(화학 사실 대조 가능 항목만) |
| 핵심 지식 | 성취기준 전문, 명제 결함 패턴(과일반화·동어반복·모순·scope 누락) |
| 특기 | **진위를 확정하지 않는다.** 결함을 지적하고 사람 게이트로 넘긴다 |

| 항목 | graph-curator |
|------|----------------|
| 트리거 | 메인이 G2 진입 시 |
| 입력 | `link_candidates.json` 경로 + 관련 카드 경로 목록 |
| 출력 | `link_verdicts.json` (valid/invalid/uncertain + 유형 + 근거) |
| 참조 스킬 | `graph-linker` |
| 핵심 지식 | 과학 개념 온톨로지, **동음이의 금지쌍**, 학년 간 심화 관계 판단 |
| 특기 | 자동 확정 권한 없음. 사전 갱신은 사람 승인 |

| 항목 | item-curator |
|------|----------------|
| 트리거 | 메인이 Q3 또는 Q4 진입 시 |
| 입력 | 문항 메타·크롭 경로 + 개념 카드 인덱스 경로 |
| 출력 | `<item-id>.json` (개념 매핑 + 해설) |
| 참조 스킬 | `concept-schema`, `curriculum-mapper` |
| 핵심 지식 | 수능 문항 유형 분석, 개념–문항 대응, **자체 해설 작성 원칙** |
| 특기 | 해설은 **카드의 명제를 인용해** 쓴다. 시중 해설을 참조하지 않는다 |

| 항목 | quiz-reviewer |
|------|----------------|
| 트리거 | 메인이 D2 진입 시, 또는 C10 튜닝 요청 시 |
| 입력 | 파생 문항 풀 경로 (D2) / 집계 로그 경로 (C10) |
| 출력 | `<concept-id>.quiz.json`(폐기 표시 포함) / `<unit-id>.tuning.md` |
| 참조 스킬 | `quiz-builder`, `loglyzer` |
| 핵심 지식 | 문항 제작 원리(변별도·매력적 오답), 학습과학(인출 연습·간격 반복), 고교생 문해 수준 |

**서브에이전트 간 직접 호출은 금지**한다. 모든 조율은 메인 에이전트를 통한다.

### 7.5 앱 인터페이스 계약 (구조 개요)

**아키텍처**: Next.js (App Router) + Supabase (Postgres · Auth · Storage · RLS)

| 테이블 | 역할 | 비고 |
|--------|------|------|
| `concepts` | 개념 카드 본문 | 파이프라인이 upsert. **공개** |
| `concept_links` | 개념 링크 | 그래프 파이프라인 산출. 공개 |
| `media_assets` | 시각자료 메타 | **`access_tier`**로 공개/제한 분기 (§0.4) |
| `items` | 기출 문항 | 메타는 공개, **이미지 자산은 `restricted`** |
| `quiz_items` | 데일리 문항 풀 | `quiz_ready`인 명제 파생분만 |
| **`courses`** | **노선 코스 데이터** | **★노선 계열.** 공개 |
| **`network`** | **노선망·환승 관계** | **★노선 계열.** 공개 |
| **`runs`** | **코스 완주 기록·랭킹** | **★노선 계열.** 서버 재계산 검증 |
| `students` | 학생 계정 | 초대 코드 가입, **학번 별칭**(R13). **★계열 공용** |
| `study_states` | **개념별** FSRS 상태 | **★계열 공용** — 노선 완주가 여기에 반영된다 |
| `attempts` | 응답 기록 | C10 튜닝 입력, 선택지 분포 포함 |
| `daily_sets` | 일자별 출제 세트 | 재현·복기 가능하도록 저장 |
| `access_grants` | 저작물 열람 권한·만료일 | R3 학기 만료. **`restricted` 자산에만 작용** |

**앱 화면 5탭 — §0.2의 계열과 1:1 대응**

| 탭 | 계열 | 내용 | 로그인 |
|----|------|------|--------|
| 개념 | 개념 | 단원 트리 → 카드. 링크 탭 시 대상 카드로 이동. `same` 링크는 "통합과학1에서 배운 개념" 배지 | 불필요 (그림만 잠김) |
| 지도 | 지도 | 개념 그래프. 허브·심화 체인 강조, 학습 완료 카드 색칠, **노선 코스도 노드로 표시** | 불필요 |
| 문제 | 문항 | 단원별 기출. 이미지 + 정답 + 자체 해설, 개념 카드로 점프 | **필요** |
| 오늘 | 오늘 | FSRS 기반 10문항. 결과가 `study_states`에 반영 | 필요 (기록 저장) |
| 노선 | 노선 | 타이핑 코스 완주·랭킹·실시간 멀티 | 불필요 (랭킹 등록만 필요) |

**계열 간 연결 지점** — 통합의 실익이 실제로 발생하는 곳이다.

- 개념 카드 → 그 개념이 등장하는 **노선 코스**로 이동 (`concept_key` 일치)
- 노선 완주 → 등장 개념들의 `study_states` 갱신 (인출 1회로 계산)
- 기출 문항 → 연결된 개념 카드 → 그 개념의 노선
- 지도 탭에서 카드와 코스를 **한 그래프에** 표시

**★저작권 필수 기능 (R1 — 선택 아님, `restricted` 자산에 한정)**

| 요건 (저작권법 제25조 제12항 취지) | 구현 |
|-----------------------------------|------|
| **접근제한조치** | Supabase Auth + 초대 코드 + RLS. `restricted` 자산은 비로그인 접근 0. 해당 자산 URL의 검색엔진 색인 차단 |
| **복제방지조치** | `restricted` 미디어는 **비공개 버킷 + 단시간 서명 URL**. 다운로드·우클릭 저장 차단, 인쇄 스타일 차단 |
| **경고문구 표시** | 문항·교과서 그림 하단에 저작권 보호 문구 상시 노출 |
| **출처 표기** | 자산마다 출처 자동 렌더 (권리 메타에서) |
| 이용 대상 한정 | `access_grants` 만료일 — 학기 종료 시 자동 차단 |
| 감사 추적 | `rights/ledger.jsonl` + 앱 열람 로그 |

> **`access_tier` 오분류가 이 설계의 단일 실패 지점이다.** 자산 하나가 실수로 `public`이 되면 §0.4의 논리 전체가 무너진다. 그래서 판정을 사람 판단에 맡기지 않고 **출처 유형에서 결정론적으로 도출**한다 — `rights.holder`가 자체(교사)가 아니면 무조건 `restricted`. C9·Q6 배포 직전에 이 규칙을 재검사하고, 불일치 1건이라도 있으면 배포를 중단한다(§8 권리 지표).

> 법령이 전송 시 요구하는 조치에는 "전송 관련 보상금 산정 장치"도 포함되나, 고등학교 이하 학교는 제6항에 따라 보상금 지급 의무 자체가 면제되므로 이 앱에서는 해당 장치를 두지 않는다. 대신 열람 로그로 이용 범위를 스스로 증빙할 수 있게 한다. 이 판단은 `rights_policy.md`에 근거와 함께 기록한다(§10-1).

**핵심 인터페이스 계약**: 앱은 `concept.schema.json` / `item.schema.json` / `quiz.schema.json` / `graph.schema.json`을 따르는 데이터만 소비한다. 파이프라인과 앱의 유일한 접점이며, 스키마 변경은 버전 발급을 동반한다.

### 7.6 데이터 전달 패턴

| 데이터 | 방식 | 근거 |
|--------|------|------|
| 교과서 원문·레이아웃 | **파일 기반** (`/output/source/`) | 대용량, 대조용 |
| 카드 candidates / draft / 최종본 | **파일 기반** (`/output/concepts/`) | 카드 수십 개, 구조화 |
| 검증 리포트 | **파일 기반** (`/output/validation/`) | 카드별 상세 사유 |
| 링크 후보·판정·그래프 | **파일 기반** (`/output/graph/`) | 전체 카드 교차 참조 |
| 문항·미디어 | **파일 기반** (`/output/items/`, `/media/`) | 바이너리 |
| 단계 진입 지시·검사 모드·재시도 사유 | **프롬프트 인라인** | 짧고 단순 |
| 사람 승인 여부 | **파일 기반** (`review.md` 플래그, `verified_by`) | 감사 추적 필요 |

원칙: **서브에이전트에는 파일 경로만 전달**하고 내용을 프롬프트에 붙여넣지 않는다.

### 7.7 주요 산출물 파일 형식

| 산출물 | 형식 | 스키마 |
|--------|------|--------|
| 개념 카드 | JSON | `concept.schema.json` |
| 개념 그래프 | JSON | `graph.schema.json` |
| 링크 후보/판정 | JSON | `link.schema.json` |
| 문항 | JSON + 이미지 | `item.schema.json` |
| 데일리 문항 풀 | JSON | `quiz.schema.json` |
| 시각자료 메타 | JSON | `media.schema.json` |
| 검증 리포트 | JSON | `report.schema.json` |
| 교사 검토 패키지 | Markdown | 템플릿 고정 |
| 개념 사전 / 오개념 목록 | YAML | 사람 관리, 스키마 검증만 |
| 권리 대장 | JSONL | 자산 1건 = 1행 |
| 배포 매니페스트 | JSON | `manifest.schema.json` |
| 실행 로그 | JSONL | 단계·결과·사유·타임스탬프 |

---

## 8. 성공 기준 (시스템 레벨)

| 층위 | 지표 | 목표 |
|------|------|------|
| **정확성 (하한선)** | 배포 카드의 과학 오류 | **0건** (타협 불가) |
| 정확성 | 자동 검증이 잡아낸 형식 오류 / 전체 형식 오류 | ≥ 95% |
| **권리 (하한선)** | 권리 메타 없는 자산의 배포 | **0건** |
| 권리 | **`restricted` 자산의 공개 URL 노출** | **0건** |
| 권리 | `access_tier` 오분류 (출처 유형 대비) | **0건** — 배포 차단 조건 |
| 권리 | 원문 12어절 이상 일치 카드 배포 | **0건** |
| **학습 효과** | 3회차 정답률 / 1회차 정답률 | ≥ 1.5배 |
| 학습 효과 | 데일리 테스트 7일 연속 수행률 | ≥ 40% |
| 학습 효과 | **`same` 링크를 통한 하위 학년 카드 진입률** | ≥ 20% *(개념 연계의 존재 이유)* |
| 학습 효과 | 오개념 기반 OX의 초회 오답률 | 40~70% *(너무 낮으면 함정이 아니고, 너무 높으면 카드가 부실)* |
| **효율** | 단원 1개당 교사 개입 시간 | ≤ 20분 (C8 검토만) |
| 효율 | 교과서 투입 → C8 도달 자동 처리 성공률 | ≥ 70% |
| 효율 | 기출 회차 1개 처리 시간 | ≤ 30분 (Q6 검토 포함) |
| **그래프** | `prereq` 순환 | **0건** |
| 그래프 | 고립 카드 비율 | ≤ 10% |
| 그래프 | G2 `uncertain` 비율 | ≤ 20% |

---

## 9. 착수 순서 권장안

**전 단원을 동시에 시작하지 않는다.** 파이프라인이 한 바퀴 완주하는 것이 먼저다.

> **1인 개발에서 우산을 먼저 세우면 양쪽 다 미완성으로 끝난다.** 플랫폼 통합(P9)을 결정했다고 해서 두 계열을 동시에 착수하는 것이 아니다. **0차에서 공용 자산만 분리**해두고, 나머지는 각 설계서의 순서를 그대로 지킨다. 우산은 한쪽이 완주한 뒤에 씌워도 늦지 않다.

| 단계 | 대상 | 이유 |
|------|------|------|
| **0차** | **공용 자산 분리** — `concept_registry.yaml`, `homonyms.yaml`, `naming_policy.md`, 공통 스킬 4종, `students`·`study_states` 스키마 | 나중에 합치면 이미 갈라진 뒤다. **이것만 먼저** 하고 계열 통합은 미룬다 |
| **1차** | 화학 `물질의 구조와 성질` 1개 단원, 카드만 (C0~C9) | 증기 압력·분자 간 힘처럼 **관계 명제가 선명한** 단원. 파이프라인 전체를 가장 짧게 검증 |
| **2차** | 같은 단원에 D1~D3 가동 | 카드가 실제로 문항으로 파생되는지, `invertible` 판정이 버티는지 확인 |
| **3차** | 통합과학1 `물질과 규칙성` 화학 단원 추가 → G1~G3 가동 | **`same` 링크가 실제로 생기는지**가 이 앱의 성패. 카드가 두 학년에 걸쳐야 검증 가능 |
| **4차** | 기출 1회차로 B 파이프라인 (Q0~Q6) | 개념 카드가 쌓인 뒤라야 Q3 매핑이 의미를 가짐 |
| **5차** | 화학 나머지 3개 영역 확대 | 반복 운영 |
| **6차** | 진로선택(물질과 에너지 / 화학 반응의 세계) | 성취기준 코드 확정 후 |
| **7차** | **노선 계열 통합** — 앱 5탭 연결, 계열 간 이동, `study_states` 상호 반영 | 개념 계열과 노선 계열이 **각각 완주한 뒤**. SciMetro는 자체 순서(지질시대 → BIO → CHEM)를 따로 돈다 |

1차만으로 C0~C9가 한 바퀴 돌아야 하며, 그 전에 2차로 넘어가지 않는다. 7차의 통합 작업은 §0.3의 공용 자산이 0차에서 제대로 분리돼 있으면 **연결 코드만 쓰는 일**이 된다. 0차를 건너뛰면 7차가 데이터 마이그레이션이 된다.

---

## 10. 열린 질문 / 다음 단계

1. **저작권 운영 방침 확정** — `rights_policy.md`에 이용 근거·크롭 상한·게재 조건·학기 만료 정책을 문서로 고정한다. 특히 **평가원 문항 이미지의 게재 범위**는 학교 관리자와 한 번 상의해두는 편이 안전하다. 이 설계는 "수업 참여 학생 한정 + 4대 조치"를 전제로만 성립한다
2. **진로선택 과목 성취기준 코드** — `물질과 에너지`, `화학 반응의 세계`의 영역·코드 체계를 NCIC 원문에서 확정해 references에 넣어야 C5가 동작한다
3. **오개념 시드 구축** — `misconceptions.yaml`을 몇 개로 시작할지. 교사가 가진 현장 데이터가 이 앱의 가장 큰 차별점이므로, 1차 단원에서 20~30개는 직접 넣는 편이 좋다
4. **동음이의 금지쌍 목록** — `homonyms.yaml`(주기·핵·전위·일·계 등)을 미리 만들 것인지. **노선 계열의 환승 판정도 같은 파일을 쓰므로 0차 대상이다**
5. **`invertible` 판정 기준의 정밀화** — 1차 단원 운영 후 오판 사례를 모아 규칙으로 승격
6. **교과서 판본 관리** — 천재교과서 개정판이 나오면 카드의 `media.source` 페이지가 어긋난다. 판본 필드를 카드에 둘 것인지
7. **앱 리포 분리 여부** — 파이프라인과 앱을 한 리포로 갈지, JSON만 주고받는 별도 리포로 갈지. 계열이 다섯이 되었으므로 v0.1보다 무거운 결정이 되었다
8. ~~SciMetro와의 자산 공유~~ → **v0.2에서 해결.** §0.3의 공용 자산으로 확정, 복사본 금지
9. **노선 완주의 인출 가중치** — 코스 완주가 `study_states`에 몇 회 인출로 반영될지. 타이핑은 재인(recognition)에 가까우므로 카드 인출과 같은 무게를 주면 과대평가된다. 1차 통합 후 로그로 보정
10. **비로그인 사용자의 학습 기록** — 공개 계층(카드·지도·노선)만 쓰는 학생의 기록을 로컬에 둘지, 익명 계정을 발급할지

---

## 부록 A. 참고 자료

**교육과정**

- [2022 개정 교육과정 / 과학과 / 고등학교 / 통합과학 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교/통합과학) — 통합과학1·2 영역과 성취기준 코드 체계
- [2022 개정 교육과정 / 과학과 / 고등학교 / 화학 — 나무위키](https://namu.wiki/w/2022%20개정%20교육과정/과학과/고등학교/화학) — 화학 4개 영역(12화학01~04)

**저작권**

- [저작권법 제25조(학교교육 목적 등에의 이용) — 국가법령정보센터](https://law.go.kr/LSW//lsLawLinkInfo.do?lsJoLnkSeq=900605120&lsId=000798&chrClsCd=010202&print=print) — 제3항 수업 목적 이용, 제6항 고교 이하 보상금 면제, 제12항 복제방지 등 필요 조치
- [교육 목적 등에의 이용 — 찾기쉬운 생활법령정보](https://www.easylaw.go.kr/CSP/CnpClsMainBtr.laf?popMenu=ov&csmSeq=695&ccfNo=3&cciNo=2&cnpClsNo=6) — "공표된 저작물의 일부분", 전송 시 4대 조치(접근제한·복제방지·경고문구·보상금 산정 장치), 출처 명시
- [수능 기출문제에 저작권이 있을까? — 한국저작권보호원](https://www.copycat.or.kr/55) — 기출 문항의 저작권은 평가원에 귀속, 출판 등 상업적 이용 제한

**용어·검증 소스**

- [대한화학회 화학술어](https://new.kcsnet.or.kr/cheminfo) — 화학 용어 한글·영문 대조
- [국립국어원 표준국어대사전](https://stdict.korean.go.kr/) — 한자어 표기 대조 (오픈API 제공)

**도구**

- [py-fsrs](https://github.com/open-spaced-repetition/py-fsrs) — FSRS 간격 반복 스케줄러, **MIT 라이선스**
- [PyMuPDF 라이선스 안내](https://pymupdf.readthedocs.io/en/latest/about.html) — **AGPL/상용 이중 라이선스** (본 설계에서 제외한 근거)

**같은 플랫폼의 다른 계열**

- `SciMetro_에이전트_설계서.md` v0.3 — **Sci_Map 노선 계열**. 코스 파이프라인(S0~S9)·노선망(N1~N3)·도메인별 사실 검증기 매트릭스
- `ChemTyping_에이전트_설계서.md` v0.1 — SciMetro의 전신(화학 단독). 화학 사실 검증 도구(OPSIN/RDKit/PubChem)의 최초 정의
